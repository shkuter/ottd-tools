## 1. Каскад

- [ ] 1.1 Добавить `mantine-datatable` в объявление слоёв `web/src/layers.css`
      (`@layer mantine, mantine-datatable, app`). Проверка: у шапки каталога граница снизу
      берётся из скина, а не `1px` из библиотеки
- [ ] 1.2 Перенести правила таблиц с голых тегов на компоненты: липкая шапка (`thead th`),
      подсветка строки (`tbody tr:hover`), плотность ячеек (`th, td`) должны адресовать
      `.mantine-Table-*`. Проверка: на всех трёх вкладках со списками шапка по-прежнему липкая,
      строка подсвечивается, плотность прежняя
- [ ] 1.3 Удалить правила по голым `button`, `input`, `select` из `web/src/index.css`.
      Проверка: у числового поля появились стрелки (сейчас `padding: 5px 10px` от `button`
      сдавливает svg до нулевой ширины)
- [ ] 1.4 Снять префикс `:root[data-skin]` со всех селекторов `skin.css` и `skin-mantine.css`,
      удалить строку `document.documentElement.dataset.skin` из `web/src/main.tsx`. Проверка:
      просмотр diff целиком и обход вкладок — вид не изменился
- [ ] 1.5 Удалить мёртвый набор токенов из блока `:root` в `index.css` (`--bg`, `--accent`,
      `--profit` и остальные): он перекрыт скином с первого рендера. Проверка: поиск по коду не
      находит использований, вид не изменился

## 2. Цвета по палитре

- [ ] 2.1 Поверхности и грани: заливка окна `grey.light`, утопленная `grey.lighter`, светлая
      грань `grey.lightest`, тёмная `grey.dark`. Проверка: утопленное поле видно светлее
      поверхности, как в игре
- [ ] 2.2 Кнопки и переключатели: заливка нажатой кнопки `yellow.lighter` вместо `lightest`
      (`lightest` занят светлой гранью)
- [ ] 2.3 Роли текста: основной `TC_BLACK`, заголовки `TC_GOLD`, ссылки `TC_ORANGE`, текст на
      тёмном `TC_WHITE`, приглушённый — самый тёмный оттенок темы. Проверка: приглушённый текст
      отличим от основного (сейчас оба `#000`)
- [ ] 2.4 Поле ввода: заливка `PC_BLACK`, введённое значение `TC_YELLOW`
- [ ] 2.5 Выбранный пункт выпадающего списка (`PC_BLACK` + `TC_WHITE`) и подсказка (заливка
      `PC_LIGHT_YELLOW`, рамка `PC_BLACK`). Проверка: выбранный пункт отличим от пункта под
      курсором
- [ ] 2.6 Недоступное состояние: штриховка шахматкой оттенком `SHADE_DARKER` поверх контрола
- [ ] 2.7 Тень: `--skin-text-shadow` назначается ролям (заголовок, ссылка, текст на тёмном), но
      не основному и не приглушённому тексту

## 3. Масштаб

- [ ] 3.1 Ввести `--skin-scale` и выразить через него бевель и отступы виджетов из
      `WidgetDimensions::unscaled`
- [ ] 3.2 Шкалы в `web/src/theme.ts`: шрифты (`xs` 9, `sm` = `md` 15, `lg` 21, `xl` 27) и высоты
      контролов (`xs` 12, `sm` = `md` 18, `lg` 24, `xl` 30). Проверка: контролы стали игрового
      размера без простановки `size` в коде страниц
- [ ] 3.3 Плотность строк списков из метрики `matrix` (2/3/2/1 при 1x)

## 4. Темы окон

- [ ] 4.1 Разложить токены темы `grey` в `:root` — база, которую получают оболочка и все вкладки
- [ ] 4.2 Добавить `[data-window="mauve"]`: поверхности из группы `mauve`, основной текст
      `TC_WHITE`, приглушённый `TC_SILVER`, подписи полей `TC_LIGHT_BLUE`. Проверка: вкладка
      настроек выглядит как окно настроек игры, приглушённые пояснения читаются
- [ ] 4.3 Выставлять `data-window` на `<html>` из `App` по таблице маршрутов рядом с массивом
      вкладок. Проверка: переход на настройки и обратно перекрашивает окно; выпадающий список на
      настройках окрашен темой вкладки, хотя рендерится в портал
- [ ] 4.4 Завести `[data-window="brown"]` и `[data-window="dark-green"]` как задел под будущие
      списки предприятий — с полным набором токенов, но пока без вкладок

## 5. Страница элементов

- [ ] 5.1 Собрать `web/src/features/kit/KitPage.tsx`: кнопки во всех состояниях, поля,
      выпадающие списки, переключатели, вкладки, таблица, подсказка, всплывающее сообщение,
      полоса прокрутки, образцы текста всех ролей
- [ ] 5.2 Показать набор в каждой цветовой группе окна, которую поддерживает оболочка
- [ ] 5.3 Добавить маршрут `/kit`: лениво, вне массива вкладок. Проверка: страницы нет в
      навигации, её код не попадает в основной чанк
- [ ] 5.4 Добавить строки страницы в `web/src/i18n/en.json` и `ru.json`. Проверка:
      `i18n/__tests__/locales.test.ts` проходит

## 6. Проверка и завершение

- [ ] 6.1 Тест, который читает `skin.css` и падает на цвете, отсутствующем в
      `opengfx2_palette.json` (со списком исключений — фон графа FIRS рисует graphviz)
- [ ] 6.2 Обход всех вкладок в браузере на предмет читаемости: заголовки, приглушённый текст,
      прибыль и убыток, выбранные пункты списков
- [ ] 6.3 Решить открытый вопрос масштаба (1.5x или 2x) на собранной странице элементов и
      зафиксировать значение
- [ ] 6.4 Запись в `CHANGELOG.md` в секцию `[Unreleased]` (на английском)
- [ ] 6.5 Прогон `make test`
