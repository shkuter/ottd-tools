## 1. Данные адаптера

- [ ] 1.1 В `web/src/vanilla.ts` выдавать секции ванильной машины ту же вместимость по всем
      пяти столбцам, что и самой машине, с комментарием о том, что у базовой игры
      GRF-параметра нет; проверка — кейс в `web/src/__tests__/vanilla.test.ts`: у вагона
      совпадают `capacities` машины и её секции

## 2. Проверка расчёта

- [ ] 2.1 Завести кейс на то, что тормозная растяжка ванильного грузового состава одинакова при
      любом выбранном столбце вместимости и длиннее собственной длины состава (множитель
      грузовых поездов больше единицы, реалистичное торможение); проверка — кейс падает на
      прежнем адаптере и проходит на новом

## 3. Документация и завершение

- [ ] 3.1 Дописать в `## [Unreleased]` → `### Fixed` файла `CHANGELOG.md` (на английском)
      запись с пометкой **BREAKING**: какие числа изменились и почему; проверка —
      `scripts/next-version.sh` даёт minor
- [ ] 3.2 Прогнать `make test`; проверка — набор зелёный
