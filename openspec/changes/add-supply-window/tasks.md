## 1. Данные

- [ ] 1.1 Выгрузить длину окна в тиках в `meta` набора (`meta.supply_window_ticks`) в
      `pipeline/extract_firs.py` — одно значение на весь набор. Режим приёма и тип предприятия
      не дублировать: `accept_mode` уже лежит по экономикам, тип правила выводится из `type`
- [ ] 1.2 Выгрузить пороги и бонусы накопительного пула: базовые значения параметров NewGRF и
      множитель типа предприятия
- [ ] 1.3 Эталоны в `pipeline/tests/test_known_values.py`: окно 6912 тиков, вторичка с
      обязательными входами, вторичка «любые три из пяти», шахта 16/80, порт 128/640, бонусы
      производства 150 % и 250 %
- [ ] 1.4 Прогнать `make data` и убедиться, что диффом в `industries.json` затронуты только
      новые поля

## 2. Модуль снабжения

- [ ] 2.1 Создать `web/src/engine/supply.ts`: конверсия вторички по сумме входных ratio под
      потолком 8 и выпуск по каждому выходному грузу — с домножением на выходной ratio под тем
      же делителем 8
- [ ] 2.2 Отношение интервала к окну и минимальный парк, удерживающий предприятие поданным
      (`trainsForWindow`)
- [ ] 2.3 Накопительный пул: объём за окно против двух порогов, бонус производства по уровню
- [ ] 2.4 Пороги промежуточной зоны — именованные константы с пометкой о сверке в игре
- [ ] 2.5 Признак «правило неизвестно» для типов вне двух правил (`IndustryTertiary`,
      `IndustryPrimaryNoSupplies`, городские производители) — отдельный результат, а не
      молчаливый ноль
- [ ] 2.6 Перевод единиц в одном месте: окно в тиках против интервала в движковых днях
      (`DAY_TICKS = 74`); проверить тестом, что `dayLengthFactor` отношение не двигает
- [ ] 2.7 Тесты `supply.test.ts`: все входы поданы, один выпал, «любые три из пяти» (пять,
      шесть и семь входов), выходной ratio урезает выпуск, граница отношения, пул по обоим
      порогам, неизвестное правило, выпуск не задан (не `NaN`)

## 3. Обратный индекс грузов

- [ ] 3.1 Построить в `web/src/dataset.ts` индекс «груз → принимающие предприятия» по активной
      экономике (`activeEconomy(game)`), с кешем по `economy.id`
- [ ] 3.2 Тест: индекс согласован с `accepts` в данных, груз без потребителей даёт пустой список

## 4. Цель и колонка

- [ ] 4.1 Добавить цель `supply` в `OptimizeGoal` и `GoalStrategy` с лексикографическим
      сравнением, включая выбор ветки загрузки под этой целью и ответы `sweepsShorter` /
      `stopsSweep` (все допустимые длины, перебор до предела парка)
- [ ] 4.2 Считать снабжение для строки выдачи и вынести результат в `OptimizeResult`
- [ ] 4.3 Колонка в таблице Best-train: значение, сортировка, цветовая отметка
- [ ] 4.4 Подсказка колонки: интервал, длина окна, минимальный парк, оговорка про остальные
      входы предприятия
- [ ] 4.5 Выбранный получатель в `web/src/state/optimizerStore.ts` рядом с `goal`: persist и
      чтение через функцию, падающую на автоподстановку, когда получателя в текущей экономике
      нет (приём из ADR-0002)
- [ ] 4.6 Выбор получателя в интерфейсе: автоподстановка единственного, список при нескольких,
      недоступность цели при отсутствии потребителей
- [ ] 4.7 Тесты `optimize`: ранжирование по цели `supply`, выбор ветки загрузки под ней,
      недоступность цели без выпуска, прочерк в колонке без выпуска, замена получателя после
      смены экономики

## 5. Локализация

- [ ] 5.1 Строки колонки, цели и подсказки в `web/src/i18n/en.json` и `ru.json`, прогнать
      `i18n/__tests__/locales.test.ts`

## 6. Завершение

- [ ] 6.1 Обновить глоссарий `CONTEXT.md`: у термина **Goal** третья цель, рядом с уже
      описанными supply window / supply ratio / trainsForWindow
- [ ] 6.2 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском
- [ ] 6.3 Прогнать `make data`, `make test`, `make verify`
