## 1. Данные: множитель стоимости постройки пути

- [x] 1.1 В `pipeline/extract_vanilla.py` добавить в запись типа пути `cost_multiplier` из поля
      `/* cost multiplier */` таблицы `_original_railtypes[]` — тем же приёмом, что и
      `maintenance_multiplier` рядом; проверка: юнит-тест разбирает **искусственный** кусок
      таблицы, где множитель постройки и множитель обслуживания различаются, и ждёт оба на
      своих местах (сверка с числами настоящей таблицы этого не ловит — там оба поля равны)
- [x] 1.2 В `pipeline/extract_iron_horse.py` добавить `cost_multiplier` из `rt.construction_cost`
      с откатом на ванильный множитель лейбла при `None` (как уже сделано для обслуживания);
      проверка: тест сверяет извлечённое значение с атрибутом объекта источника, а не с
      константой, и отдельно ждёт ванильные 8 / 12 у `RAIL` / `ELRL`, оставленных игре
- [x] 1.3 В `pipeline/validate.py` потребовать `cost_multiplier` целым и положительным у каждого
      типа обоих наборов; проверка: `make data` проходит, а искусственно убранное поле роняет
      валидатор
- [x] 1.4 Прогнать `make data` и закоммитить перегенерированные `trains.json` и
      `vanilla_trains.json`; проверка: `git diff` показывает только добавленное поле, версии
      данных в `meta.json` не изменились

## 2. Движок: цены строительства и цена перевода

- [x] 2.1 Добавить в `BASE_PRICES` (`engine/costs.ts`) `build_rail: 100` и `clear_rail: -70` с
      ссылкой на `pricebase.h`; проверка: тест на `basePriceAfterMultipliers` для обеих цен при
      сложности строительства 0 / 1 / 2 даёт множители 6/8, 1, 9/8, а сложность расходов
      транспорта их не трогает
- [x] 2.2 Добавить кламп нуля со знаком стартовой цены (`Clamp(start_price, -1, 1)` из
      `RecomputePrices`) для этих цен — клампер из `infrastructure.ts` всегда даёт 1 и здесь
      превратил бы заработок в расход; проверка: при наименьшем множителе Base Costs цена
      постройки остаётся положительной, а цена сноса — неположительной (обнуление
      отрицательной базы округлением вниз недостижимо, см. спеку `pricing`)
- [x] 2.3 Реализовать `railBuildCost` / `railClearCost` / `railConvertCost` по `rail.h`, где
      каждый сдвиг игры записан **делением с `Math.floor`**, а усечение к нулю (`Math.trunc`)
      стоит только на отрицательной величине; проверка: юнит-тесты на обе ветки, на то, что
      переоборудование никогда не дороже перестройки, и на наибольшем множителе Base Costs —
      цена растёт и остаётся положительной (битовый сдвиг здесь заворачивается)
- [x] 2.4 Добавить настройку `basecostRailConstruction` в `GameSettings` (по умолчанию 1,
      диапазон как у остальных множителей Base Costs, действует только при включённом
      `basecostGrf`) и провести её в обе цены строительства; проверка: цена перевода куска при
      ×2 вдвое больше, при выключенном наборе Base Costs не меняется, а годовое содержание сети
      не трогается ни в одном случае

## 3. Движок: сравнение коридора

- [x] 3.1 Создать `engine/corridorUpgrade.ts` с входом из двух объектов — существующего
      `RouteWithFlowParams` и нового `CorridorInputs` (целевой тип пути, куски пути коридора,
      число поездов, локомотив замены, количества сети) — и расчётом обеих сторон через
      `routeWithFlow` с подменённым `calc.trackType` и заменёнными ведущими машинами; проверка:
      сторона «сейчас» совпадает по кругу, рейсам и доходу с прямым вызовом `routeWithFlow`
- [x] 3.2 Посчитать прирост содержания как разницу двух прогонов `networkMaintenance` с
      переносом кусков коридора и правилом `max(введено, коридор)` на исходном типе; проверка:
      тесты на коридор внутри большой сети, на коридор без введённой сети и на выключенную
      статью обслуживания (прирост 0)
- [x] 3.3 Посчитать годовую дельту из `TripEconomics.profitPerYear` обеих сторон (не
      пересобирая её из дохода и содержания) и порог загрузки арифметикой (`max(1, floor(ΔM / g) + 1)` при
      `g > 0`; при `g ≤ 0` решает один поезд — порог 1, если `g − ΔM > 0`, иначе порога нет);
      проверка: при пороге дельта положительна и не больше `g`, при пороге минус один поезд —
      отрицательна, а перевод на более дешёвый в содержании путь даёт порог 1, а не «никогда»
- [x] 3.4 Посчитать капитальные затраты (цена перевода куска × куски + новые локомотивы ×
      поезда, ноль при том же локомотиве) и год выхода в ноль от `calc.priceYear`; проверка:
      тесты на совпадение локомотива и на отсутствие года при неположительной дельте
- [x] 3.5 Отдать список кандидатов на замену (`kind === 'engine'`, `poweredOutputOn > 0`,
      `standsInBuyMenu` в год расчёта) и не считать дельту, пока машина не выбрана; проверка:
      тест на то, что список совпадает с тем, что даёт оптимизатор на том же пути и годе, а без
      выбранной машины результат — «не посчитано», а не число

## 4. Приёмочные тесты модели

- [x] 4.1 Тест «сеть под редкий трафик»: коридор ~10 000 кусков пути, единицы поездов →
      отрицательная дельта и порог заметно выше введённого числа поездов
- [x] 4.2 Тест «ровно на пороге» (случай 1986 «около нуля»): при числе поездов, равном порогу,
      дельта положительна и не превышает годовой разницы по одному поезду
- [x] 4.3 Тест «перевод без смены локомотива»: тот же локомотив на обеих сторонах → дельта
      неположительна при любой длине и любом числе поездов, порога и года выхода в ноль нет.
      Плюс тест на усечение: 12 кусков вне сети дают нулевой прирост содержания

## 5. Интерфейс

- [x] 5.1 Добавить в `state/routeStore.ts` поля коридора (целевой тип пути, куски пути, число
      поездов, id локомотива замены) с сеттерами; проверка: значения переживают перезагрузку
      (тест на persist), миграция не нужна — `merge` доливает значения по умолчанию
- [x] 5.2 Создать `features/route/CorridorUpgrade.tsx` под блоком обслуживания сети: поля ввода
      (длина — в кусках пути, с тем же правилом пересчёта тайлов, что называет блок
      обслуживания), сравнение сторон (круг, рейсы, доход рейса, содержание, скорость на
      уклоне), годовая дельта, порог загрузки, капитал и год выхода в ноль; проверка: блок
      считает при пустых количествах сети и говорит «сравнивать нечего» при невыбранном или
      совпадающем пути и при невыбранном локомотиве
- [x] 5.3 Назвать в блоке допущения: каждый поезд как показанный выше, остаточная стоимость
      снятых машин не зачитывается, круг считается по ровному пути, порог — про годовую дельту,
      а не про срок окупаемости; проверка: строки видны в отрисованном блоке
- [x] 5.4 Добавить множитель Base Costs для цен строительства пути на вкладку Settings рядом с
      остальными множителями Base Costs; проверка: переключение меняет капитал в блоке коридора
- [x] 5.5 Добавить UI-строки в `i18n/en.json` и `ru.json`; проверка: `i18n/__tests__/locales.test.ts`
      и `make check-i18n` проходят

## 6. Документация, гейты и закрытие

- [x] 6.1 Написать `docs/adr/0006-…`: пункт ADR-0005 «track construction and maintenance costs
      stay out of the model» отменён, новое правило — эти статьи входят в модель ровно
      настолько, насколько пользователь назвал количества; в ADR-0005 проставить ссылку на
      преемника; проверка: оба файла ссылаются друг на друга
- [x] 6.2 Добавить в `CONTEXT.md` термины «коридор» (участок сети, который переводят целиком,
      мерится кусками пути) и «порог загрузки» — рядом с уже описанным «куском пути»; проверка:
      термины употреблены в спеке и в UI-строках в том же значении
- [x] 6.3 Добавить кейс на `basecostRailConstruction` в `engine/__tests__/settings-effect.test.ts`
      со своей базой сравнения, отличающейся ровно этой настройкой; проверка: тест падает, если
      настройку не провести в расчёт
- [x] 6.4 Записать изменение в `CHANGELOG.md`, секция `[Unreleased]`, на английском (новая
      возможность → `### Added`, новая настройка и поле данных — там же)
- [x] 6.5 Прогнать `make data` и `make test`, затем `make verify`; проверка: всё зелёное,
      включая `make check-i18n` и визуальные проверки скина
