## 1. Пайплайн: id машин

- [x] 1.1 `extract_iron_horse.py`: экспортировать `numeric_ids` (id юнитов каждой ливреи от
      `model_def.base_numeric_id`, плюс id скрытых клонов) в `trains.json`; проверить
      `make data` и эталон в `pipeline/tests/test_known_values.py` (id известных машин против
      grf.farm/iron-horse/4.29.0).
- [x] 1.2 `extract_vanilla.py`: экспортировать ванильный `engine_id` явным полем в
      `vanilla_trains.json`; проверить `make data` + существующие тесты пайплайна.

## 2. Пайплайн: имена

- [x] 2.1 Экстрактор словников English Original из `vendor/openttd/src/table/townname.h`
      → `web/src/data/town_names.json`; проверить тестом пайплайна на паре известных сидов
      (эталонные имена сгенерировать самой игрой/по коду townname.cpp).
- [x] 2.2 Извлечь строки `STR_SV_STNAME_*` (ru/en) из lang-файлов игры и
      `nearby_station_name` индустрий FIRS (en из FIRS, ru из `vendor/firs-ru`) в словари
      i18n; проверить
      `make check-i18n` и `i18n/__tests__/locales.test.ts`.

## 3. Парсер: новые чанки

- [x] 3.1 `read.ts`/`parse.ts`: запросить `EIDS`, `IIDS`, `VEHS`, `ORDL`, `ORDR`, `STNN`, `CAPA`, `INDY`,
      `CITY`, `GRPS`, `PLYR`; экстрактор `extract/ids.ts` (индекс пула → GRFID+id; `IIDS` —
      той же формы для типов индустрий) с юнит-тестом на синтетическом чанке.
- [x] 3.2 `extract/vehs.ts`: поезда — сборка составов по `next`, глава по флагу front,
      поля engine_type/груз/загрузка/прибыли/возраст; юнит-тест на составе из
      нескольких звеньев.
- [x] 3.3 `extract/ordl.ts`: списки приказов, привязка поездов к спискам, флаги
      full load / до станции vs депо/вейпоинт; для сейвов старше версии 354 приказы
      собираются из пула `ORDR` по ссылке списка (`SLV_ORDERS_OWNED_BY_ORDERLIST`);
      юнит-тесты на общем списке двух поездов в обоих форматах — приказы внутри `ORDL`
      (354+) и в пуле `ORDR` (295–353).
- [x] 3.4 `extract/stnn.ts` + `extract/capa.ts`: станции, рейтинги и суммы ожидающего
      груза по ссылкам пакетов; юнит-тест на станции с двумя грузами.
- [x] 3.5 `extract/indy.ts`, `extract/city.ts`, `extract/grps.ts`, `extract/plyr.ts`:
      индустрии (производство, % вывоза), города (сид/имя), группы, компании; юнит-тесты
      по одному сценарию на чанк.

## 4. Модель снапшота

- [x] 4.1 `snapshot.ts`: сборка модели (в главном потоке, из свёрнутой воркером сети;
      см. design) — маршруты из общих списков приказов
      (поезд без приказов вне маршрутов), сопоставление машин каталогу через EIDS +
      `numeric_id`/ванильные id, неопознанные машины сохраняются; юнит-тесты: общий
      список → один маршрут, чужой GRF → неопознанная машина.
- [x] 4.2 Генератор имён городов (порт `MakeEnglishOriginalTownName` на TS поверх
      `town_names.json`) + имена станций (суффикс по `string_id`, индустрийные по
      `indtype`, кастомные строкой, заглушка для чужих стилей); юнит-тесты на эталонных
      сидах и «Londworth Печь»/«Londworth Furnace» по языку.
- [x] 4.3 Интеграционный тест на реальных сейвах (ваниль + JGRPP, маленькие файлы в
      фикстурах): снапшот содержит ожидаемые числа поездов/маршрутов/станций.

## 5. Хранение и флоу импорта

- [x] 5.1 `snapshotStore.ts`: IndexedDB-обёртка (один ключ `current`, запись
      `{schemaVersion, fileName, savedAt, snapshot}`), загрузка при старте, подписка для
      React; юнит-тест с fake-indexeddb: запись → чтение → устаревшая версия удаляется.
- [x] 5.2 Единый флоу: подтверждение импорта сохраняет снапшот вместе с настройками,
      отказ не сохраняет ничего; сводка снапшота (поезда/маршруты/станции) в
      `SavegameImportPanel.tsx`.
- [x] 5.3 Новые UI-строки сводки импорта в `en.json`/`ru.json`; проверить
      `i18n/__tests__/locales.test.ts`.
- [x] 5.4 Проверка в браузере: импорт реального сейва пользователя, перезагрузка
      страницы — снапшот жив; содержимое не уходит в сеть (вкладка Network пуста).

## 6. Завершение

- [x] 6.1 Запись в `CHANGELOG.md` → `[Unreleased]` → `### Added` (на английском).
- [x] 6.2 Прогон `make data` и `make test` (+ `make check-i18n`); всё зелёное.
