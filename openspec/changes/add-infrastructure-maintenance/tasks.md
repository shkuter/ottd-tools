## 1. Данные: множители обслуживания путей

- [ ] 1.1 Добавить множитель обслуживания типа пути в `pipeline/extract_vanilla.py`
      (`table/railtypes.h`) и проверить, что `vanilla_trains.json` даёт 8 / 12 / 16 / 24 для
      RAIL / ELRL / MONO / MGLV
- [ ] 1.2 Добавить множитель обслуживания в `pipeline/extract_iron_horse.py`, читая
      **атрибут** `railtype.maintenance_cost` объекта (а не объявленный kwarg), и проверить,
      что у узкоколейки в `trains.json` стоит 5, а не 7 — как уходит в GRF
- [ ] 1.3 Подставлять ванильный множитель по лейблу типу, у которого набор оставил `None`
      (RAIL / ELRL Iron Horse), и проверить сравнением двух наборов, что значения совпали
- [ ] 1.4 Дописать поле в `pipeline/validate.py` (обязательное, положительное) и убедиться,
      что `make data` проходит
- [ ] 1.5 Закрепить множители тестом `pipeline/tests/test_known_values.py` (ваниль: 8 и 12;
      Iron Horse: узкоколейка 5, метро 10, LGV 16) и прогнать его точечно

## 2. Настройки

- [ ] 2.1 Добавить `infrastructureMaintenance` и `linearMaintenance` в `GameSettings`
      (`engine/settings.ts`) со значениями по умолчанию как в игре; проверить, что
      `npx vitest run engine` зелёный
- [ ] 2.2 Ветка `migrate` в `settingsStore` для двух новых полей, с поднятием раннего выхода
      до новой версии; тест на состояние, сохранённое каждой прошлой версией
- [ ] 2.3 Кейс на `infrastructureMaintenance` в `engine/__tests__/settings-effect.test.ts`
- [ ] 2.4 Кейс на `linearMaintenance` там же, со своей базой (`base`), чтобы кейс не
      «проходил» за счёт соседнего флага

## 3. Движок

- [ ] 3.1 Добавить базовые цены обслуживания (путь 10, дорога 10, вода 8, станция 100) в
      `BASE_PRICES` (`engine/costs.ts`) и убедиться, что существующие тесты цен не сдвинулись
- [ ] 3.2 Написать в `engine/infrastructure.ts` `IntSqrt` игры (округление к ближайшему) и
      выбор множителя роста по настройке; проверить `IntSqrt` таблицей значений, включая
      числа, где округление вверх и вниз расходятся
- [ ] 3.3 Реализовать месячную стоимость категории целыми: сдвиги через
      `Math.floor(x / 2 ** n)`, множители типов путей из данных набора, ванильная таблица
      множителей дорог; проверить тестом на одной категории против формулы исходников
- [ ] 3.4 Собрать категории (путь по типам, сигналы, станции, дороги и трамвай по типам,
      каналы) и годовую сумму `monthly × 12`; размер сети — общий по путям, раздельный по
      дорогам и трамваям, собственный у сигналов, станций и каналов; проверить тестом, где
      непусты дорога и трамвай одновременно
- [ ] 3.5 Тест на восьми эталонных строках партии — все совпадают до целых
- [ ] 3.6 Тест на усечение малых количеств: 6 клеток пути дают 10 800 в год, а не 11 137
- [ ] 3.7 Тест ванильной ветки против формулы исходников на синтетических количествах, в том
      числе на числе, где `IntSqrt` округляет вверх
- [ ] 3.8 Тест на большой сети с множителем Base Costs — результат не обрезан до int32
- [ ] 3.9 Тест: множитель сложности расходов транспорта меняет суммы, строительной — нет;
      множитель инфляции цен общий с ценами машин

## 4. Вкладка «Доход рейса»

- [ ] 4.1 Добавить количества в `state/routeStore.ts` (поля + ветка `migrate`) и в
      `RoutePrefill`, чтобы пометка «из партии» шла существующим механизмом; проверить
      тестом стора, что правка снимает пометку
- [ ] 4.2 Блок «Обслуживание сети» на вкладке: поля количеств рельсовых категорий, строка на
      каждую непустую категорию, годовой итог; проверить, что итог равен сумме строк и что
      пустые категории не показываются
- [ ] 4.3 Показ «статья выключена» при выключенной настройке; проверить, что суммы нулевые
- [ ] 4.4 Строки интерфейса в `en.json` / `ru.json`: «Обслуживание инфраструктуры» — из
      русской локали игры (`russian.txt:1659`), «Линейный рост стоимости обслуживания» — наше
      имя (русской строки у JGRPP нет); проверить `i18n/__tests__/locales.test.ts`

## 5. Импорт сохранения

- [ ] 5.1 Читать `economy.infrastructure_maintenance` и `economy.linear_maintenance` из
      сохранения; отсутствующая настройка (партия не на JGRPP) читается как выключенный
      линейный рост — проверить тестом парсера на сейве без неё
- [ ] 5.2 Показывать обе среди переносимых настроек и применять при подтверждении; проверить
      тестом вкладки «Партия»
- [ ] 5.3 Предзаполнять количества сети из снапшота партии через существующий мост; проверить,
      что поля помечены как значения партии
- [ ] 5.4 Сверить годовой итог на импортированном сейве с окном инфраструктуры той же партии
      (аэропорты исключены явно) и закрепить сверку тестом на тестовом сохранении

## 6. Завершение

- [ ] 6.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском (Added)
- [ ] 6.2 Прогнать `make data` и `make test`; оба зелёные
