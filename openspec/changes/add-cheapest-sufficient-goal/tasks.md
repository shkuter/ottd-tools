## 1. Движок: цель и отбор

- [x] 1.1 Добавить `'cheapest'` в `OptimizeGoal` (`web/src/engine/optimize.ts`) и поле
      `running` в `RankKeys` (округление до целого, как у остальных ключей); проверка —
      `cd web && npx tsc --noEmit` проходит, существующие тесты движка зелёные
- [x] 1.2 Расширить `GoalStrategy` необязательным методом `refuses(row)`, возвращающим причину
      отказа (три существующие цели его не объявляют), и применить его там, где строки уходят
      в `results` — до схлопывания по «локомотив × число секций», причём `sweepsShorter` /
      `stopsSweep` по-прежнему получают нефильтрованный набор строк связки; проверка — тесты
      `optimize-branches`, `optimize-supply`, `dual-headed`, `engine` без изменений зелёные
- [x] 1.6 Спрашивать цель об обеих ветках загрузки в `forFleet` до выбора между ними:
      победитель ищется среди допустимых, строки нет только когда недопустимы обе, проигравшая
      ветка по-прежнему уходит в `otherBranch`; проверка — тест «ветка ожидания дешевле, но
      выпадает из окна получателя → строка показана в обычной ветке»
- [x] 1.3 Реализовать ветку `goalStrategy('cheapest')`: свип коротких составов, отсутствие
      обрыва свипа, `primary` по `running` по возрастанию; проверка — новый тест «при равной
      достаточности выше строка с меньшим годовым содержанием, даже если её прибыль ниже»
- [x] 1.4 Реализовать три условия отказа (`insufficiency`) для этой цели: скорость на уклоне строго больше
      минимальной внутренней единицы (именованная константа с комментарием на
      `ground_vehicle.cpp: GetAcceleration` и на `balancingSpeed`), отсутствие остатка на
      станции (`fleetLimited === false`), а при получателе, по которому есть вердикт
      (`hasVerdict(supplyRule(industry))`), — `holdsSupplied` этого вердикта; получатель без
      вердикта окном не отсеивается; проверка — тесты на каждое условие в отдельности
- [x] 1.5 Откат на `'profit'` без выпуска источника — по тому же правилу, что у
      `'transported'` и `'supply'`; проверка — тест «производство ноль → порядок совпадает с
      целью „Прибыль“»

## 2. Тесты движка

- [x] 2.1 Новый файл `web/src/engine/__tests__/optimize-cheapest.test.ts` (английский, как
      соседние файлы каталога) со сценариями спеки: дешевле при равной достаточности, равное
      содержание решает прибыль, короткий состав выигрывает по расходам, лишний поезд не
      берётся; проверка — `cd web && npx vitest run optimize-cheapest`
- [x] 2.2 Тесты отбора там же: вязнущий на уклоне состав не показывается; при упрощённой
      модели ускорения уклон не отсеивает никого; связка с недостаточным флотом показана
      бо́льшим флотом, а не отброшена; при копящейся очереди при любом флоте связки нет;
      выпадающий из окна получателя вариант не показывается; без получателя окно не
      применяется; проверка — те же тесты зелёные
- [x] 2.3 Тест «получатель без вердикта не обнуляет выдачу»: у предприятия правило
      `no-supplies` или неизвестное — выдача цели непуста, работают два других условия;
      проверка — тест зелёный
- [x] 2.4 Тест «прежние цели ничего не отсеивают»: на задаче, где есть вязнущая и не
      вывозящая связки, под целями «Прибыль», «Вывоз» и «Снабжение» эти строки остаются в
      выдаче (вязнущая — на своём месте, ограниченная парком — с пометкой), а поиск сообщает
      пустой список отвергнутого; проверка — тест зелёный. Побайтную сверку с выдачей до
      правки закрепить тестом нельзя (прежнего кода в дереве нет) — она сделана разово при
      ревью и дала совпадение по 13 полям строки на 8 задачах × 3 целях

## 3. Интерфейс

- [x] 3.1 Четвёртый пункт выбора цели в `web/src/features/optimizer/OptimizerPage.tsx` с тем
      же `disabled`, что у «Вывоза» (по осевшему производству); проверка — тест вкладки в
      `features/optimizer/__tests__/`: при нулевом производстве пункт неактивен и виден хинт,
      при заданном — выбирается и меняет порядок строк
- [x] 3.2 Пустое состояние при активной цели «Мин. расходы»: вместо пустой таблицы — три
      условия и что можно ослабить; проверка — тест вкладки в
      `features/optimizer/__tests__/`, где ни одна связка не проходит отбор, ожидает текст
      объяснения
- [x] 3.3 Строки интерфейса в `web/src/i18n/en.json` и `ru.json` (подпись цели, текст пустого
      состояния и его условия) плюс переформулировка общей строки `opt.goalNeedsProduction`:
      она показывается при любой цели, которой нужен выпуск, а сегодня называет только «вывоз»;
      проверка — `cd web && npx vitest run locales`

## 5. Правки по ревью

- [x] 5.1 Отбор возвращает причину отказа, а не «да/нет»: `Insufficiency`, `searchConsists`
      с `{ rows, refused }`, фасад `optimizeConsists`; проверка — тесты «называет условия,
      опустошившие выдачу», «молчит про уклон, который не мог сработать», «молчит про окно у
      получателя без вердикта», «под прежними целями список пуст»
- [x] 5.2 Текст пустого состояния собирается из `refused`, а не из повторённой во вкладке
      модели условий; проверка — тесты вкладки на равнине (уклон не назван) и с холмом
      (назван), плюс прежний тест про фильтр по имени машины
- [x] 5.3 Глоссарий `CONTEXT.md` и комментарии `optimize.ts`, говорившие про «обе цели»,
      приведены к четырём целям; проверка — в файлах нет упоминаний «both goals»

- [x] 5.4 Запись причины от отвергнутой ветки убрана как ненаблюдаемая: причины читаются
      только при пустой выдаче, а строка, выжившая в другой ветке, выдачу не опустошает —
      проверено перебором задач (там, где выдача пуста, причина всегда одна). Осталось то, что
      наблюдаемо: выбор ветки среди допустимых и сообщение о причине от самой строки; проверка —
      тесты «picks the branch among those it admits» и «reports the window as a reason even
      while other rows come back»
- [x] 5.10 Правка «свип коротких не обрывается на негодном полном составе» **отменена**:
      замер показал 18 расходящихся строк на 900 задачах у целей «Вывоз» и «Снабжение»
      (`sweepsShorter` у них — константа `true`, а не «нет на пустом наборе», как я
      предполагал), а этот change добавляет цель, а не перекраивает прежние. Дефект перебора
      записан в `.scratch/feature-backlog/plan.md` под отдельный change; проверка — выдача
      прежних целей снова побайтно совпадает с той, что была до change
- [x] 5.5 Тест вкладки «нет ни одной пары в этом году → прежнее сообщение о фильтрах»;
      проверка — `cd web && npx vitest run OptimizerPage`
- [x] 5.6 Требование «Цель «Вывоз» требует заданного производства» распространено в дельте на
      три цели, дублирующий сценарий из ADDED убран; проверка —
      `openspec validate add-cheapest-sufficient-goal --strict`

- [x] 5.7 Тесты вкладки: недоступны все три цели, которым нужен выпуск (не только новая), и
      откат на ранжирование по прибыли, когда выпуск обнулили под активной целью; проверка —
      `cd web && npx vitest run OptimizerPage`
- [x] 5.8 Порядок причин под охраной типа (`Record<Insufficiency, number>`), общий хелпер
      `run()` в тестах движка вместо повторённого каста, снято утверждение, которое не могло
      упасть; проверка — `npx vitest run optimize-cheapest`, `npx tsc -b`

- [x] 5.9 Порядок причин вынесен в чистую `orderedInsufficiencies()` и покрыт прямым тестом:
      через поиск он не проверяется — на сегодняшних данных перебор отвергает варианты уже в
      каноническом порядке, и тест прошёл бы и без сортировки; заодно сняты два тавтологичных
      утверждения в тестах движка и подправлены формулировки строк пустого состояния (условие
      называется причиной отказа, а не универсальным отрицанием); проверка —
      `npx vitest run optimize-cheapest`

- [x] 5.11 `README.md` и `README.ru.md` — четвёртая цель в перечне вкладки «Лучший поезд»
      (правка в оба файла одним коммитом, как требует CLAUDE.md); проверка — в обоих файлах
      перечислены четыре цели
- [x] 5.12 Тест вкладки переведён на русский: в каталоге `features/optimizer/__tests__` оба
      соседних файла русские, поэтому правило CLAUDE.md требует следовать их языку (в
      `engine/__tests__` набор смешанный, там английский файл правомерен); проверка —
      `npx vitest run OptimizerPage`

## 4. Приёмка и завершение

- [x] 4.1 Воспроизвести приёмку из партии (соль, 20 клеток, 396 т/мес, Iron Horse + FIRS
      Steeltown, год партии) под целью «Мин. расходы»; проверка — тест-приёмка в
      `web/src/engine/__tests__/optimize-cheapest.test.ts` утверждает фактический ответ модели:
      первая строка, её годовое содержание и содержание самой прибыльной строки той же выдачи.
      Если маневровая машина не выигрывает — тест закрепляет реальный ответ, а объяснение
      дописывается в `design.md` к риску о приёмке и в `.scratch/feature-backlog/plan.md`;
      модель под ожидание не подгоняется
- [x] 4.2 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском: новая цель поиска и
      её условия достаточности; проверка — секция содержит пункт под `### Added`
- [x] 4.3 Прогон `make test` (тесты пайплайна + vitest) и `cd web && npm run lint` (oxlint,
      как в CI); проверка — обе команды завершаются с нулевым кодом
