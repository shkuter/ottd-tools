## 1. Расчёт полноты

- [x] 1.1 Вынести индекс «груз → производители» из `features/firs/dependencies.ts` в
  экспортируемую функцию; проверка — существующие тесты `dependencies.test.ts` и
  `tasks.test.ts` проходят без правок
- [x] 1.2 Создать `features/firs/completeness.ts` с расчётом работоспособности наименьшей
  неподвижной точкой (стартовое множество — всё, на чём `carriesTheChain()` даёт false);
  проверка —
  тест на прямой разрыв, на разрыв, поднявшийся на этаж выше, и на два взаимно питающих друг
  друга предприятия (все три остаются неработоспособными и расчёт завершается)
- [x] 1.3 Досчитать доступную долю финальным проходом через `conversion()` из
  `engine/supply.ts` по всем входам, снабженческие в том числе; проверка — тест «три источника
  из пяти дают 1» и «один из пяти даёт 3/8» на `appliance_factory`, плюс тест на `plate_mill`
  (STSL 6 + WELD 2): с обоими источниками доля 1, без источника WELD — 6/8
- [x] 1.4 Собрать три группы вывода — разрывы (сгруппированные по недостающему звену, с
  числом оживающих предприятий), тупики по грузам и строку про снабженческие грузы без
  источника; проверка — тест на карте, где груз производится, но не принимается (тупик, не
  разрыв), тест на предприятии с двумя выпусками, из которых принят один (тупик только по
  второму), и тест на карте без производителя снабженческого груза (отдельная строка,
  разрывов нет)
- [x] 1.5 Сгруппировать строки по типу предприятия с числом экземпляров и упорядочить по
  числу починенного, затем по названию; проверка — тест на трёх одинаковых заводах (одна
  строка, count 3) и на порядке двух недостающих звеньев
- [x] 1.6 Убедиться, что расчёт не читает построенное игроком: проверка — тест, где к снимку
  добавлены маршруты, возящие входы, и результат расчёта побайтно тот же

## 2. Блок на вкладке

- [x] 2.1 Добавить блок «Полнота цепочек» третьим на `FirsPage`, с пустым состоянием и
  ссылкой на вкладку партии, когда снимка нет; проверка — тест рендера без снимка (списков
  нет, ссылка есть)
- [x] 2.2 Вывести три секции (разрывы — группами по недостающему звену) и один раз на весь
  блок — утверждение, что предприятия FIRS не закрываются; отдельные строки пометок о
  закрытии не несут; проверка — тест рендера утверждает, что строка выведена ровно один раз
  на снимке с несколькими типами
- [x] 2.3 Добавить оговорку о предприятиях чужих наборов (`catalogueId === null`) с их числом;
  проверка — тест рендера на снимке с неизвестным предприятием (списки на месте, оговорка
  выведена)

## 3. Данные и страховка утверждений

- [x] 3.1 Тест в `pipeline/tests/`, стерегущий утверждение о незакрываемости: все пять
  шаблонов `industry_*.pynml` объявляют `monthly_prod_change`, тело каждого switch в
  `produce_*.pynml` отвечает `CB_RESULT_IND_PROD_NO_CHANGE`, и нигде не появился
  `IND_FLAG_ALLOW_CLOSING_LAST_INSTANCE` (имя флага в NML, не C++-`CanCloseLastInstance`);
  со `skipUnless` на отсутствие `vendor/` (образец — `test_chain_graph_data.py`); проверка —
  подмена ответа колбэка в одном шаблоне роняет тест

## 4. Локализация и записи

- [x] 4.1 Новые UI-строки в `i18n/en.json` и `ru.json` (заголовки секций, утверждение о
  закрытии, оговорка, пустое состояние); проверка — `i18n/__tests__/locales.test.ts` проходит
- [x] 4.2 Абзац вкладки «FIRS chains» в `README.md` и `README.ru.md` — одним коммитом, с
  совпадающей структурой; проверка — оба абзаца описывают один и тот же набор блоков
- [x] 4.3 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском

## 5. Проверка

- [x] 5.1 Прогон `make test`; проверка — оба набора (unittest пайплайна и vitest) зелёные
