## 1. Каркас страницы

- [ ] 1.1 Разделить `features/kit/KitPage.tsx` на два яруса: повторяемый `Specimen` (формы,
      текст, сообщения) и одиночную секцию тяжёлых специменов, обёрнутую в `.kit-window` с
      `data-window` из `kitWindowStore`. Специмены выпадающего списка, заведённые change
      `fit-dropdown-options` (длинный пункт, пункт с картинкой), разложить по ярусам, а не
      потерять: длинный пункт остаётся в повторяемом, список с картинкой — там же, их `data-testid`
      сохранить, иначе отвалятся проверки того change. Проверка: `/kit` открывается,
      переключатель цвета меняет цвет одиночной секции, `make check-visual` (проверки
      выпадающего списка) остаётся зелёным
- [ ] 1.2 Разбить страницу озаглавленными разделами (формы, списки, сообщения, картинки,
      график) — заголовки через `t()`. Проверка: разделы видны, `npx tsc --noEmit` чистый

## 2. Фикстуры

- [ ] 2.1 Завести `features/kit/fixtures.ts`: id машины Iron Horse (`bean_feast`) и ванильной
      (`vanilla_0`), груз, строки списка, синтетический ряд для графика, данные для
      `BuyMenuNote`, `PrefillNote` и `StrandedVehicles`. Модуль не импортирует сторы. Проверка:
      `npx tsc --noEmit` чистый, `grep -n "Store" features/kit/fixtures.ts` пуст

## 3. Общая строка сводки

- [ ] 3.1 Вынести `Row` и `NestedRow` из `features/settings/SettingsPage.tsx` в
      `components/` (сейчас они локальные и наружу не отданы), страницу настроек перевести на
      вынесенные. Проверка: `npx vitest run features/settings` зелёный, вид вкладки настроек не
      изменился (`make check-visual`)
- [ ] 3.2 Специмен строки сводки: `SummaryRow` в таблице сводки и вынесенная вложенная строка
      рядом с обычной. Проверка: на `/kit` вложенная строка отличается от обычной отступом, как
      на вкладке настроек

## 4. Специмены списка

- [ ] 4.1 Специмен списка на настоящих `TableFrame` + `SortableTh`: колонка «картинка +
      название», денежная колонка через `Money`, прибыль и убыток, отсортированная колонка,
      `pinEdges` и ширина больше своей колонки, чтобы была боковая прокрутка. Проверка: список
      прокручивается вбок, крайние колонки стоят на месте
- [ ] 4.2 Специмен пустого списка (`rowCount = 0`) с сообщением. Проверка: вместо таблицы видно
      сообщение
- [ ] 4.3 Специмен `Pagination`. Проверка: элемент виден, страницы переключаются

## 5. Специмены форм, сообщений и картинок

- [ ] 5.1 Добавить в повторяемый специмен то, чего в нём нет: `Paper`, `Field`, `List`,
      `ActionIcon`, недоступное поле и недоступную кнопку рядом с доступными. Проверка: элементы
      видны во всех цветовых группах
- [ ] 5.2 Добавить `Warning`, `BuyMenuNote`, `PrefillNote`, `IconSwitch`, `GuiIcon`, `CargoIcon`
      и `TrainImage` — на фикстурах из 2.1. Проверка: картинки отрисованы (не спрятаны
      обработчиком ошибки), подписи читаются
- [ ] 5.3 Проверить по списку, что каждый **вид** элемента с вкладок представлен: пройти
      `web/src/components/` и импорты `@mantine/core` в `features/`, и для каждого решить —
      показан он или отличается от показанного только источником данных (`TrackTypeField`,
      `YearField`, `StrandedVehicles` — поле со списком, числовое поле и предупреждение).
      Расхождения закрыть специменом. Проверка: перечень решений записан комментарием в
      `KitPage.tsx`

## 6. График

- [ ] 6.1 Специмен `LineChart` из `@mantine/charts` на синтетическом ряде, в одиночной секции.
      Проверка: график виден, цвет плиты и подписей следует выбранному окну

## 7. Строки интерфейса

- [ ] 7.1 Подписи новых специменов и разделов в `web/src/i18n/en.json` и `ru.json`. Проверка:
      `npx vitest run src/i18n/__tests__/locales.test.ts` зелёный

## 8. Проверки вида

- [ ] 8.1 Дополнить `__tests__/visual/kit.ts` помощниками для новых специменов (открыть страницу
      в нужном цвете, найти список, пустой список, график) — по `data-testid`, не по подписям.
      Проверка: помощники используются в 8.2–8.5
- [ ] 8.2 Утверждения о списке страницы в `list.visual.test.ts` и `table.visual.test.ts`: имена
      начинаются с одной вертикали, деньги выровнены по разряду, крайние колонки держатся при
      боковой прокрутке, метрики строки — из скина. Проверка: `make check-visual` зелёный
- [ ] 8.3 Утверждение о пустом списке: на месте таблицы сообщение, окрашенное скином. Проверка:
      `make check-visual` зелёный
- [ ] 8.4 Утверждение о графике в `chart.visual.test.ts`: плита и подписи следуют цвету
      выбранного окна. Проверка: `make check-visual` зелёный
- [ ] 8.5 Утверждение о независимости от состояния: открыть `/kit` с иным набором машин в сторе
      и убедиться, что состав специменов тот же. Проверка: `make check-visual` зелёный

## 9. Завершение

- [ ] 9.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском: страница элементов
      интерфейса показывает весь набор оболочки вместе с его состояниями. Проверка: секция
      `[Unreleased]` содержит запись, текст английский
- [ ] 9.2 Прогнать `make test` и `make verify`. Проверка: оба зелёные
