## 1. Каркас страницы

- [x] 1.1 Разделить `features/kit/KitPage.tsx` на два яруса: повторяемый `Specimen` (формы,
      текст, сообщения) и одиночную секцию тяжёлых специменов, обёрнутую в `.kit-window` с
      `data-window` из `kitWindowStore`. Три специмена выпадающего списка, заведённые change
      `fit-dropdown-options` (`kit-dropdown-long`, `kit-dropdown-short`, `kit-dropdown-picture`),
      остаются в повторяемом ярусе вместе со своими `data-testid`, и комментарий о том, что они
      временные, снимается. Одиночная секция не должна содержать `Select` раньше повторяемого
      яруса: `openDropdown` в `visual/kit.ts` берёт первый `Select` страницы как «серое окно».
      Проверка: `/kit` открывается, переключатель цвета меняет цвет одиночной секции,
      `make check-visual` (в нём — пять проверок выпадающего списка) остаётся зелёным
- [x] 1.2 Разбить страницу озаглавленными разделами (формы, списки, сообщения, картинки,
      график) — заголовки через `t()`. Проверка: разделы видны, `npx tsc -b` в `web/` чист

## 2. Фикстуры

- [x] 2.1 Завести `web/src/features/kit/fixtures.ts`: id машины Iron Horse (`bean_feast`) и
      ванильной (`vanilla_0`), груз, строки списка, синтетический ряд для графика, данные для
      `BuyMenuNote` и `PrefillNote`. Модуль не импортирует сторы. Проверка: `npx tsc -b` чист,
      `grep -n "Store" web/src/features/kit/fixtures.ts` пуст

## 3. Вложенная строка настройки

- [x] 3.1 Вынести `Row` и `NestedRow` из `features/settings/SettingsPage.tsx` в `components/`
      (сейчас они локальные и наружу не отданы; `components/SummaryRow.tsx` — другой компонент,
      строка сводки, и он тут ни при чём), страницу настроек перевести на вынесенные. Проверка:
      `npx vitest run features/settings` зелёный, `make check-visual` (в нём `nested`) зелёный
- [x] 3.2 Специмен строки настройки: обычная и вложенная рядом, обе с `data-testid`. Проверка:
      на `/kit` вложенная отличается от обычной отступом, как на вкладке настроек

## 4. Специмены списка

- [x] 4.1 Специмен списка на настоящих `TableFrame` + `SortableTh`: колонка «картинка +
      название», денежная колонка через `Money`, прибыль и убыток, отсортированная колонка,
      `pinEdges` и ширина больше своей колонки, чтобы была боковая прокрутка; у специмена
      устойчивый `data-testid`. Проверка: список прокручивается вбок, крайние колонки стоят на
      месте
- [x] 4.2 Специмен пустого списка (`rowCount = 0`) с сообщением, со своим `data-testid`.
      Проверка: вместо таблицы видно сообщение
- [x] 4.3 Специмен `Pagination` с `data-testid`. Проверка: элемент виден, страницы переключаются
- [x] 4.4 Сохранить вертикальную полосу прокрутки, которую сейчас даёт таблица в
      `.table-wrap` с `maxHeight` (`routes.ts` держит `/kit` вне проверок боковой прокрутки
      именно из-за неё, а `TableFrame` своей вертикальной прокрутки не имеет). Проверка:
      `make check-visual` (в нём проверка полос прокрутки) зелёный

## 5. Специмены форм, сообщений и картинок

- [x] 5.1 Добавить в повторяемый специмен то, чего в нём нет: `Paper`, `Field`, `List`,
      `ActionIcon` (недоступные поле и кнопка там уже есть). Проверка: элементы видны во всех
      цветовых группах
- [x] 5.2 Добавить `Warning`, `BuyMenuNote`, `PrefillNote`, `IconSwitch`, `GuiIcon`, `CargoIcon`
      и `TrainImage` — на фикстурах из 2.1, каждый с `data-testid`. Проверка: картинки
      отрисованы (не спрятаны обработчиком ошибки), подписи читаются
- [x] 5.3 Свести набор витрины и набор приложения тестом, а не глазами: список показанных видов
      элементов объявляется рядом со страницей, а тест сверяет его с экспортами
      `web/src/components/` и импортами `@mantine/core` в `features/`, отмечая исключения
      («отличается от показанного только источником данных» — `TrackTypeField`, `YearField`,
      `StrandedVehicles`). Проверка: `npx vitest run features/kit` зелёный, а добавление
      компонента без специмена его роняет

## 6. График

- [x] 6.1 Специмен `LineChart` из `@mantine/charts` на синтетическом ряде, в одиночной секции,
      со своим `data-testid`; цвет ряда брать из темы, как вкладка дохода. Проверка: график
      виден, цвет плиты и подписей следует выбранному окну
- [x] 6.2 Довести цвета графика до палитры: подписи и линии рисует recharts, а проверка палитры
      метёт всю страницу во всех цветах окна, и список исключений (`visual/exemptions.ts`) обязан
      остаться пустым. Проверка: `make check-visual` (в нём `palette` и `shadow`) зелёный
- [x] 6.3 Убедиться, что recharts не переехал в основной чанк: вкладка дохода грузится лениво, и
      витрина должна грузиться так же. Проверка: `npm run build` — размер основного чанка не
      вырос заметно, recharts остался в отдельном

## 7. Строки интерфейса

- [x] 7.1 Подписи новых специменов и разделов в `web/src/i18n/en.json` и `ru.json`. Проверка:
      `npx vitest run src/i18n/__tests__/locales.test.ts` зелёный

## 8. Проверки вида

- [x] 8.1 Дополнить `__tests__/visual/kit.ts` тем, чего в нём ещё нет: найти список, пустой
      список, график (открывать поля и мерить списки уже умеют `openField`, `openList`,
      `dropdownMetrics`, `specimenInput`). Проверка: помощники используются в 8.2–8.5
- [x] 8.2 Утверждения о списке витрины в `list.visual.test.ts` и `table.visual.test.ts`: имена
      начинаются с одной вертикали, деньги выровнены по разряду, крайние колонки держатся при
      боковой прокрутке, метрики строки — из скина. Проверка: `make check-visual` зелёный
- [x] 8.3 Утверждение о пустом списке: на месте таблицы сообщение, окрашенное скином. Проверка:
      `make check-visual` зелёный
- [x] 8.4 Утверждение о графике в `chart.visual.test.ts`: плита и подписи следуют цвету
      выбранного окна. Проверка: `make check-visual` зелёный
- [x] 8.5 Завести `visual/kit.visual.test.ts` о самой странице (перекличка специменов,
      отрисованные картинки) и научить стенд открывать витрину с подменённым состоянием (`addInitScript` до перехода:
      сегодня `harness.ts` переписывает `settingsStore` на каждой навигации, а сменить набор
      кликом на `/kit` нельзя — её нет в ряду вкладок), и утверждать, что состав специменов при
      другом наборе машин тот же. Проверка: `make check-visual` зелёный

## 9. Завершение

- [x] 9.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском: страница элементов
      интерфейса показывает весь набор оболочки вместе с его состояниями. Проверка: секция
      `[Unreleased]` содержит запись, текст английский
- [x] 9.2 Прогнать `make test` и `make verify`. Проверка: оба зелёные
