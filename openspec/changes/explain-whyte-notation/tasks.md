## 1. Разбор формулы

- [x] 1.1 `web/src/components/whyteNotation.ts`: чистая функция имя → разобранная формула
      (группы осей, признак сочленённости) или `null`. Формула ищется в имени целиком, не
      только с начала. Проверка: юнит-тест на формы из данных — простая `0-8-4`, сочленённая
      `2-6-0+0-6-2`, четырёхчастная `0-4-4-0`, формула не первым словом (`Wills 2-8-0`), имя
      без формулы.
- [x] 1.2 Тест-сторож по данным обоих наборов: каждое имя машины, в котором встречается
      группа чисел через дефис, разбирается. Проверка: тест видит `Wills 2-8-0` в
      `vanilla_trains.json` и падает, если в любом из наборов появится незнакомая форма.

## 2. Строки подсказки

- [x] 2.1 Ключи подсказки в `web/src/i18n/en.json` и `ru.json`: название каждой группы осей и
      что она даёт, отдельная строка про сочленённую машину. Проверка:
      `i18n/__tests__/locales.test.ts` зелёный (он падает на ключе без перевода).
- [x] 2.2 Сборка текста подсказки из разобранной формулы — с локалью аргументом, как
      `cargoName()`. Проверка: тест на текст при `en` и при `ru`.

## 3. Компонент имени

- [x] 3.1 `web/src/components/VehicleName.tsx`: принимает машину и текст, которым её зовут в
      этом месте, оборачивает текст в `Tooltip`, когда формула разобралась, и отдаёт как есть,
      когда нет. Проверка: компонентный тест — у паровоза подсказка есть, у дизеля её нет.
- [x] 3.2 Ячейки, где имя приходит из `engineLabel()` / `wagonLabel()`: выдача
      `features/optimizer/OptimizerPage.tsx:674,685`, колонки `ComparisonPanel.tsx:119`,
      вкладка снабжения `features/industry-supply/IndustrySupplyPage.tsx:253,266`. Проверка:
      тесты этих вкладок зелёные, метка с числом секций (`2× Haar`) не изменилась.
- [x] 3.3 Ячейки, где имя берётся у машины напрямую: `features/consist/ConsistPage.tsx:273,345`,
      `features/kit/KitPage.tsx:357`. Проверка: тесты конструктора зелёные, вид ячейки прежний.
- [x] 3.4 Перечисление состава партии: рядом с `consistText()` в
      `features/savegame/labels.ts` появляется сборка тех же частей в разметку, её рисуют
      `TrainsTab.tsx:124` и список флота `RoutesTab.tsx:256` через `VehicleName`; сортировка
      (`TrainsTab.tsx:67`) и атрибут `title` (`RoutesTab.tsx:176`) продолжают звать строковую
      функцию. Проверка: `GamePage.test.tsx` (он сверяет строку состава посимвольно, включая
      пробел перед `×N`) и сортировка по колонке состава — зелёные; добавить кейс на то, что
      строковая и разметочная сборки дают один и тот же текст.

## 4. Проверка вида

- [x] 4.1 Посмотреть подсказку в браузере на вкладках выдачи, конструктора, снабжения и
      «Партии»: она не перекрывает строку и не мешает клику по ней. Проверка:
      `make check-visual` зелёный (тултип уже покрыт `__tests__/visual/theme.visual.test.ts`).

## 5. Документация и гейты

- [x] 5.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском. Проверка: запись на
      месте, разряд patch (числа не меняются).
- [x] 5.2 Отметить FR-06 в `.scratch/feature-backlog/plan.md`. Проверка: пункт «Мелочи по
      случаю» больше не числит FR-06 несделанным.
- [x] 5.3 Прогнать `make test`, `make verify` и `cd web && npm run lint` (oxlint — гейт CI в
      `.github/workflows/pages.yml`, в `make verify` он не входит). Проверка: все три зелёные.
