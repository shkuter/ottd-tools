## 1. Показатель цели и равноценность

- [ ] 1.1 В `web/src/engine/optimize.ts` добавить в `GoalStrategy` показатель цели: прибыль,
      вывоз, годовое содержание, пара «конверсия + удержание». Проверка: юнит-тест на каждую
      из четырёх целей — показатель берётся тот, которым цель ранжирует.
- [ ] 1.2 Строка выдачи получает признак равноценности лучшей по этому показателю; считается
      один раз по выдаче в порядке цели. Проверка: тест — у «Снабжения» на задаче из замера
      равноценны все строки, у «Прибыли» с убывающей прибылью только первая.
- [ ] 1.3 Тест на то, что признак ничего не изменил: строки, их порядок и числа те же.
      Проверка: прогон `.scratch/group-equivalent-rows/equivalence-bench.test.ts` до и после
      даёт одинаковые ответы (поле `rows` в снимке).

## 2. Подача

- [ ] 2.1 Строки в `web/src/i18n/en.json` и `ru.json`: сообщение над таблицей (сколько строк
      равны лучшей), пометка строки и общее пояснение — порядок задан прибылью, ценой покупки,
      предпочтением вагона, размером флота. Проверка: `i18n/__tests__/locales.test.ts` зелёный.
- [ ] 2.2 Сообщение над выдачей, когда равноценных больше половины показанных строк, с числом
      равноценных и названием действующей цели. Проверка: компонентный тест — при цели
      «Снабжение» сообщение есть, при «Прибыли» с убывающей прибылью его нет.
- [ ] 2.3 Пометка в строке, когда равноценных меньшинство, идиомой `components/BuyMenuNote.tsx`.
      Проверка: тест — пометка стоит у равноценной строки и отсутствует у отстающей.
- [ ] 2.4 Сообщение называет **действующую** цель: при недоступной выбранной калькулятор
      считает по прибыли (`OptimizerPage.tsx`, `activeGoal`). Проверка: тест на задачу без
      заданного выпуска с выбранной целью «Вывоз».
- [ ] 2.5 Пересортировка выдачи по любой колонке не меняет ни набор равноценных строк, ни
      сообщение. Проверка: тест сортирует выдачу и сверяет оба.

## 3. Документация и гейты

- [ ] 3.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, заголовок `### Changed` (под
      `### Added` скрипт версии посчитал бы minor), на английском. Проверка: запись на месте,
      `bash scripts/next-version.sh` даёт patch-разряд относительно текущей версии.
- [ ] 3.2 Отметить FR-27 в `.scratch/feature-backlog/plan.md` — с ним закрывается весь пункт
      «Мелочи по случаю». Проверка: незакрытых пунктов в очереди не осталось.
- [ ] 3.3 Прогнать `make verify` (включает тесты пайплайна, vitest и проверку вида) и
      `cd web && npm run lint`. Проверка: обе команды зелёные.
