## 1. Настройка в движке

- [x] 1.1 Добавить `wagonSpeedLimits: boolean` в `GameSettings` (`web/src/engine/settings.ts`)
  со значением `true` в `DEFAULT_GAME_SETTINGS`; проверка — `npx tsc --noEmit` проходит, а
  существующие тесты движка не меняют ни одного числа
- [x] 1.2 Убедиться, что старому сохранению настройка достаётся включённой через `merge`
  стора, и версию persist НЕ поднимать; проверка — свой кейс в
  `state/__tests__/settingsStore.test.ts`, утверждающий **значение** (`true`), а не наличие
  ключа: соседний кейс перебирает ключи через `toHaveProperty` и сломанный `merge` пропустил бы

## 2. Правило минимума

- [x] 2.1 В `consistPhysics` (`web/src/engine/consist.ts`) гейтить участие предела машины в
  минимуме условием `train.kind !== 'wagon' || game.wagonSpeedLimits`; проверка — тест
  движка: локомотив 120 и вагоны 96 дают предел 96 при включённой настройке и 120 при
  выключенной
- [x] 2.2 Убедиться, что лимит типа путей и машина без собственного предела ведут себя
  по-прежнему при обоих значениях настройки; проверка — сценарии «Лимит пути при выключенной
  настройке» и «Вагон без собственного предела» из дельты `track-types` как тесты
- [x] 2.3 Покрыть тестом не-вагон без питания на выбранном пути: предел он задаёт при любой
  настройке, хотя мощности не даёт; проверка — сценарий «Локомотив без питания на этом пути»
  (гейт по роду машины, а не по мощности)
- [x] 2.4 Регрессия уровня рейса на паре из партии (Iron Horse, локомотив 120 + вагоны 96,
  150 клеток): при выключенной настройке круг рейса короче и рейсов за год больше; проверка —
  тест `engine/__tests__` сравнивает круг при двух значениях настройки
- [x] 2.5 Добавить кейс в `web/src/engine/__tests__/settings-effect.test.ts` (своя база не
  нужна: эталон отличается от кейса ровно этой настройкой); проверка — кейс падает, если
  условие 2.1 убрать

## 3. Источник предела

- [x] 3.1 Вернуть из `consistPhysics` поле `speedLimitSource` (`'engine' | 'wagon' | 'track'
  | null`: категория со строго наименьшим кандидатом, при ничьей `null`); отдельного флага для
  пометки не заводить — пометка это `speedLimitSource === 'wagon'`; проверка — тесты на все
  четыре исхода, включая ничью
- [x] 3.2 Показать источник рядом с пределом скорости в сводке конструктора
  (`features/consist`, `consist.stats.speedLimit`) — единственном месте, где показан сам
  предел; проверка — render-тест страницы (`features/__tests__/speedLimitSource.test.tsx`)
  читает строку сводки, как её читает игрок
- [x] 3.3 Пометить строку подбора, где предел составу задают вагоны, и снимать пометку при
  выключенной настройке и когда связывает путь; к колонке установившейся скорости источник не
  приписывать; проверка — тест цепочки: `optimizeConsists` отдаёт строки с
  `speedLimitSource === 'wagon'`, и ни одной такой при выключенной настройке

## 4. Настройка в интерфейсе

- [x] 4.1 Добавить контрол настройки на вкладку Settings рядом с остальными параметрами
  игры; проверка — переключение меняет числа на вкладке подбора без перезагрузки
- [x] 4.2 Добавить строки `en.json` / `ru.json`: подпись настройки («Учитывать ограничение
  скорости у вагонов» — формулировка из `vendor/openttd/src/lang/russian.txt:2047`), подписи
  источников предела и пометки; проверка — `i18n/__tests__/locales.test.ts` проходит

## 5. Импорт сохранения

- [x] 5.1 Перенести `vehicle.wagon_speed_limits` из `INFO_SETTINGS` в `GAME_SETTING_SOURCES`
  (`web/src/savegame/mapping.ts`) как `read: (v) => ({ wagonSpeedLimits: v !== 0 })`, а также
  завести запись в `GAME_LABELS` (`savegame/diff.ts`) и `SETTING_LABEL_KEYS`
  (`features/savegame/settingNames.ts`) — без первой расхождение не показывается игроку;
  проверка — тест `savegame`: сейв с выключенной настройкой даёт строку в списке различий с
  подписью настройки, и справочный список пуст
- [x] 5.2 Привести подпись настройки в списке различий к той же формулировке, что на вкладке
  Settings (ключ `savegame.info.wagonSpeedLimits` заменяется подписью настройки); проверка —
  в обоих местах одна строка, `locales.test.ts` проходит

## 6. Завершение

- [x] 6.1 Дописать запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском: новая
  настройка (minor), источник предела скорости в интерфейсе, перенос настройки из сейва
- [x] 6.2 Прогнать `make test`; проверка — тесты пайплайна и `vitest` зелёные (данные не
  трогаются, `make data` не нужен)
