## 1. Экономика круга (engine/trip.ts)

- [ ] 1.1 Добавить в `TripParams` необязательные `deliveredShare` (по умолчанию 1) и
  `fleetSize` (по умолчанию 1); умножить `incomePerTrip` на долю, а `buyCostTotal`,
  `runningCostPerYear` и годовой доход — на размер парка. Проверяемо: при умолчаниях все числа
  прежние, `npx vitest run trip` зелёный.
- [ ] 1.2 Тест: доля 0.5 вдвое уменьшает `incomePerTrip`, но не трогает `capacity`,
  `roundTripDays` и `tripsPerYear`.
- [ ] 1.3 Тест: `fleetSize = 2` удваивает цену покупки, содержание и прибыль, а `paybackYears`
  не меняется.

## 2. Перебор и ранжирование (engine/optimize.ts)

- [ ] 2.1 Добавить в `OptimizeParams` цель (`'profit' | 'transported'`) и `maxTrains`; при
  `productionPerMonth = 0` цель принудительно считать `'profit'`.
- [ ] 2.2 Переписать `evaluate` по порядку из design.md п. 4: `cargoPerTrip` делится на число
  поездов парка, доля вывоза считается от интервала `roundTripDays / k` и передаётся в
  `tripEconomics` вместе с `fleetSize = k`.
- [ ] 2.3 Добавить в `OptimizeResult` вывоз за год (`hauledPerYear`) и признак «парк ограничен
  пределом» (`fleetLimited`).
- [ ] 2.4 При цели «Вывоз» перебирать `k` от минимума до `maxTrains` (при `trainsNeeded >
  maxTrains` — единственный кандидат `k = maxTrains` с `fleetLimited`), при цели «Прибыль»
  оставить `k = trainsNeeded`.
- [ ] 2.5 При цели «Вывоз» перебирать все допустимые длины состава; правило текущего перебора
  (короче полной станции только при переполнении потока) сохранить для цели «Прибыль».
- [ ] 2.6 Параметризовать `better()` целью: вывоз (с допуском) → прибыль → цена покупки →
  нерандомизированный вагон; тем же компаратором пользуются группировка и финальная сортировка.

## 3. Тесты движка

- [ ] 3.1 Тест: при заданном производстве прибыль строки меньше, чем при том же входе без
  учёта доли вывоза, ровно во столько раз, во сколько доля меньше единицы.
- [ ] 3.2 Тест: смена цели меняет порядок выдачи — первая строка при цели «Вывоз» имеет
  наибольший `hauledPerYear` и не обязана быть самой прибыльной.
- [ ] 3.3 Тест: при `maxTrains`, которого не хватает на поток, `hauledPerYear` равен провозной
  способности разрешённого парка и выставлен `fleetLimited`.
- [ ] 3.4 Тест: при равном вывозе выигрывает вариант с меньшим числом поездов (лишний поезд не
  добавляет вывоза, но режет прибыль).
- [ ] 3.5 Тест: при `productionPerMonth = 0` цель «Вывоз» даёт ту же выдачу, что и «Прибыль».
- [ ] 3.6 Тест: поток делится между поездами парка — при удвоении числа поездов груз за рейс
  вдвое меньше (пока вместимость не ограничивает), вывоз за год тот же, а окупаемость хуже.

## 4. Состояние и интерфейс

- [ ] 4.1 Добавить в `state/optimizerStore.ts` поля `goal` (по умолчанию `'profit'`) и
  `maxTrains` (по умолчанию 4) с сеттерами; проверить, что старый localStorage подхватывается
  и значения не сбрасываются.
- [ ] 4.2 Добавить в `OptimizerPage` `SegmentedControl` цели и поле «Поездов не больше»
  (видно только при цели «Вывоз»); цель «Вывоз» недоступна при нулевом производстве, рядом —
  объяснение почему.
- [ ] 4.3 Показывать деньги строки как деньги парка и колонку абсолютного вывоза при цели
  «Вывоз»; помечать строки с `fleetLimited`.
- [ ] 4.4 Обновить подсказки: деньги урезаны долей вывоза и относятся к парку, вкладка «Доход
  рейса» считает один состав по полному потоку и с оптимизатором не сойдётся.

## 5. Локализация

- [ ] 5.1 Добавить новые строки в `web/src/i18n/en.json` и `ru.json` (цель и её варианты, поле
  предела парка, колонка вывоза за год, пометка ограниченного парка, объяснение блокировки
  цели, обновлённые подсказки); прогнать `npx vitest run locales`.

## 6. Производительность

- [ ] 6.1 Замерить время `optimizeConsists` в браузере при цели «Вывоз» на худшем входе
  (поздний год, станция 7 тайлов, распространённый груз) с CPU-троттлингом 4x и 6x; записать
  числа в change.
- [ ] 6.2 Если замер показывает заметную задержку ввода — снизить стоимость перебора (шаг по
  длине состава с добором вокруг лучшей либо дебаунс полей); иначе задачу закрыть с пометкой
  «не требуется».

## 7. Документация

- [ ] 7.1 Создать `CONTEXT.md` с глоссарием: поток, доля вывоза, вывоз, парк.
- [ ] 7.2 Создать ADR в `docs/adr/` на решение «доход умножается на долю доставки, а поток,
  физика и длина состава остаются полными».
- [ ] 7.3 Дописать запись в `CHANGELOG.md` в секцию `[Unreleased]` (на английском), пометив
  `**BREAKING**`.

## 8. Проверка

- [ ] 8.1 Прогнать `make test` целиком; убедиться, что `trip.test.ts`, `settings-effect.test.ts`
  и `rating.test.ts` зелёные.
- [ ] 8.2 Проверить вкладку в браузере: обе цели, блокировка при нулевом производстве,
  сохранение выбора после перезагрузки страницы.
