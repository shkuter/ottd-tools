## 1. Оценка рейтинга

- [ ] 1.1 Добавить в `StationRatingParams` вместимость одного визита (сколько груза забирает
      приезд состава) и провести её через `estimateStationRating` без изменения результата,
      пока парк вывозит весь поток
- [ ] 1.2 Считать остаток ждущего груза внутри существующего цикла проходов: приход за
      интервал минус вывезенное, с переносом между интервалами и обратной связью через долю
- [ ] 1.3 Ограничить рост остатка минимальным штрафом за ждущий груз: дальше него рейтинг не
      падает
- [ ] 1.4 Тесты: парк вывозит весь поток (числа не изменились), парк отстаёт (рейтинг ниже),
      парк отстаёт вдвое (не ниже минимального штрафа), устойчивость к числу итераций

## 2. Вызывающие

- [ ] 2.1 Провести вместимость визита через `engine/waiting.ts`: параметр у
      `routeStationRating` и `ratingAt`, значение — из `RouteLoad` (`capacity`, `fleetSize`),
      которые слой уже получает от обеих вкладок
- [ ] 2.2 Развести ветки: «уезжает с накопленным» забирает `min(накопленное, вместимость)`,
      полная загрузка — всегда вместимость; `settleBranchFlows` и `settleWaitingBranch`
      сходятся на обеих
- [ ] 2.3 Расширить ключ кеша рейтинга в `engine/optimize.ts` (сейчас
      `ratingPeriods|speedRating`) вместимостью визита, иначе строки с разным парком получат
      чужой рейтинг; тест — две строки с одним интервалом и разным парком
- [ ] 2.4 Тест: обе вкладки дают один рейтинг для одного маршрута — требование `route-economics`
      «Единая модель во всех вкладках» (`engine/__tests__/tabs-agree.test.ts`)

## 3. Сверка с игрой

- [ ] 3.1 Записать эталон стенда как известное значение: шахта 405 т/мес, три поезда
      Athena + 6 Coal Wagon, 300 клеток, JGRPP с множителем длины дня 5 — рейтинг ≈ 69 % и
      доля вывоза ≈ 69 % (с допуском, обоснованным в тесте)
- [ ] 3.2 Проверить, что прежний эталон дня длины (`2026-08-20-fix-station-rating-day-length`)
      не сломался: те же кейсы в `engine/__tests__/rating.test.ts` и `waiting.test.ts`
      проходят или обновлены осознанно

## 4. Завершение

- [ ] 4.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском, с пометкой
      `**BREAKING**` (числа меняются при том же вводе)
- [ ] 4.2 Прогнать `make test` и `make verify`
