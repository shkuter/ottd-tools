## 1. Оценка рейтинга

- [x] 1.1 Добавить в `StationRatingParams` вместимость одного визита (сколько груза забирает
      приезд состава) и провести её через `estimateStationRating` без изменения результата,
      пока парк вывозит весь поток
- [x] 1.2 Считать остаток ждущего груза внутри существующего цикла проходов: приход за
      интервал минус вывезенное, с переносом между интервалами и обратной связью через долю
- [x] 1.3 Ограничить рост остатка минимальным штрафом за ждущий груз: дальше него рейтинг не
      падает
- [x] 1.4 Тесты: парк вывозит весь поток (числа не изменились), парк отстаёт (рейтинг ниже),
      парк отстаёт вдвое (не ниже минимального штрафа), равновесие не зависит от числа проходов
      поиска

## 2. Вызывающие

- [x] 2.1 Провести вместимость визита через `engine/waiting.ts`: параметр у
      `routeStationRating` и `ratingAt`, значение — из `RouteLoad` (`capacity`, `fleetSize`),
      которые слой уже получает от обеих вкладок
- [x] 2.2 Развести ветки: «уезжает с накопленным» забирает `min(накопленное, вместимость)`,
      полная загрузка — всегда вместимость; `settleBranchFlows` и `settleWaitingBranch`
      сходятся на обеих, включая маршрут с остатком
- [x] 2.3 Расширить ключ кеша рейтинга в `engine/optimize.ts` (сейчас
      `ratingPeriods|speedRating`) вместимостью визита, иначе строки с разным парком получат
      чужой рейтинг; тест — две строки с одним интервалом и разным парком
- [x] 2.4 Тест: обе вкладки дают один рейтинг для одного маршрута — требование `route-economics`
      «Единая модель во всех вкладках» (`engine/__tests__/tabs-agree.test.ts`)

## 3. Сверка с игрой

- [x] 3.1 Записать эталон стенда как известное значение: шахта 405 т/мес, три поезда
      Athena + 6 Coal Wagon, 300 клеток, JGRPP с множителем длины дня 5 — рейтинг ≈ 69 % и
      доля вывоза ≈ 69 % (с допуском, обоснованным в тесте)
- [x] 3.2 Проверить, что прежний эталон дня длины (`2026-08-20-fix-station-rating-day-length`)
      не сломался: те же кейсы в `engine/__tests__/rating.test.ts` и `waiting.test.ts`
      проходят или обновлены осознанно

## 4. Находки ревью

- [x] 4.1 Груз за рейс остаётся средним за визит (`engine/waiting.ts`): «визит уходит полным»
      в буквальном виде столкнулось с инвариантом ADR-0001 «вывоз ≤ доля × выпуск» —
      равновесие встаёт на ступени рейтинга, которая всегда чуть ниже точного равенства
- [x] 4.2 Признак «есть чего ждать» читать по остатку, а не по недобору до вместимости: на
      станции с постоянным остатком ждать нечего. Тест — через `settleBranchFlows`, а не мимо
      него (`trip-branches.test.ts` зовёт `tripBranches` напрямую и это не ловит)
- [x] 4.3 Закрепить в `engine/__tests__/engine.test.ts` строку с остатком: недобор груза до
      вместимости берётся из ступени рейтинга, ветка ожидания на такой строке не предлагается
- [x] 4.4 Пометка «не хватает поездов»: подсказка в `i18n/en.json` и `ru.json` обещает сравнение
      с отдаваемым потоком — переписать под остаток
- [x] 4.5 `CONTEXT.md`: завести термин остатка и обновить определение пометки «не хватает
      поездов» (в глоссарии до сих пор `fleet × tripsPerYear × capacity < offered`)
- [x] 4.6 `docs/adr/0001-*`: раздел Revision — снимается допущение «состав увозит всё
      накопленное», у доли вывоза появился новый вход (вместимость визита)
- [x] 4.7 Убрать дубль правила равновесия: приход за интервал и `flowPerYear /
      engineDaysPerYear` считаются и в `rating.ts`, и в кеше `optimize.ts` — разъедутся молча
- [x] 4.8 Мелочи чтения: порог 1500 задан и константой, и литералом внутри
      `waitingCargoRating` (вынести шагами, как `WAIT_TIME_STEPS`); комментарий к `MAX_BACKLOG`
      говорит 3001, код даёт 3002; проверка выхода стоит в теле поиска, хотя срабатывает только
      на первом проходе

## 5. Находки второго ревью

- [x] 5.1 Равновесная доля при непустом остатке (`engine/rating.ts`): доля — вместимость визита,
      делённая на то, что предприятие отдало бы станции за интервал; рейтинг выводится из доли
      обратным пересчётом. Сейчас берётся первая ступень ниже границы, и вывоз падает с ростом
      выпуска: тот же парк при потоке 800 вывозит 181,6 т/год, при 30000 — 117,2 при провозной
      способности 182,5
- [x] 5.2 Тест монотонности: при том же парке и переполненной станции больший выпуск не даёт
      меньшего вывоза за год
- [x] 5.3 Вернуть в `engine/__tests__/engine.test.ts` проверку сценария «Предела не хватает на
      поток»: вывоз равен провозной способности парка (сейчас тест ждёт `доля × выпуск`)
- [x] 5.4 Пересчитать эталон партии: с равновесной долей оценка даёт ≈ 67,8 % против игровых
      68,75 % — сузить допуск теста до фактического промаха
- [x] 5.5 Убрать из `fleetLimited` сравнение потоков (`|| fleetCapacityPerYear < offered`):
      с равновесной долей эти величины равны, а дельта `train-optimizer` требует пометку
      по остатку
- [x] 5.6 Закрепить сценарий «Вывоз режет доля, а не парк»: у строки без остатка
      `backlog === 0`, а не только `fleetLimited === false`
- [x] 5.7 `engine/waiting.ts`: `flowPerEngineDay` вставлен между JSDoc и `routeStationRating` —
      документация осиротела, функция осталась без комментария
- [x] 5.8 Чтение и мелочи: `BACKLOG_PASSES` недостижим (epsilon срабатывает раньше);
      `outrunsAtCeiling` стоит в условии цикла, хотя внутри не меняется; проза комментария
      `MAX_BACKLOG` читается как 3001 при коде 3002; `WAITING_CARGO_STEPS` и
      `MAX_PENALISED_WAITING_PER_HOP` экспортированы без внешних потребителей; русские
      комментарии в англоязычных тестовых файлах

## 6. Находки третьего ревью

- [x] 6.1 Заменить пятипроходную итерацию доли сходящимся поиском неподвижной точки
      (`engine/rating.ts`, `settleShare`): контур раскачивается на большом выпуске и садится
      на 1/256 — при вместимости 200 и интервале 100 дней поток 80 000 т/год даёт 200 т за
      визит, а 90 000 — 96 т. Дефект есть и до этой правки (проверено на `c9b9f22`)
- [x] 6.2 Тест на этот край: сетка потоков до экстремальных, вывоз не падает с ростом выпуска
      ни на одном; существующий тест монотонности покрывает только режим с остатком
- [x] 6.3 Пересверить эталоны после смены метода: эталон партии, эталон дня длины, кейсы
      `waiting.test.ts` и `optimize-branches.test.ts` — числа обновить осознанно
- [x] 6.4 Производительность: ключ второго яруса кеша в `engine/optimize.ts` содержит точный
      интервал, поэтому строки с остатком считаются заново — тот же тестовый файл идёт 860 мс
      против 345 мс на базе. Огрубить ключ до того, от чего ответ реально зависит; в задаче
      привести замер до и после. Замер: 860 мс до правки, 355–371 мс после (база 328–352 мс);
      снято вложенным поиском — доля при остатке считается формулой, а не вторым делением
      отрезка, а средний штраф — по ступеням вместо перебора периодов
- [x] 6.5 `docs/adr/0001-*`, раздел Revision: «the load per trip is a fraction below the
      capacity» устарело — с равновесной долей груз равен вместимости точно. Тем же
      объяснением живут комментарий `canWait` в `engine/waiting.ts`, комментарий в
      `engine/__tests__/engine.test.ts` и допуск `> capacity * 0.99` в `waiting.test.ts`
- [x] 6.6 Комментарий к `parts.equilibrium` в `OptimizerPage.tsx` обещает, что слагаемые
      сходятся с итогом, а при клампе рейтинга это неверно (поток 30 000: рейтинг 0,557
      против суммы 0,307) — либо оговорить, либо считать поправку от клампнутой суммы
- [x] 6.7 `CONTEXT.md`: одно понятие названо тремя именами (`parts.equilibrium` в коде,
      «balance» в глоссарии, «Swing on a full platform» в UI) и в глоссарий не заведено
- [x] 6.8 Русский комментарий вставлен в англоязычный блок `engine/__tests__/engine.test.ts` —
      следовать языку блока
- [x] 6.9 Предикат равновесия («приход больше вместимости») повторяется в `rating.ts` и
      `optimize.ts`: вынесена арифметика, а не сама проверка. В `waiting.test.ts` четыре
      разных `ratingOf` с двумя разными сигнатурами — развести имена

## 7. Находки четвёртого ревью

- [x] 7.1 Оценка возвращает долю с границы баланса, а штраф — со ступени под ней, и наружу
      доля идёт от ступени: состав, вывозящий весь приход, помечается остатком. Полоса —
      2,1 %, 10,0 % и 11,6 % вместимости на трёх замерах. Свести к одному значению: доля =
      граница, рейтинг = обратный пересчёт из неё, разница со ступенью — в `parts.equilibrium`
- [x] 7.2 Та же причина роняет монотонность там, где остатка нет: скан 60 конфигураций даёт
      5 падений, худшее — поток 500 000 → 1 000 000 роняет вывоз с 5083 до 3612. Тест 6.2
      расширить на режим без остатка
- [x] 7.3 Пересверить эталоны после сдвига доли: партия, день длины, `optimize-branches`,
      `londworth-rubber` — числа обновить осознанно
- [x] 7.4 Комментарий над `SHARE_EPSILON` описывает проходы итерации, которой больше нет
- [x] 7.5 `arrivalsBetweenVisits` экспортируется с обоснованием «нужна кеширующему вызову», но
      снаружи не используется — кеш зовёт `visitClearsFlow`
- [x] 7.6 Доккомментарий `WaitingSettleParams.ratingAt` скопирован у соседа и обещает, что
      вместимость едет с вызовом, тогда как ветка ожидания всегда передаёт `Infinity`
- [x] 7.7 `CONTEXT.md`, статья «Balance share»: `parts.equilibrium` (дельта рейтинга) и доля
      баланса склеены в «три названия, одну величину» — развести
- [x] 7.8 Два одинаковых деления отрезка (по доле и по остатку) — вынести общий шаг;
      `share: balanceShare` пишется и не читается; `clampRating(balanceShare * 256 - 1)`
      считается дважды; `ratingOf` снова означает три разные вещи в трёх модулях
- [x] 7.9 Записать в задачу 6.4 фактический замер производительности (база 328–352 мс против
      360–387 мс на HEAD), как она и требовала

## 8. Находки пятого ревью

- [x] 8.1 Поправка `parts.equilibrium` обещает ноль на станции, которая пустеет, и UI по этому
      нулю решает, показывать ли строку подсказки. На деле граница баланса всегда лежит выше
      ступени под ней: ненулевая в 40 замерах из 40, максимум +36,1. Переопределить поле
      (ноль только когда баланс попал ровно на ступень) и показывать строку по тому, видна ли
      поправка после округления
- [x] 8.2 Свести три имени одной величины к одному: поле в коде, статья в `CONTEXT.md` и
      строка UI. Заодно название «на полной платформе» неверно — колебание есть и на
      пустеющей станции
- [x] 8.3 Тест, запирающий расхождение ярусов кеша: доля, отданная наружу, — ровно та, по
      которой решается, копится ли остаток (`visitClearsFlow(..., r.deliveredShare, ...)`
      совпадает с `r.backlog === 0`). Сейчас это держится на побитовом равенстве, а JSDoc
      поля `rating` приглашает его округлить
- [x] 8.4 Позиционные `number`-аргументы (`visitClearsFlow`, `ratingAt` / `rateRoute`) —
      параметр-объект; находка висит с третьего ревью
- [x] 8.5 Дословный дубль комментария в `rating.ts` (две строки «The station can hand over…»),
      мёртвая запись `share: balanceShare`, `ratingOf` в прежнем смысле в `tabs-agree.test.ts`
      и `londworth-rubber.test.ts`

## 9. Находки шестого ревью

- [x] 9.1 Требование монотонности переписано по факту (просадка до 1 % на переходе через
      ступень) — привести тест в соответствие: тонкая сетка вместо девяти точек и порог, а не
      строгое неубывание. Прежняя проба показывала ноль падений только из-за редкой сетки
- [x] 9.2 `/** 0..255, as the game stores it. */` над `StationRating.rating` — величина
      дробная (172,558 на эталоне); задача 8.3 это называла, а комментарий остался
- [x] 9.3 Слагаемые в подсказке округляются по отдельности, поэтому их сумма расходится с
      показанным итогом на единицу в 89 случаях из 720 — при том что строка поправки добавлена
      ровно ради того, чтобы они сходились
- [x] 9.4 Мёртвая запись `share: balanceShare` и пустая строка за ней в `rating.ts`: отмечены
      как убранные в 8.5, но остались
- [x] 9.5 Хвосты именования: `arrivalsBetweenVisits` берёт три позиционных числа под JSDoc,
      объясняющим, почему у соседа объект; анонимный тип `{ pickupIntervalDays, visitCapacity }`
      выписан пятью копиями при живых `VisitFlow` и `RouteVisit`; локальная `step` названа как
      элемент `WAITING_CARGO_STEPS`
- [x] 9.6 ~~Допуски эталонов~~ — отклонено: допуски работают, запас в них заложен осознанно
      (игра хранит мгновенный рейтинг, оценка — среднее по кругу), сужать до последней цифры
      значило бы ловить шум округления вместо регресса
- [x] 9.7 ~~Перемерить производительность~~ — отклонено: замер в 6.4 верен для файла, на
      котором делался (`optimize-supply.test.ts`); разница в ревью — с другого файла, а не
      расхождение

## 10. Завершение

- [x] 10.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском, с пометкой
      `**BREAKING**` (числа меняются при том же вводе)
- [x] 10.2 Прогнать `make test` и `make verify`
