# Tasks: track-type-axis

## 1. Данные railtype: ваниль

- [ ] 1.1 `extract_vanilla.py`: декодировать макрос колонки railtype в лейбл игры в самом
      экстракторе (`R → RAIL`, `C → ELRL`, `O → MONO`, `L → MGLV`), рядом с существующими
      `RUNNING_COST_KEYS`/`ENGINE_CLASS_KEYS`. Проверка: `make data` проходит, в
      `vanilla_trains.json` у машин лейблы вместо букв, `C` наружу не выходит.
- [ ] 1.2 `extract_vanilla.py`: писать `meta.railtypes` — четыре записи с масками
      powered/compatible и `speed_limit_internal` из `vendor/openttd/src/table/railtypes.h`,
      английские имена из `src/lang/english.txt:2816-2819`. Проверка: у `RAIL`
      powered = `[RAIL, ELRL]`, у `ELRL` powered = `[ELRL]`, compatible = `[RAIL, ELRL]`,
      лимит у всех 0.

## 2. Данные railtype: Iron Horse

- [ ] 2.1 `extract_iron_horse.py`: писать `meta.railtypes` из
      `vendor/iron-horse/src/railtypes/*.py` — 6 записей, английские имена из
      `src/lang/english.toml:675-734`, `speed_limit` из `railtype.py`. Для `rail` и
      `rail_electrified_ohle` (`suppress_for_nml`, `sort_order = None`) маски и порядок
      берутся ванильные. Проверка: 6 записей с лейблами RAIL, ELRL, NAAN, MTRO, LGVN, LGVE;
      `lgv` стоит у LGVN и LGVE.
- [ ] 2.2 **Нормализация масок** (без неё каталог пустеет): отбросить legacy-лейблы, которых
      нет в собственной таблице (`NAAE`, `IHA_`, `IHB_`, `IHBA`), и добавить в обе маски сам
      тип. Проверка: тест пайплайна — у каждого типа его лейбл входит и в `powered`, и в
      `compatible`, все ссылки масок разрешаются в таблице; `NAAN` и `MTRO` не пусты.
- [ ] 2.3 Эталоны `test_known_values.py` на railtype машин: узкоколейная несёт `NAAN`,
      электричка — `ELRL`, электро-дизель — `[ELRL, RAIL]`, `power_by_source` у него —
      мапа с двумя источниками. Проверка: `make test` (pipeline) зелёный.
- [ ] 2.4 `validate.py`: таблица railtypes непуста, `track_types` каждой машины ссылаются на
      известные лейблы (с учётом ванильной пары у Iron Horse), маски непусты. Проверка:
      `make data` падает на временно испорченных данных и проходит на живых.

## 3. Русские имена путей

- [ ] 3.1 Генерация словаря `railtypes.ru.json` рядом с `cargos.ru.json`: ванильные имена из
      `russian.txt:2979-2990`, приведённые к читаемой форме («Магнитная ж/д» вместо
      «Магнитный») через `ru_overrides.json`; для путей Iron Horse русских строк в наборе
      нет — переводы наши. Проверка: `make check-i18n` зелёный, словарь перегенерируется
      воспроизводимо.
- [ ] 3.2 Различение `LGVN` и `LGVE`: набор назвал их одной строкой — электрифицированный
      вариант получает уточнение в обоих языках. Проверка: в списке нет двух одинаковых
      подписей (юнит на список значений).
- [ ] 3.3 Расширить `i18n/__tests__/locales.test.ts` на записи railtypes — путь без имени
      валит тест. Проверка: тест падает при временно вычищенной записи, проходит на живых
      данных.

## 4. Движок

- [ ] 4.1 `types.ts`: тип `Railtype`; `dataset.ts`: `activeRailtypes(game)`; убрать
      `RAILTYPE_TRACK` из `vanilla.ts` (лейбл приходит из данных). Проверка: юнит — ваниль
      отдаёт 4 пути, Iron Horse 6.
- [ ] 4.2 Модуль `engine/tracktypes.ts`: `isPoweredOn`, `isCompatibleWith`, `trackSpeedLimit`.
      Проверка: юниты — электровоз не запитан на RAIL и запитан на ELRL; дизель запитан на
      обоих; NG-вагон несовместим с RAIL; узкоколейный локомотив запитан на NAAN (ловит
      ненормализованные маски).
- [ ] 4.3 Там же: `vehicleSpeedOn` (LGV-скорость на `lgv`-пути) и `vehiclePowerOn` (источник
      тяги по пути, `power_by_source`). Проверка: юниты — HST на LGVE едет `speed_on_lgv`, на
      ELRL обычную; электро-дизель на RAIL отдаёт дизельную мощность, на ELRL — OHLE.
- [ ] 4.4 `optimize.ts`: фильтры локомотивов и вагонов через `isPoweredOn`/`isCompatibleWith`
      от `calc.trackType`; убрать параметр `allowElectric` и `isPureElectric`. Проверка:
      юниты по сценариям спеки `train-optimizer` (смена пути меняет выдачу; на NAAN выдача
      не пуста).
- [ ] 4.5 `consist.ts`: принять railtype, предел скорости и мощность считать через
      `vehicleSpeedOn`/`vehiclePowerOn`, предел состава ограничить `trackSpeedLimit`.
      Проверка: юнит с синтетическим railtype — лимит ниже предела состава режет плечи рейса.

## 5. Настройка и сторы

- [ ] 5.1 `engine/settings.ts`: домен `calc.trackType` — лейбл railtype, дефолт `'RAIL'`,
      нормализация неизвестного лейбла в `'RAIL'` при чтении. Проверка: юнит на чужой лейбл
      после смены набора.
- [ ] 5.2 Кейс в `engine/__tests__/settings-effect.test.ts` перестроить с `'NG'` на пару,
      различимую только путём (`RAIL` против `ELRL`), со своей базой через `base`/`baseCalc`.
      Проверка: тест зелёный и падает при закомментированном прокидывании `trackType`.
- [ ] 5.3 `settingsStore` v2 → v3: миграция семейств в лейблы (`NG → NAAN`, `METRO → MTRO`,
      `MAGLEV → MGLV`), веткой лесенкой. Проверка: тесты на состояние, сохранённое каждой
      прошлой версией.
- [ ] 5.4 Одноразовый шаг апгрейда **вне сторов**, в точке входа до их создания: читает три
      сырых ключа `localStorage`, при `trackType === 'RAIL'` и сохранённом
      `allowElectric === true` (стор оптимизатора приоритетнее стора снабжения) пишет
      `'ELRL'`, вычищает поля флажка и помечается выполненным; отсутствие или порча чужого
      ключа оставляет `'RAIL'`. Порядок гидратации сторов на результат влиять не должен.
      Проверка: юниты на пять случаев — галка в оптимизаторе, в снабжении, обе, ни одной,
      испорченный JSON чужого ключа; плюс тест, что повторный запуск ничего не меняет.
- [ ] 5.5 `searchParams.ts` и `industrySupplyStore.ts`: перестать объявлять и читать
      `allowElectric` (миграции самих сторов о флажке не знают — его вычистил шаг 5.4).
      Проверка: старое сохранённое состояние читается без ошибок, вкладки считают по общему
      типу путей.

## 6. UI

- [ ] 6.1 Компонент `TrackTypeSelect` (пишет в `settingsStore.calc.trackType`, подписи из
      таблицы railtype и словаря). Проверка: юнит — список значений следует набору, две
      одинаковые строки набора дают различимые подписи.
- [ ] 6.2 Поставить его в ряды отбора вкладок подбора, конструктора и дохода рейса; удалить
      флажок электрификации подбора и локальный фильтр пути `ConsistPage`. Проверка: смена
      пути на одной вкладке меняет выдачу другой; флажка и фильтра в рядах нет.
- [ ] 6.3 То же для вкладки снабжения: `IndustrySupplyPage.tsx` получает контрол вместо
      флажка, `industry-supply/inputs.ts` перестаёт передавать `allowElectric` в
      `optimizeConsists`. Проверка: расчёт снабжения следует общему пути, ввод не потерян.
- [ ] 6.4 `TrackTypeSelect` в Settings + ключи подписи в `en.json`/`ru.json` («Track type» /
      «Тип путей», hint). Проверка: `i18n/__tests__/locales.test.ts` зелёный.

## 7. Финал

- [ ] 7.1 `CHANGELOG.md` → `[Unreleased]`: запись на английском с пометкой **BREAKING**
      (миграция сохранённого выбора и упразднение флажка; числа электро-дизелей и HST теперь
      зависят от пути). Проверка: секция непуста, формат Keep a Changelog.
- [ ] 7.2 Полный прогон: `make data`, `make test`, `make verify` (включая check-i18n и
      check-visual). Проверка: всё зелёное.
