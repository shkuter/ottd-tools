# Tasks: track-type-axis

## 1. Данные railtype: ваниль

- [x] 1.1 `extract_vanilla.py`: декодировать макрос колонки railtype в лейбл игры в самом
      экстракторе (`R → RAIL`, `C → ELRL`, `O → MONO`, `L → MGLV`), рядом с существующими
      `RUNNING_COST_KEYS`/`ENGINE_CLASS_KEYS`. Проверка: `make data` проходит, в
      `vanilla_trains.json` у машин лейблы вместо букв, `C` наружу не выходит.
- [x] 1.2 `extract_vanilla.py`: писать `meta.railtypes` — четыре записи с масками
      powered/compatible и `speed_limit_internal` из `vendor/openttd/src/table/railtypes.h`,
      английские имена из `src/lang/english.txt:2816-2819`. Проверка: у `RAIL`
      powered = `[RAIL, ELRL]`, у `ELRL` powered = `[ELRL]`, compatible = `[RAIL, ELRL]`,
      лимит у всех 0.

## 2. Данные railtype: Iron Horse

- [x] 2.1 `extract_iron_horse.py`: писать `meta.railtypes` из
      `vendor/iron-horse/src/railtypes/*.py` — 6 записей, английские имена из
      `src/lang/english.toml:675-734`, `speed_limit` из `railtype.py`. Для `rail` и
      `rail_electrified_ohle` (`suppress_for_nml`, `sort_order = None`) маски и порядок
      берутся ванильные. Проверка: 6 записей с лейблами RAIL, ELRL, NAAN, MTRO, LGVN, LGVE;
      `lgv` стоит у LGVN и LGVE.
- [x] 2.2 **Нормализация масок** (без неё каталог пустеет): отбросить legacy-лейблы, которых
      нет в собственной таблице (`NAAE`, `IHA_`, `IHB_`, `IHBA`), добавить в обе маски сам
      тип и достроить `compatible` до `powered` (правило игры, а не набора). Пометить
      скрытые из меню строительства пути (`RAILTYPE_FLAG_HIDDEN`) полем `hidden`. Проверка:
      тесты пайплайна — свой лейбл в обеих масках, `powered ⊆ compatible`, все ссылки
      разрешаются, `NAAN` и `MTRO` не пусты, `LGVN` помечен скрытым.
- [x] 2.3 Эталоны `test_known_values.py` на railtype машин: узкоколейная несёт `NAAN`,
      электричка — `ELRL`, электро-дизель — `[ELRL, RAIL]`, `power_by_source` у него —
      мапа с двумя источниками. Проверка: `make test` (pipeline) зелёный.
- [x] 2.4 `validate.py`: таблица railtypes непуста, `track_types` каждой машины ссылаются на
      известные лейблы (с учётом ванильной пары у Iron Horse), маски непусты. Проверка:
      `make data` падает на временно испорченных данных и проходит на живых.

## 3. Русские имена путей

- [x] 3.1 Генерация словаря `railtypes.ru.json` рядом с `cargos.ru.json`. Имена, которые
      источник не даёт в пригодном виде, — наши: у путей Iron Horse русских строк нет вовсе,
      а ванильные строки игры («Монорельсовый», «Магнитный», «Ж/д») — прилагательные без
      существительного. Все они объявлены в `RAILTYPES_WITHOUT_A_SOURCE` с объяснением, а не
      в `ru_overrides.json`: тот файл держит только правки поверх чужого перевода. Проверка:
      `make check-i18n` зелёный, словарь перегенерируется воспроизводимо.
- [x] 3.2 Различение `LGVN` и `LGVE`: набор назвал их одной строкой — электрифицированный
      вариант получает уточнение в обоих языках. Проверка: в списке нет двух одинаковых
      подписей (юнит на список значений).
- [x] 3.3 Расширить `i18n/__tests__/locales.test.ts` на записи railtypes — путь без имени
      валит тест. Проверка: тест падает при временно вычищенной записи, проходит на живых
      данных.

## 4. Движок

- [x] 4.1 `types.ts`: тип `Railtype`; `dataset.ts`: `activeRailtypes(game)` и
      `selectableRailtypes(game)`; `RAILTYPE_TRACK` в `vanilla.ts` перестаёт декодировать
      букву и переводит лейбл в семейство машины (семейство остаётся ради `purchaseKey`).
      Проверка: юниты — ваниль отдаёт 4 пути, Iron Horse 6, скрытый в выбор не попадает,
      незнакомый лейбл читается как обычный путь.
- [x] 4.2 Модуль `engine/tracktypes.ts`: `isPoweredOn`, `isCompatibleWith`, `trackSpeedLimit`.
      Проверка: юниты — электровоз не запитан на RAIL и запитан на ELRL; дизель запитан на
      обоих; NG-вагон несовместим с RAIL; узкоколейный локомотив запитан на NAAN (ловит
      ненормализованные маски).
- [x] 4.3 Там же: `vehicleSpeedOn` (LGV-скорость на `lgv`-пути) и `vehiclePowerOn` (источник
      по иерархии набора OHLE > METRO > BATTERY_HYBRID > DIESEL > STEAM, `power_by_source`).
      Проверка: юниты — HST на LGVE едет `speed_on_lgv`, на ELRL обычную; электро-дизель на
      RAIL отдаёт дизельную мощность, на ELRL — OHLE; при слабом OHLE побеждает всё равно он
      (порядок, а не величина); источник вне иерархии машину не обнуляет.
- [x] 4.4 `optimize.ts`: фильтры локомотивов и вагонов через `isPoweredOn`/`isCompatibleWith`
      от `calc.trackType`; убрать параметр `allowElectric` и `isPureElectric`. Проверка:
      юниты по сценариям спеки `train-optimizer` (смена пути меняет выдачу; на NAAN выдача
      не пуста).
- [x] 4.5 `consist.ts`: принять railtype, предел скорости и мощность считать через
      `vehicleSpeedOn`/`vehiclePowerOn`, предел состава ограничить `trackSpeedLimit`
      (перевод в mph — точным отношением, без усечения), тягу складывать только у машин с
      ненулевой мощностью на этом пути. Проверка: юниты — лимит ниже предела состава режет
      плечи рейса; электровоз на пути без сети даёт и нулевую мощность, и нулевую тягу.

## 5. Настройка и сторы

- [x] 5.1 `engine/settings.ts`: домен `calc.trackType` — лейбл railtype, дефолт `'RAIL'`.
      Незнакомый лейбл нормализуется при **чтении**, в `dataset.ts::activeRailtype()` — там,
      где известен активный набор. Проверка: юнит на чужой лейбл после смены набора.
- [x] 5.2 Кейс в `engine/__tests__/settings-effect.test.ts` перестроить с `'NG'` на пару,
      различимую только путём (`RAIL` против `ELRL`); своя база не нужна — эталон и так
      считается на обычном пути, важнее убрать пиннинг пути из `baseParams`, иначе кейс
      слеп к фильтру оптимизатора. Проверка: тест зелёный и падает при закомментированном
      прокидывании `trackType`.
- [x] 5.3 `settingsStore` v2 → v3: миграция семейств в лейблы (`NG → NAAN`, `METRO → MTRO`,
      `MAGLEV → MGLV`), веткой лесенкой. Проверка: тесты на состояние, сохранённое каждой
      прошлой версией.
- [x] 5.4 Одноразовый шаг апгрейда **вне сторов**: читает три сырых ключа `localStorage`,
      при `trackType === 'RAIL'` и сохранённом `allowElectric === true` пишет `'ELRL'`, сам
      вычищает поля флажка из обоих ключей вкладок и помечается выполненным; отсутствие или
      порча чужого ключа оставляет `'RAIL'`. Запускается импортом отдельного модуля
      (`state/upgradeOnLoad.ts`), стоящим первым в `main.tsx`: вызов в теле точки входа
      опаздывает — ES-модули выполняют импорты раньше, и стор вкладки успевает переписать
      свой ключ. Проверка: юниты на пять случаев (галка в оптимизаторе, в снабжении, обе, ни
      одной, испорченный JSON), повторный запуск ничего не меняет, плюс два теста на порядок
      — первый импорт точки входа и перенос по факту импорта модуля.
- [x] 5.5 `searchParams.ts` и `industrySupplyStore.ts`: перестать объявлять и читать
      `allowElectric`; их миграция v1 → v2 убирает поле как подстраховка, переносом она не
      занимается. Проверка: тесты в `state/__tests__/tabStoresYear.test.ts` — состояние с
      флажком (и состояние ещё более старой версии) читается, поля у стора не остаётся,
      остальные сохранённые значения целы.

## 6. UI

- [x] 6.1 Компонент `TrackTypeField` (пишет в `settingsStore.calc.trackType`, подписи из
      таблицы railtype и словаря, список — `selectableRailtypes`, ширина и подпись —
      пропами, чтобы строка настроек не дублировала свою). Проверка: юниты — список следует
      набору, скрытый путь в нём не появляется, две одинаковые строки набора дают различимые
      подписи, незнакомый лейбл показывается тем же путём, каким считает движок.
- [x] 6.2 Поставить его в ряды отбора вкладок подбора, конструктора и дохода рейса; удалить
      флажок электрификации подбора и локальный фильтр пути `ConsistPage`. Проверка:
      компонентный тест `features/__tests__/trackType.test.tsx` — поле есть на каждой из
      четырёх считающих вкладок (подбор, конструктор, доход рейса, снабжение), выбор на
      одной виден на другой, каталог следует выбору и своего фильтра пути не имеет,
      скрытый путь в списке не появляется.
- [x] 6.3 То же для вкладки снабжения: `IndustrySupplyPage.tsx` получает контрол вместо
      флажка, `industry-supply/inputs.ts` перестаёт передавать `allowElectric` в
      `optimizeConsists`. Проверка: тест `industry-supply/__tests__/inputs.test.ts` — на
      электрифицированном пути подбирается другой состав, на узкоколейке только узкоколейный.
- [x] 6.4 `TrackTypeField` в Settings + ключи подписи в `en.json`/`ru.json` («Track type» /
      «Тип путей», hint). Проверка: `i18n/__tests__/locales.test.ts` зелёный.

- [x] 6.5 Каталог показывает числа выбранного пути (мощность по источнику, скорость с учётом
      лимита), а сортировка читает те же значения. Проверка: юниты
      `features/consist/__tests__/sorting.test.ts` — электро-дизель ранжируется мощностью
      этой линии, скоростной поезд своей скоростью, электровоз без проводов уходит из
      сортировки, лимит пути режет колонку скорости.
- [x] 6.6 Вкладка «Партия»: путь маршрута выводится из состава (`trackTypeOfConsist`), а не
      берётся из настроек калькулятора — иначе электрический маршрут импортированной партии
      считался бы с нулевой мощностью. Проверка: юниты — электровоз читается как идущий под
      контактной сетью, дизель как идущий по обычному пути, состав из машин двух колей не
      даёт пути вовсе.

- [x] 6.7 Предупреждение о машинах, которые не ходят по выбранному пути: назвать их (каталог
      их уже скрывает, а сводка считает по нулю — нули надо объяснить). Состав не разбирать.
      Компонент общий (`components/StrandedVehicles.tsx`) и стоит на **обеих** вкладках,
      которые считают этот состав и дают менять путь, — конструктора и дохода рейса: на
      второй нули видны не нулями, а рейсом в сотни дней. Проверка: компонентный тест —
      предупреждение называет машину на каждой из двух вкладок, исчезает при возврате
      подходящего пути, состав остаётся собранным; строки в `en.json`/`ru.json`.

## 7. Финал

- [x] 7.1 `CHANGELOG.md` → `[Unreleased]`: запись на английском с пометкой **BREAKING**
      (миграция сохранённого выбора и упразднение флажка; числа электро-дизелей и HST теперь
      зависят от пути). Проверка: секция непуста, формат Keep a Changelog.
- [x] 7.2 Полный прогон: `make data`, `make test`, `make verify` (включая check-i18n и
      check-visual). Проверка: всё зелёное.
