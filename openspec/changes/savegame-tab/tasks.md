## 1. Слой данных: владелец, расстояния, настройки партии

- [x] 1.1 Читать `owner` у станции (`savegame/extract/stnn.ts`, поле `base`) и провести его в
      `SnapshotStation.companyId` — проверка: кейс в `snapshot-vanilla.test.ts` на построенном
      чанке со станциями двух компаний и кейс в `snapshot.test.ts` на настоящем сейве
      (станции нефтяных вышек не принадлежат никому)
- [x] 1.2 Читать `xy` у станции и размеры карты из чанка `MAPS` (`dim_x`/`dim_y`) — проверка:
      кейсы в `snapshot.test.ts` (размер карты настоящего сейва, снапшот без размера карты
      собирается и остаётся без расстояний) и в `snapshot-vanilla.test.ts` (синтетический
      чанк `MAPS`)
- [x] 1.3 Считать в экстракторе расстояния между последовательными станционными остановками
      маршрута (манхэттен по развёрнутым через `dim_x` координатам) и класть их в
      `SnapshotRoute.legTiles`; координаты и размер карты в модель снапшота не попадают —
      проверка: кейс в `snapshot.test.ts` на маршруте со станциями, отстоящими на известные
      dx/dy
- [x] 1.4 Завести тип `SnapshotSettings` (`game: GameSettings`, `calc: CalcSettings`) и поле
      `settings` в `SnapshotRecord`; собирать его при подтверждении импорта как дефолты,
      перекрытые извлечёнными значениями — проверка: кейс в `snapshotStore.test.ts`, что
      значение, которого нет в сейве, равно дефолту, а не текущей настройке стора
- [x] 1.5 Снимать настройки тем же подтверждением, что сохраняет снапшот (`savegame/apply.ts`)
      — проверка: кейсы в `savegame/__tests__/apply.test.ts` (подтвердили при настройках,
      отличных от сейва → в записи значения сейва; правки после импорта запись не трогают)
- [x] 1.6 Поднять `SNAPSHOT_SCHEMA_VERSION` до 2 — проверка: существующий кейс на
      отбрасывание записи с чужой схемой проходит на записи версии 1

## 2. Расчётный слой вкладки

- [x] 2.1 Чистый модуль строк маршрутов: из снапшота собирает по маршруту остановки, парк,
      груз и факт-прибыль (сумма `profitLastYear` парка, `profitThisYear` — отдельным полем
      как неполный год) — проверка: юнит-тест на фикстуре, включая маршрут из одного поезда,
      поезд без приказов и партию первого года (прошлого года нет)
- [x] 2.2 Признак применимости прогноза: меньше двух станционных остановок, груз не определён,
      в составе есть неопознанная машина, составы парка неодинаковы — каждый со своей
      причиной — проверка: юнит-тест на все четыре случая, включая состав с одним чужим
      вагоном (не только целиком неопознанный)
- [x] 2.3 Прогноз через `engine/trip.ts`: `entries` из `consist`, `meta` из `dataset`, `cargo`
      и `payment` — по экономике из `SnapshotSettings`, `distanceTiles` — из `legTiles`,
      `fleetSize` — размер парка, `waitForFullLoad` — из `stops[].fullLoad`, `game`/`calc` —
      из `SnapshotSettings` — проверка: юнит-тест, что прогноз не меняется при подмене
      текущих настроек в `settingsStore`

## 3. Оболочка

- [x] 3.1 Запись вкладки в `tabs.ts` (без `windowColour`) и ленивый маршрут в `App.tsx` —
      проверка: маршрут открывается, код вкладки лежит отдельным чанком (`npm run build`)
- [x] 3.2 Кнопка вкладки показывается только при наличии записи снапшота, заголовок — её
      `fileName`; прямой адрес без снапшота ведёт на главную — проверка: кейсы в
      `features/__tests__` со снапшотом и без него
- [x] 3.3 Запись вкладки в `visual/routes.ts` (`PER_TAB`) и посев снапшота-фикстуры в
      IndexedDB в harness визуальных проверок — проверка: `make check-visual` доходит до
      вкладки и проверяет её, а не падает на исключении или таймауте селектора

## 4. Под-вкладки

- [x] 4.1 Каркас вкладки: четыре под-вкладки и переключатель компаний с дефолтом «первая
      человеческая, иначе первая» — проверка: кейс на снапшоте, где компания 0 — AI
- [x] 4.2 «Маршруты», строка: остановки, парк, груз, факт за прошлый год и текущий год как
      неполный, прогноз или названная причина его отсутствия, подсказка о разной модели года —
      проверка: кейс рендера строки из фикстуры и кейс на маршрут без прогноза
- [x] 4.3 «Маршруты», раскрытие строки: все остановки с признаком полной загрузки, поезда
      парка по отдельности, разбор прогноза (расстояние, груз, время рейса, доход за рейс) —
      проверка: кейс на развёрнутую строку
- [x] 4.4 «Поезда», список и колонки: номер/имя, состав, груз, прибыль за два года, год
      постройки, «остановлен»; неопознанная машина показана как неопознанная — проверка:
      кейс на состав с чужим GRF
- [x] 4.5 «Поезда», фильтр по группам с включением подгрупп (иерархия `SnapshotGroup.parent`) —
      проверка: кейс на группу с подгруппами
- [x] 4.6 «Станции»: ожидающий груз и рейтинг, рейтинга нет там, где его нет в снапшоте;
      waypoints не попадают; список делится по выбранной компании — проверка: кейсы на груз
      без рейтинга, на waypoint и на станции двух компаний
- [x] 4.7 «Индустрии»: производство и вывоз за месяц, список не зависит от выбранной компании,
      заглушка вместо названия типа при `catalogueId === null` — проверка: кейс на ванильной
      фикстуре
- [x] 4.8 Сообщение «прогноз считается по настройкам партии», когда текущие настройки
      отличаются от снятых при импорте — проверка: кейс, где после импорта поменяли год расчёта

## 5. Сброс сохранённых данных

- [x] 5.1 `deleteSnapshot()` в `snapshotStore.ts`; сброс сохранённых данных ждёт удаления до
      перезагрузки страницы — проверка: кейсы в `state/__tests__/reset.test.ts` (запись
      исчезает и из памяти, и из базы) и `features/settings/__tests__/reset.test.tsx` (кнопка
      «Сбросить всё» доводит удаление до конца перед `reload`)
- [x] 5.2 Подсказка на вкладке настроек называет и импортированную партию — проверка:
      `locales.test.ts` зелёный

## 6. Названия предприятий базовой игры и иконки грузов

- [x] 6.1 Извлекать предприятия базовой игры (`pipeline/extract_vanilla.py`:
      `_origin_industry_specs` задаёт тип из сейва, `STR_INDUSTRY_NAME_*` — имя) в
      `vanilla_industries.json`, русские имена — тем же путём, что ванильные грузы
      (`extract_firs_ru.py`) — проверка: `make data` и `make check-i18n` зелёные, имена
      совпадают с окном предприятий игры
- [x] 6.2 Подпереть инвариант «индекс = тип»: якоря по готовому JSON в `validate.py` (пять
      опорных типов, уникальность id) и выписанный эталон порядка всех 37 имён в
      `pipeline/tests/test_known_values.py` — проверка: перестановка двух записей в середине
      роняет `test_types_are_the_games_own_order`
- [x] 6.3 Ванильные предприятия входят в проверку полноты перевода рядом с ванильными грузами
      (`i18n/__tests__/locales.test.ts`) — проверка: удаление записи из словаря роняет тест
- [x] 6.4 Опознавать ванильные типы в снапшоте (нет чанка `IIDS` → тип это индекс таблицы
      игры) и называть их в списке — проверка: кейсы в `snapshot-vanilla.test.ts` (число по
      типам сходится с окном игры: 40 из 40) и в `GamePage.test.tsx` (имя, которого у FIRS
      нет, приходит из ванильного набора)
- [x] 6.5 Иконка груза перед названием во всех списках вкладки — проверка: кейс на иконки
      в `GamePage.test.tsx`, `make check-visual` зелёный
- [x] 6.6 Дельта `vanilla-dataset`: набор перестаёт быть только машинами и грузами —
      требование о предприятиях базовой игры, Purpose принятой спеки поправлен там же —
      проверка: `openspec validate savegame-tab --strict` и `openspec validate --specs`
- [x] 6.7 Слияние имён игры с именами FIRS вынесено в `merge_game_names` (одна функция вместо
      двух копий цикла) и покрыто тестом: расхождение имён у общего id роняет сборку —
      проверка: снятие защиты роняет `test_disagreeing_names_stop_the_build`
- [x] 6.8 Число типов сверяется с константой самой игры (`NEW_INDUSTRYOFFSET` читается из
      `industry_type.h`), а не с литералом, и разбор падает у источника — проверка:
      `test_a_shifted_table_stops_the_build` подменяет таблицу игры и ждёт падения сборки;
      все кейсы, читающие `vendor/`, собраны в одном классе и без чекаута пропускаются, а не
      падают — прогон без него даёт «OK (skipped=2)»

## 7. Локализация, документация, прогон

- [x] 7.1 Новые UI-строки в `i18n/en.json` и `ru.json` — проверка:
      `i18n/__tests__/locales.test.ts` зелёный
- [x] 7.2 Статья **Snapshot settings** в `CONTEXT.md` (термин для настроек партии внутри
      снапшота) — проверка: термин используется в коде (`SnapshotSettings`) и артефактах
      одним словом
- [x] 7.3 Запись в `CHANGELOG.md` → `## [Unreleased]` → `### Added` (на английском) —
      проверка: секция содержит запись о вкладке партии
- [x] 7.4 Прогон `make test`, `make check-i18n` и `make check-visual` — проверка: все три
      команды зелёные
