## 1. Данные

- [ ] 1.1 Убрать удвоение `power_hp` и `weight_t` для `multihead` в
      `pipeline/extract_vanilla.py`; проверка — эталоны в `pipeline/tests/test_known_values.py`
      обновлены на значения таблицы игры (SH '125': мощность 4500, вес 70; DMU: 600 и 32) и
      `pipeline/.venv/bin/python -m unittest discover pipeline/tests` зелёный
- [ ] 1.2 Перегенерировать данные (`make data`) и закоммитить `web/src/data/vanilla_trains.json`;
      проверка — `validate.py` проходит, в JSON у сдвоенных машин стоят свойства таблицы

## 2. Правило игры в движке

- [ ] 2.1 Добавить `vehicleWeightT(train)` в `web/src/engine/consist.ts` рядом с
      `vehicleLengthUnits`, со ссылкой на `train.h: Train::GetWeight` и
      `engine.cpp: Engine::GetDisplayWeight`; проверка — юнит-тест: сдвоенная машина отдаёт
      удвоенный вес, обычная — свой
- [ ] 2.2 Перевести `consistPhysics` на `vehicleLengthUnits` и `vehicleWeightT`, удвоить число
      частей у сдвоенной и добавить тормозную растяжку за обе половины; снять комментарий о
      сознательном расхождении — его больше нет; проверка — юнит-тесты на состав из одной
      сдвоенной машины: длина 1 клетка, масса вдвое, тяга от удвоенной массы, число частей 2
- [ ] 2.3 Перевести оптимизатор (`web/src/engine/optimize.ts`) на `vehicleLengthUnits` во всех
      трёх местах — предел перебора, число вагонов, длина состава — и добавить длину целой
      машины в профиль вагона; проверка — юнит-тест: при длине платформы в N клеток состав со
      сдвоенной машиной берёт на столько вагонов меньше, на сколько занимает вторая половина
- [ ] 2.4 Перевести ключ пункта покупки (`web/src/engine/purchase.ts`) на длину и вес целой
      машины; проверка — существующий набор `engine/__tests__/purchase.test.ts` зелёный, плюс
      кейс: сдвоенная и обычная машина с одинаковыми свойствами длины и веса дают разные пункты
- [ ] 2.5 Перевести колонку «Масса» каталога и её ключ сортировки
      (`web/src/features/consist/ConsistPage.tsx`, `sorting.ts`) на `vehicleWeightT`; проверка —
      компонентный тест: в строке сдвоенной машины стоит вес пары, и он же сортирует; без правки
      ванильная строка показала бы вес половины

## 3. Проверка правила целиком

- [ ] 3.1 Завести тест, перебирающий все сдвоенные машины обоих наборов и сверяющий каждую
      величину с правилом игры (длина, вес, вместимость, число частей — вдвое; мощность и
      расходы — как есть); проверка — тест падает, если какую-то величину забыли перевести
- [ ] 3.2 Проверить эталонами числа из таблицы игры: состав из одного SH '125' — 4500 л.с.,
      140 т, 1 клетка; из одного Firebird — 3300 л.с., 136 т, 1 клетка; проверка — юнит-тест с
      выписанными числами

## 4. Строки, документация, вид

- [ ] 4.1 Добавить термин **Dual-headed vehicle** в `CONTEXT.md` в формулировке из design.md;
      проверка — термин есть, соседние записи на него ссылаются там, где речь о длине и весе
- [ ] 4.2 Дописать в `## [Unreleased]` → `### Fixed` файла `CHANGELOG.md` (на английском)
      запись с пометкой **BREAKING**: какие числа изменились и почему; проверка —
      `scripts/next-version.sh` даёт minor (версия 0.x)
- [ ] 4.3 Прогнать `make check-visual`; проверка — зелёный, новых исключений нет

## 5. Завершение

- [ ] 5.1 Прогнать `make test` (и `make data` перед ним, раз правится экстрактор); проверка —
      оба зелёные
