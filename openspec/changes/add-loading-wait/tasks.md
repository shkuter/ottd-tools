## 1. Глоссарий и допущения

- [ ] 1.1 Поправить `CONTEXT.md`, статья `Cargo per trip`: убрать неверное «the train waits to
      be filled rather than running light», описать две ветки загрузки и то, что физика в обеих
      считается на полном составе
- [ ] 1.2 Добавить в `CONTEXT.md` термины `Loading branch` (ветка загрузки) и `Accumulation
      wait` (ожидание накопления) с пометкой о допущении равномерного деления потока

## 2. Модуль ожидания

- [ ] 2.1 Создать `web/src/engine/waiting.ts`: темп накопления из отдаваемого за год потока и
      множителя длины дня, время накопления вместимости на парк, замкнутая форма
      `max(физический круг, парк × вместимость ÷ темп)`
- [ ] 2.2 Тесты `waiting.test.ts`: медленный источник (ожидание > 0), быстрый источник
      (ожидание = 0), удвоение парка (круг растёт, годовой вывоз тот же), множитель длины дня
      (ожидание растёт пропорционально), поток = 0 (ожидание не определено, не `NaN`)

## 3. Ветки в расчёте рейса

- [ ] 3.1 Ввести ветку загрузки параметром `tripMoney` (`waitForFullLoad`), по умолчанию —
      текущее поведение «уезжает с накопленным»
- [ ] 3.2 В ветке ожидания: круг из `waiting.ts`, груз за рейс равен вместимости, пересчёт
      рейсов за год и интервала от нового круга
- [ ] 3.3 Вынести в результат рейса поля обеих веток, чтобы вызывающий код мог их сравнить, не
      считая маршрут дважды
- [ ] 3.4 Тест-регрессия: при ветке без ожидания все числа `trip.ts` совпадают с текущими до
      последнего знака (снять эталон до правки)
- [ ] 3.5 Тесты веток: расхождение на медленном источнике, совпадение на быстром, равенство
      годового вывоза в обеих ветках

## 4. Перебор в оптимизаторе

- [ ] 4.1 Считать обе ветки в переборе, `tripSetup` при этом вычислять один раз
- [ ] 4.2 Выбирать ветку по цели через `GoalStrategy`, не дублируя логику сравнения
- [ ] 4.3 Добавить в `OptimizeResult` признак выигравшей ветки и различимость веток (при
      незаданном выпуске — «неразличимы»)
- [ ] 4.4 Тесты `optimize`: ветка без ожидания выигрывает по вывозу, ветка ожидания выигрывает
      по прибыли, при выпуске = 0 признак не выставляется
- [ ] 4.5 Проверить, что удвоение перебора не роняет отзывчивость выдачи (требование
      «Отзывчивость выдачи» в `train-optimizer`)

## 5. Интерфейс

- [ ] 5.1 Показать признак ветки в строке оптимизатора (значок или пометка в подсказке, без
      новой колонки)
- [ ] 5.2 Добавить выбор ветки на вкладку «Доход рейса» как состояние формы, не настройку
- [ ] 5.3 Новые UI-строки в `web/src/i18n/en.json` и `ru.json`, прогнать
      `i18n/__tests__/locales.test.ts`

## 6. Сверка с игрой

- [ ] 6.1 Завести эталонный сценарий из партии Steeltown (завод покрышек, каучук с причала,
      1 поезд × 14 вагонов) как тест: обе ветки, ожидаемое расхождение интервала
- [ ] 6.2 Сверить числа обеих веток с партией и записать расхождения; при расхождении больше
      допустимого — не подгонять формулу, а зафиксировать наблюдение в change

## 7. Завершение

- [ ] 7.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском, с пометкой
      `**BREAKING**`
- [ ] 7.2 Прогнать `make test` и `make verify`
