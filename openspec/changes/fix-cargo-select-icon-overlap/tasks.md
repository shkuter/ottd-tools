## 1. Подтвердить первопричину

- [x] 1.1 Запустить `make dev`, открыть вкладку дохода рейса и в chrome-devtools снять с поля
      груза `getComputedStyle(input).paddingInlineStart` и `--input-padding-inline-start`:
      убедиться, что вычисленный падинг равен 8px из тегового правила, а не ширине левой секции
- [x] 1.2 Записать эталонные размеры до правки (высота поля, падинги) на вкладках Route income,
      Best train и Consist builder, чтобы потом сверить, что поля не «поехали».
      Эталон: `height: 36px`, `padding: 5px 8px`, `font-size: 14px` — одинаково на всех вкладках

## 2. Правка стилей

- [x] 2.1 В `web/src/skin-mantine.css` дописать в блок `.mantine-Input-input` падинги ссылками
      на переменные Mantine — `padding-block: var(--input-padding-y, 0)` (фолбэк обязателен,
      у обычного поля переменная не определена), `padding-inline-start`/`-end` — на
      одноимённые переменные, с комментарием на английском о том, что теговое правило
      `input, select` из `index.css` иначе перебивает их по слою `app`
- [x] 2.2 Там же задать компактный `--input-left-section-width` под иконку груза 10×10
      (ширина иконки плюс поля по бокам), не трогая правую секцию

## 3. Проверка в браузере

- [x] 3.1 На всех трёх вкладках с выпадающим списком груза убедиться, что правый край
      `.cargo-icon` левее левого края текста, зазор — несколько пикселей, а не полполя
- [x] 3.2 Проверить длинное название груза на русском и английском: текст обрезается по правому
      краю текстовой области и не заезжает под стрелку списка
- [x] 3.3 Проверить числовое поле со стрелками (дистанция, год, длина станции): значение видно
      целиком, под стрелки не уходит
- [x] 3.4 Пройти по вкладкам без иконок в полях (Settings, фильтры каталога, FIRS chains) и
      сверить с эталоном из 1.2: высота и вертикальное выравнивание прежние, меняется только
      горизонтальный падинг — 8px на штатные 12px у поля без секций и на ширину секции там,
      где она есть

## 4. Завершение

- [x] 4.1 Дописать запись в `CHANGELOG.md` в секцию `[Unreleased]` (на английском) — исправление
      наложения иконки груза на текст в полях выбора
- [x] 4.2 Прогнать `make test` и `make build`

## 5. По итогам ревью

- [x] 5.1 Починить обрезку счётчика вагонов в конструкторе составов: убрать
      `.consist-list input { width: 52px }` (спорило с `w` на обёртке) и поднять счётчик до
      `w={84}` — текстовая область 42px, влезают и четырёхзначные значения
- [x] 5.2 Сузить правую секцию выпадающего списка до 24px (шеврон 18px) и расширить колонку
      контролов настроек 180px → 210px, чтобы длинные подписи («метрическая (км/ч)») не
      обрезались; сплошной скан всех вкладок — ни одного обрезанного поля
- [x] 5.3 Перенести иконку груза в фильтре каталога из соседнего элемента в `leftSection`,
      убрать осиротевшую обёртку `.field-with-icon` из `index.css`
- [x] 5.4 Уточнить комментарий в скине: 34px — это высота поля минус 2px, а не сама высота
