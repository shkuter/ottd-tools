## 1. Подтвердить первопричину

- [ ] 1.1 Запустить `make dev`, открыть вкладку дохода рейса и в chrome-devtools снять с поля
      груза `getComputedStyle(input).paddingInlineStart` и `--input-padding-inline-start`:
      убедиться, что вычисленный падинг равен 8px из тегового правила, а не ширине левой секции
- [ ] 1.2 Записать эталонные размеры до правки (высота поля, падинги) на вкладках Route income,
      Best train и Consist builder, чтобы потом сверить, что поля не «поехали»

## 2. Правка стилей

- [ ] 2.1 В `web/src/skin-mantine.css` дописать в блок `.mantine-Input-input` падинги ссылками
      на переменные Mantine — `padding-block: var(--input-padding-y, 0)` (фолбэк обязателен,
      у обычного поля переменная не определена), `padding-inline-start`/`-end` — на
      одноимённые переменные, с комментарием на английском о том, что теговое правило
      `input, select` из `index.css` иначе перебивает их по слою `app`
- [ ] 2.2 Там же задать компактный `--input-left-section-width` под иконку груза 10×10
      (ширина иконки плюс поля по бокам), не трогая правую секцию

## 3. Проверка в браузере

- [ ] 3.1 На всех трёх вкладках с выпадающим списком груза убедиться, что правый край
      `.cargo-icon` левее левого края текста, зазор — несколько пикселей, а не полполя
- [ ] 3.2 Проверить длинное название груза на русском и английском: текст обрезается по правому
      краю текстовой области и не заезжает под стрелку списка
- [ ] 3.3 Проверить числовое поле со стрелками (дистанция, год, длина станции): значение видно
      целиком, под стрелки не уходит
- [ ] 3.4 Пройти по вкладкам без иконок в полях (Settings, фильтры каталога, FIRS chains) и
      сверить с эталоном из 1.2, что вид полей не изменился

## 4. Завершение

- [ ] 4.1 Дописать запись в `CHANGELOG.md` в секцию `[Unreleased]` (на английском) — исправление
      наложения иконки груза на текст в полях выбора
- [ ] 4.2 Прогнать `make test` и `make build`
