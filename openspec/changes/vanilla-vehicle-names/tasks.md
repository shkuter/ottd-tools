## 1. Имена в данных

- [ ] 1.1 `pipeline/extract_vanilla.py`: имя машины читается из английской локали игры по
      номеру (`engine_id`) от первой строки блока `STR_VEHICLE_NAME_TRAIN_*`, суффикс тяги не
      срезается. Проверка: в `vanilla_trains.json` машина `vanilla_0` зовётся
      `Kirby Paul Tank (Steam)`, а номер 54 — первым монорельсовым движком.
- [ ] 1.2 Экстрактор падает, если строк имён в локали не столько, сколько машин в таблице.
      Проверка: тест пайплайна на укороченном списке строк — сборка кончается ошибкой, а не
      сдвинутыми именами.
- [ ] 1.3 Поправить всё, что знает имена наизусть: `pipeline/validate.py:153` (искать по `id`),
      `web/src/__tests__/vanilla.test.ts`, `web/src/engine/__tests__/engine.test.ts`,
      `web/src/features/kit/fixtures.ts`. Проверка: `make data` и `npx vitest run` зелёные.

## 2. Словарь русских имён

- [ ] 2.1 Генератор словарей (`pipeline/extract_firs_ru.py`) пишет
      `web/src/i18n/vehicles.ru.json`: ключ — `id` машины, значение — строка русской локали
      игры по тому же номеру. Проверка: `vanilla_0` → «Паровоз Kirby Paul Tank»; `make
      check-i18n` ловит ручную правку файла (он гоняет экстрактор с `--check` целиком).
- [ ] 2.2 Тест локалей падает, если в данных есть ванильная машина без записи в словаре, и
      **не** падает на машине, которой игра не дала русского имени (`Wizzowow Z99`, номер 56).
      Проверка: кейс в `web/src/i18n/__tests__/locales.test.ts`; заодно поправить там
      комментарий у `kit.sampleValue`, обещающий, что имена машин не переводятся.

## 3. Имя в интерфейсе

- [ ] 3.1 `web/src/i18n/names.ts`: хелпер имени машины — словарь по id, локаль аргументом,
      откат на `train.name`. Проверка: юнит-тест на ванильную машину в обеих локалях, на
      машину Iron Horse и на `Wizzowow Z99` (английское имя в обоих языках).
- [ ] 3.2 Имя показывается через хелпер везде, где калькулятор называет машину:
      `components/format.ts`, `components/VehicleName.tsx` (проп расширить с `{ name }` до
      машины с `id` — правятся `features/kit/fixtures.ts` и
      `features/savegame/ConsistLine.tsx`), `features/savegame/labels.ts`,
      `features/optimizer/doubtful.ts`, `features/optimizer/OptimizerPage.tsx` (фильтр
      сомнительных машин), `features/network/CorridorUpgrade.tsx`,
      `features/network/NetworkPage.tsx`, `components/StrandedVehicles.tsx`. Проверка: тесты
      этих мест зелёные, на русском языке машина всюду зовётся по-русски.
- [ ] 3.3 Поиск по подстроке (`matchesTrainName()` в `names.ts`) ищет по отображаемому имени.
      Проверка: тест — на русском языке запрос «Паровоз» находит ванильные паровозы.
- [ ] 3.4 Сортировки по имени (`features/optimizer/sorting.ts`, `features/consist/sorting.ts`)
      читают отображаемое имя. Проверка: тест — при русском языке порядок следует русским
      именам.
- [ ] 3.5 Ключ группировки покупок (`engine/purchase.ts`) продолжает брать имя из данных.
      Проверка: тест — при смене языка набор пунктов каталога не меняется.

## 4. Проверка вида

- [ ] 4.1 Посмотреть каталог и выдачу в браузере **глазами** в обеих локалях: имя с суффиксом
      и русское имя не ломают колонку (`make check-visual` про ширину ничего не утверждает).
      Проверка: снимки обеих локалей приложены к отчёту.

## 5. Документация и гейты

- [ ] 5.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, заголовок `### Added` (новый словарь
      имён — набор данных, разряд minor). Проверка: `bash scripts/next-version.sh` даёт minor.
- [ ] 5.2 Отметить задачу сделанной в `.scratch/vanilla-vehicle-names/spec.md`.
- [ ] 5.3 Прогнать `make data`, `make verify` и `cd web && npm run lint`. Проверка: все
      зелёные, перегенерированные данные закоммичены.
