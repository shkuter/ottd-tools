## 1. Замер до правки

- [ ] 1.1 Снять эталон выдачи на текущем коде: одноразовый прогон (скрипт держать в
      `.scratch/fix-short-consist-sweep/`, в репозиторий не класть) по набору задач — груз ×
      длина станции × дистанция × выпуск, порядка 900 сочетаний — для всех четырёх целей,
      снимок верхних строк в файл. Проверка: файл со снимком есть и содержит строки по каждой
      из целей «Прибыль», «Вывоз», «Снабжение», «Мин. расходы».
- [ ] 1.2 Тем же прогоном найти на **текущих данных** (Iron Horse 4.31.0) пару, которая
      воспроизводит дефект: состав во всю станцию не едет, укороченный едет. Пример из
      `proposal.md` снят до бампа данных, и id вагона в нём уже нет. Проверка: пара и обе
      длины записаны, `proposal.md` и задача 2.2 названы её настоящими id.

## 2. Правка перебора

- [ ] 2.1 Снять `if (!full.length) continue` в `web/src/engine/optimize.ts`: негодный состав
      на полную длину больше не заканчивает пару, свип укороченных вариантов идёт при любой
      цели. Проверка: тест на паре из 1.2 — при полной длине строк нет, при короткой строка
      есть и попадает в выдачу.
- [ ] 2.2 Тот же тест прогнать по всем четырём целям: короткий вариант доходит до выдачи и при
      «Прибыли», где свип раньше выключался молча. Проверка: кейс на каждую цель. Тест писать
      **по-английски** — каталог `web/src/engine/__tests__/` разноязычный, и по CLAUDE.md
      новый файл в нём идёт на английском.
- [ ] 2.3 Обновить комментарий на месте правки — он сейчас описывает дефект и обещает «change
      of its own». Проверка: в `optimize.ts` не осталось упоминаний отложенного дефекта.

## 3. Существующие тесты и замер после

- [ ] 3.1 Прогнать `cd web && npx vitest run` и разобрать каждое падение поимённо: под удар
      попадают тесты, дёргающие оптимизатор (`engine/__tests__/optimize-cheapest`,
      `optimize-branches`, `tabs-agree`, `lists-agree`, `settings-effect`,
      `wagon-speed-limits`, `features/optimizer/__tests__/*`). Эталон править только там, где
      новое число объясняется появившимся коротким составом; расхождение, которому объяснения
      нет, — стоп и разбор. Проверка: каждое исправленное ожидание объяснено в сообщении
      коммита или в комментарии теста.
- [ ] 3.2 Повторить прогон 1.1 и посчитать расхождения: сколько строк разошлось по каждой из
      четырёх целей и менялись ли **верхние** строки выдачи. Заодно отметить время прогона —
      свип стал шире, и заметное замедление означает, что пар с нетронувшимся полным составом
      больше, чем думалось. Проверка: числа записаны в `.scratch/fix-short-consist-sweep/`.

## 4. Документация

- [ ] 4.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском, с пометкой
      `**BREAKING**` и числами расхождений из 3.2. Проверка: запись на месте, пометка стоит.
- [ ] 4.2 Снять дефект из раздела «Найденные попутно дефекты» в
      `.scratch/feature-backlog/plan.md`, отметив, каким change закрыт. Проверка: раздел
      больше не обещает эту правку.

## 5. Проверка

- [ ] 5.1 Прогнать `make test` и `make verify` целиком. Проверка: обе команды зелёные.
