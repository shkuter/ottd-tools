# xussr-dataset — tasks

## 1. Инфраструктура и извлечение

- [x] 1.1 Makefile: `XUSSR_REF` (хеш `100716f9…`), цель `fetch-xussr` (shallow-клон в
  `vendor/xussrset`), ревизия в `data/meta.json`; проверить `make fetch-xussr` и что
  повторный запуск пропускает существующий клон
- [x] 1.2 Каркас `pipeline/extract_xussr.py`: препроцессор `cc -C -E -P -x c` → `.nml` →
  `NMLParser`; проверка наличия компилятора с внятной ошибкой; проверить разбор всех
  корневых `.pnml` без падений и счётчик Item по каждому GRF
- [x] 1.3 Свёртка константных выражений AST в числа; валидатор «каждый Item либо извлечён,
  либо поимённо в списке осознанно пропущенных» — проверить, что список пуст или объяснён
- [x] 1.4 Извлечение машин: id `xussr_<имя item>`, свойства (мощность, вес, ТУ, скорость,
  cost_factor, длина, даты, dual/articulated); тест уникальности id по всем GRF и против
  Iron Horse/ванили в `pipeline/tests/`
- [x] 1.5 Мощность/скорость по родам тока из веток `is_ER*_ds` → `power_by_source` и
  `speed_by_source` (общий обход `by_source`; каллбек скорости прогоняется пустым —
  часть машин отдаёт под грузом другое число); проверить на двухсистемнике (разные
  числа на 25 кВ и 3 кВ) юнит-тестом пайплайна
- [x] 1.6 Расход «только построен»: сумма шести статей из switch'а running cost; неконстанта
  в ветке — падение валидатора; сверить 2–3 машины с меню покупки
- [x] 1.7 Вместимость по грузу: параметры вагона + таблицы `cargoes-capacity-*.pnml` →
  `capacity_by_cargo` списком по секциям (деление формулы посекционное, метки рефита — у
  каждой секции свои); тест «уголь ≠ кокс у одного полувагона» с числами из формулы набора,
  тест суммы секций и тест почтового TGV (голова возит пассажиров, секции — почту)
- [x] 1.8 Таблица путей из `xussr-rails.pnml`: лейбл, лимит, powered/compatible
  (нормализация как в остальных наборах), hidden, catenary, `power_source`; тест: RLA1 —
  100 км/ч, ванильные RAIL/ELRL скрыты, у ERA*/ERD* разные источники
- [x] 1.9 Basecost набора (`build_engine 1`, `build_wagon 3`, running 0 при дефолтном
  `new_costs`) и параметры GRF по умолчанию → `meta`; проверить содержимое `meta` тестом
- [x] 1.10 Цель `data-xussr` в `make data` + `validate.py`; проверить `make data`
  идемпотентен (повторный прогон не меняет JSON)

## 2. Спрайты и имена

- [x] 2.1 Нарезка buy-menu-спрайтов из png по `purchase_sprites(name, x, y)` →
  `web/public/icons/xussr/`; проверить выборочно 5 машин глазами против меню покупки
- [x] 2.2 Английские имена из `lang/english.lng` — то, что машина носит **в игре**: набор
  переобъявляет её длинным именем при `enable_long_names` (дефолт 1), и `merged_items`
  берёт позднее объявление, как игра; тест: имя `electric_fp` = «Fᴾ "French" for
  passengers (Electric)», а не короткая метка «Fᴾ»
- [x] 2.3 Русский словарь `trains_xussr.ru.json` из `lang/russian.lng` (шаг в `make data`);
  подстановка в `i18n/names.ts`; `locales.test.ts` и `make check-i18n` проходят и ловят
  машину без перевода

## 3. Схема данных и выбор набора

- [x] 3.1 `types.ts`: `Railtype.power_source`, `Train.capacity_by_cargo`,
  `Train.speed_by_source`, `basecost_shifts.running_electric` и `running_roadveh`; `extract_iron_horse.py` пишет новые поля (OHLE/METRO,
  null, 0), а ванильные шифты живут не в JSON, а литералом `vanillaTrainsMeta` в
  `dataset.ts` — `running_electric: 0` дописывается туда; `make data` + существующие тесты
  зелёные
- [x] 3.2 `GameSettings.trainSet` вместо `ironHorse`: тип, дефолт, `settingsStore`-миграция
  лесенкой; тесты на состояние каждой прошлой версии стора и «живой стор» после миграции
- [x] 3.3 Перевести все упоминания `ironHorse` (123 в 35 файлах, из них 24 тестовых) на
  `trainSet`; `dataset.ts`: третья ветка в `activeTrains`/`activeRailtypes`/
  `activeTrainsMeta`/`inActiveSet`/`canCarryIn` и в `datasetMeta`; не забыть читателей вне
  очевидных мест — `features/optimizer/doubtful.ts` и собственную копию `SETTINGS_VERSION`
  в `state/upgrade.ts`; `grep -rn "ironHorse" web/src` пуст, `npx vitest run` зелёный
- [x] 3.4 Кейс `trainSet` в `settings-effect.test.ts` (своя база, чтобы ловился именно он);
  тест падает при мёртвом переключателе
- [x] 3.5 Селектор набора в Settings: одно поле с тремя значениями в том же разделе
  наборов, FIRS 5 и Base Costs остаются переключателями рядом; вкладка FIRS chains и
  предупреждение инфляции ведут себя по-прежнему при каждом наборе
- [x] 3.6 Строки `en.json`/`ru.json` для селектора набора (названия ростеров не
  переводятся — «Iron Horse», «xUSSR»); `locales.test.ts` зелёный

## 4. Движок

- [x] 4.1 `tracktypes.ts`: выбор источника по `power_source` пути, фолбэк на порядок
  набора; условие catenary-OHLE заменено полем; источники xUSSR дописаны в рукописный
  `POWER_SOURCE_ORDER` (не попавший в список источник молча уходит в порядок ключей JSON);
  тесты: двухсистемник на 25 кВ/3 кВ/без провода, Iron Horse-сценарии не изменились
- [x] 4.2 Аксессор вместимости в `dataset.ts` (правило `capacity_by_cargo` → значение по
  грузу, иначе `capacities[index]`); перевести читателей (`consist.ts`, `trip.ts`,
  `optimize.ts`, `purchase.ts`, каталог, сортировка); тест: сортировка каталога меняется
  со сменой груза
- [x] 4.3 `costs.ts`: шифт по running-классу (steam/diesel/electric); тест: электрокласс
  без шифта набора берёт 0, а не дизельный; эталон электровоза xUSSR против меню покупки
- [x] 4.4 `activeRailtype()`: искать лейбл в `selectableRailtypes`, а не в полной таблице,
  и фолбэк — её первый элемент (иначе дефолтный `'RAIL'` найдётся среди скрытых у xUSSR и
  до фолбэка дело не дойдёт); `TrackTypeField.tsx` опирается на тот же список, второй копии
  правила нет; тесты: сохранённый `'RAIL'` при активном xUSSR даёт видимый путь, смена
  набора с чужим лейблом — тоже

## 5. Импорт сейва

- [x] 5.1 `savegame/registry.ts`: `KnownGrf` получает поле «какой набор машин даёт запись»
  (роли `'trains'` уже недостаточно), записи GRFID xUSSR добавлены, `import.ts` выводит
  `trainSet` из распознанной записи вместо `ironHorse: roles.has('trains')`; тесты на
  распознавание и на «сейв без набора машин даёт ваниль»
- [x] 5.2 Баннер «обнаружены NewGRF …» над списком различий; тест рендера списка
  обнаруженных наборов
- [x] 5.3 Строки `en.json`/`ru.json` для баннера; `locales.test.ts` зелёный
- [x] 5.4 Прогнать импорт эталонного `xUSSR.sav` целиком; проверить предложенные
  настройки (набор, год, экономика) против партии. Сейв годится любой — и сохранённый
  вручную, и автосохранение

## 6. Эталоны и завершение

- [x] 6.1 Эталоны в `test_xussr_extraction.py` (рядом с остальными проверками набора) для
  xUSSR: 10 машин со скриншотов меню покупки партии пользователя — масса, скорость,
  мощность, места; расхождение с пином — зафиксировать решением (двигать пин или
  задокументировать)
- [ ] 6.1b Дополнить эталоны электровозами (ВЛ/ЧС, двухсистемный) и денежными числами —
  цена покупки и годовой расход из меню покупки. Сегодня их в `BUY_MENU` нет: числа
  двухсистемной тяги и скорости сверены с формулой исходника, а не с игрой, а £304 и £6187
  в `engine.test.ts` выведены по формуле игры вручную. Нужны скриншоты партии
- [x] 6.1a Проверить, влияет ли `enable_new_ratios` (у пользователя = 1, дефолт набора = 0)
  на извлекаемые числа машин, а не только на рейтинг станций — эталоны 6.1 снимаются именно
  с его партии, а данные извлекаются при дефолтных параметрах. Не влияет — записать проверку
  в тест пайплайна, чтобы вывод не пришлось делать заново. Влияет — эталоны снимать с партии
  с дефолтным значением параметра (данные остаются дефолтными: правило «данные извлечены при
  значениях по умолчанию» из спека не нарушается), а сам факт влияния записать в ограничения
  README и в `meta`
- [x] 6.2 README.md + README.ru.md одним коммитом: xUSSR в источниках данных, ограничения
  (без Subways/Ivolga, без возраста и скорости-от-загрузки, составность — в следующем
  change); структура разделов совпадает
- [x] 6.3 CHANGELOG.md `[Unreleased]`: запись на английском с пометкой **BREAKING**
  (миграция persist) и `### Added` (набор xUSSR)
- [x] 6.4 `make data && make test && make verify` — всё зелёное, `make check-visual`
  проходит на вкладках с новым набором
