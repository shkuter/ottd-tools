## 1. Переезд CSS: один файл на правило

Вид на этих шагах не меняется. Проверка каждого — `make check-visual` и `npx vitest run`
проходят **без правки ожиданий**; если проверка потребовала правки, значит переезд что-то
задел, и это разбирается, а не подгоняется.

- [ ] 1.1 Снять дубли внутри `skin.css`: `.big`, `body`, `.grf-error`, `.app-header h1`
      объявлены дважды — свести каждый в одно объявление, проверить `make check-visual`
- [ ] 1.2 Перенести правила оболочки (`body`, `.app`, `.app-header`, `.app-footer`, `nav`,
      `main`, заголовки, `.hint`, `.dim`) из `index.css` в `skin.css`, проверить
      `make check-visual`
- [ ] 1.3 Перенести правила вкладки настроек (`.page-settings`, `.settings-group`,
      `.setting-row*`, `.setting-label`, `.setting-hint`, `.btn-danger`, `.group-toggle`),
      проверить `make check-visual`
- [ ] 1.4 Перенести правила подбора и каталога (`.page-consist`, `.filters`, `.goal-field`,
      `.table-more`, `.train-sprite`, `.cargo-icon`, `.branch-mark*`, `.btn-add`,
      `.consist-*`), проверить `make check-visual`
- [ ] 1.5 Перенести правила дохода рейса и цепочек (`.page-route`, `.route-controls`,
      `.route-profit`, `.firs-layout`, `.graph-container`, `.firs-side`, `.node-card`),
      проверить `make check-visual`
- [ ] 1.6 Перенести остаток (`.warning*`, `.grf-error`, `.summary-table`, `.stats-wide`,
      `.big`, `.profit`, `.loss`, `.money-neg`, `.supply-*`, `.page-kit`, медиазапросы),
      проверить `make check-visual`
- [ ] 1.7 Удалить `index.css` и его импорт из `main.tsx`, обновить комментарий в `layers.css`
      (переезд закончен), проверить `make check-visual` и `npx vitest run`
- [ ] 1.8 Перевести на английский комментарии, приехавшие из `index.css` русскими (спрайт 4x,
      сводки «подпись — значение», контролы дохода, фильтры цели), проверить `npx oxlint`

## 2. Общие формы: поле, заголовок, пустое состояние, стрелка

- [ ] 2.1 Завести токены шкалы ширин полей (`--skin-field-narrow` / `-normal` / `-wide`) в блоке
      токенов `skin.css`, проверить, что `skin-palette.test.ts` проходит
- [ ] 2.2 Создать компонент поля ряда (подпись сверху, контрол под ней, ширина из шкалы) с
      юнит-тестом на то, что подпись связана с контролом
- [ ] 2.3 Применить его на вкладке снабжения — ряд там уже однороден, поэтому проверка:
      `make check-visual` не показывает изменений вертикальных позиций
- [ ] 2.4 Применить на вкладке подбора: `Goal`, `Trains at most`, оба переключателя и поле
      фильтра по движку встают в общую схему; добавить проверку в `visual`, что подписи ряда
      стоят на одной линии и контролы — на одной
- [ ] 2.5 Применить на вкладке каталога, вернув подписи трём спискам без них; добавить ключи в
      `en.json` / `ru.json`, проверить `i18n/__tests__/locales.test.ts`
- [ ] 2.6 Вынести подсказку про цель «Вывоз» (`.goal-hint`) из ряда отдельной строкой под ним,
      к строке сводки расчёта: во всю ширину она укладывается в одну строку вместо двух;
      `visual`-утверждение, что при её появлении и исчезновении поля ряда не смещаются
- [ ] 2.7 Добавить `SPR_IMG_SUBSIDIES` (679) и `SPR_BUILD_NS_ELRAIL` (`SPR_ELRAIL_BASE + 36`) в
      экстрактор ванильной графики, прогнать `make data-opengfx2` и проверить, что PNG легли в
      `web/public/icons/` и открываются
- [ ] 2.8 Заменить подписи переключателей электрификации и субсидии на эти значки с подсказкой
      (`Tooltip`) и `aria-label` с тем же текстом; тест на то, что переключатель находится по
      названию (`getByRole('switch', { name })`)
- [ ] 2.9 `visual`-утверждение, что значки нарисованы цветами палитры и стоят на одной линии с
      остальными контролами ряда
- [ ] 2.10 Вынести заголовок страницы в общую форму и дать вкладке дохода рейса собственный
      заголовок; проверка — `visual`-утверждение, что заголовок на всех вкладках имеет
      одинаковые отступы сверху и слева
- [ ] 2.11 Пустое состояние: вкладка снабжения до выбора предприятия показывает ту же рамку, что
      списки; ключи в оба словаря, проверить `locales.test.ts` и `visual`
- [ ] 2.12 Завести одну фигуру стрелки (CSS, `clip-path`, размер и направление модификаторами)
      и применить в пометке сортировки, стрелке списка, стрелках счётчика и полосе прокрутки;
      проверка — `visual`-утверждение, что все четыре имеют одну форму и цвет из палитры
- [ ] 2.13 Убрать глиф `▴`/`▾` из `SortableTh.tsx` и SVG-шеврон Mantine, проверить, что тесты
      сортировки в `components/table/__tests__` проходят

## 3. Списки

- [ ] 3.1 Добавить в описание колонки признаки `numeric` и `unit`; юнит-тест на то, что
      заголовок собирается с единицей, а ячейка — без неё
- [ ] 3.2 Разметить колонки подбора, каталога, снабжения и партии: числовые — вправо,
      текстовые — влево, денежные — как были; `visual`-утверждение на выравнивание
- [ ] 3.3 Убрать единицы из форматтеров ячеек (`km/h`, `days`, `passengers`, `years`), проверить
      `npx vitest run` и что `display-units` продолжает показывать выбранную систему
- [ ] 3.4 Увеличить горизонтальные отступы ячеек (сейчас 3px) до метрики игры для зазора между
      колонками списка, задав их от `--skin-scale`; `visual`-утверждение на итоговый отступ
- [ ] 3.5 Прижать кнопку действия к правому краю её колонки во всех трёх списках с кнопками
      (подбор, каталог, маршруты партии); `visual`-утверждение, что зазор справа от кнопки
      равен отступу ячейки, а не остатку ширины колонки
- [ ] 3.6 Убрать пробелы вокруг слеша в значениях: скорость под грузом/порожняком
      (`OptimizerPage.tsx:574`), рейтинг `total / 255` (`:78`), пороги в подсказке (`:278`),
      штраф за время в пути (`RoutePage.tsx:182`); проверка — юнит-тест на формат пары
- [ ] 3.7 Привести слеш в словарях к одному виду — 10 строк с пробелами в `en.json` и столько
      же в `ru.json` (`combined.*`, `supply.*`, `consist.stats.*`, `settings.costsWhenStoppedHint`);
      `units.mph` и `settings.speedUnit.imperial` не трогать. Проверка — `locales.test.ts` и
      grep, что ` / ` в словарях не осталось
- [ ] 3.8 Свести пять пар подписей, называющих одну величину по-разному (`opt.trips` vs
      `combined.tripsPerYear`, `opt.incomeTrip` vs `combined.incomePerTrip`, `opt.profitYear` vs
      `combined.profitPerYear`, `table.running` vs `combined.runningCost`, `route.production` vs
      `supply.production`): один способ записи, сокращение — только по ширине колонки
- [ ] 3.9 Дать удерживаемым колонкам видимый край и добавить распорку в конец строки;
      `visual`-утверждение, что при прокрутке до упора последнее значение видно целиком
- [ ] 3.10 Выровнять начало названия машины в ячейке со спрайтом (одинаковое место под спрайт);
      `visual`-утверждение, что названия строк начинаются с одной вертикали

## 4. Скин

- [ ] 4.1 Назначить `brown` вкладке цепочек и `dark-green` вкладке снабжения в `tabs.ts`;
      проверка — `visual/routes.ts` подхватывает их сам, `make check-visual` проходит
- [ ] 4.2 Добавить в `visual` утверждение, что каждая группа из `WINDOW_COLOURS` закреплена
      хотя бы за одной вкладкой (иначе мёртвая тема заведётся снова)
- [ ] 4.3 Заменить штриховку недоступности на стрелках счётчика приёмом, соразмерным их
      размеру; `visual`-утверждение на цвет из палитры
- [ ] 4.4 Убрать выступ ползунка переключателя за дорожку (сейчас 34px в 32px);
      `visual`-утверждение, что ползунок внутри габарита
- [ ] 4.5 Подобрать различимые оттенки палитры недоступному переключателю и кнопке-«subtle» в
      группах `mauve`, `brown`, `dark-green`; проверить на `/kit` и `visual`-утверждением
      (контраст приглушённого текста при этом НЕ трогаем — это non-goal)

## 5. Отдельные места

- [ ] 5.1 Перерисовать график дохода по образцу игрового: тёмное вдавленное поле, сплошная
      сетка по обеим осям, числа на оси без знака валюты, толстая линия, квадратные точки,
      цвет из палитры, легенда сбоку; `visual`-утверждения на фон поля, цвет линии и на то,
      что верхняя метка оси ничем не перекрыта
- [ ] 5.2 Вынести знак валюты и единицу времени из меток осей в подпись оси или заголовок
      графика; ключи в оба словаря, проверить `locales.test.ts`
- [ ] 5.3 Задать графу цепочек цвета палитры атрибутами dot и шрифт скина через CSS, убрать
      белый фон, поставить контейнер в рамку панели и убрать пустое поле над графом;
      `visual`-утверждение на фон и цвет узла
- [ ] 5.4 Снять из `visual/exemptions.ts` записи, которые после 5.3 прикрывают уже
      исправленное; проверка — `make check-visual` проходит без них
- [ ] 5.5 Разрешить перенос текста в кнопках и подписях боковой панели дохода рейса, чтобы
      содержимое не выходило за рамку (сейчас кнопка 324px в панели 320px);
      `visual`-утверждение, что ни один элемент панели не выходит за её правый край — и на
      английской, и на русской локали
- [ ] 5.6 Добавить разделители во вторую строку подвала; `visual`-утверждение, что между
      соседними ссылками есть видимый разделитель
- [ ] 5.7 Добавить маршрут-заглушку (`path="*"`) на главную вкладку; тест на то, что неизвестный
      путь ведёт на `/optimizer`

## 6. Завершение

- [ ] 6.1 Пройти вкладки в браузере на русской локали и посмотреть, не разъехались ли ряды
      контролов и ширины колонок с более длинными подписями; найденное чинить здесь же
- [ ] 6.2 Записать изменения в `CHANGELOG.md` в секцию `[Unreleased]` (на английском)
- [ ] 6.3 Прогнать `make test` и `make verify` (сборка, oxlint, vitest, тесты пайплайна, `check-i18n`,
      `check-visual`) и убедиться, что всё зелёное
