## 1. Переезд CSS: один файл на правило

Вид на этих шагах не меняется. Проверка каждого — `make check-visual` и `npx vitest run`
проходят **без правки ожиданий**; если проверка потребовала правки, значит переезд что-то
задел, и это разбирается, а не подгоняется.

- [x] 1.1 Выбросить из `index.css` правила, которые скин уже перекрывает целиком и которым
      незачем переезжать: `.btn-danger`, `.btn-danger:hover`, `.grf-error`, `font-size` у `h2`.
      (Повторы внутри самого `skin.css` дублями не оказались — там тематические секции, цвет
      в секции цветов и шрифт в секции шрифтов, свойства нигде не пересекаются.)
      Проверить `make check-visual`
- [x] 1.2 Перенести правила оболочки (`body`, `.app`, `.app-header`, `.app-footer`, `nav`,
      `main`, заголовки, `.hint`, `.dim`) из `index.css` в `skin.css`, проверить
      `make check-visual`
- [x] 1.3 Перенести правила вкладки настроек (`.page-settings`, `.settings-group`,
      `.setting-row*`, `.setting-label`, `.setting-hint`, `.btn-danger`, `.group-toggle`),
      проверить `make check-visual`
- [x] 1.4 Перенести правила подбора и каталога (`.page-consist`, `.filters`, `.goal-field`,
      `.table-more`, `.train-sprite`, `.cargo-icon`, `.branch-mark*`, `.btn-add`,
      `.consist-*`), проверить `make check-visual`
- [x] 1.5 Перенести правила дохода рейса и цепочек (`.page-route`, `.route-controls`,
      `.route-profit`, `.firs-layout`, `.graph-container`, `.firs-side`, `.node-card`),
      проверить `make check-visual`
- [x] 1.6 Перенести остаток (`.warning*`, `.grf-error`, `.summary-table`, `.stats-wide`,
      `.big`, `.profit`, `.loss`, `.money-neg`, `.supply-*`, `.page-kit`, медиазапросы),
      проверить `make check-visual`
- [x] 1.7 Удалить `index.css` и его импорт из `main.tsx`, обновить комментарий в `layers.css`
      (переезд закончен), проверить `make check-visual` и `npx vitest run`
- [x] 1.8 Перевести на английский комментарии, приехавшие из `index.css` русскими (спрайт 4x,
      сводки «подпись — значение», контролы дохода, фильтры цели), проверить `npx oxlint`

## 2. Общие формы: поле, заголовок, пустое состояние, стрелка

- [x] 2.1 Завести токены шкалы ширин полей (`--skin-field-narrow` / `-normal` / `-wide`) в блоке
      токенов `skin.css`, проверить, что `skin-palette.test.ts` проходит
- [x] 2.2 Создать компонент поля ряда (подпись сверху, контрол под ней, ширина из шкалы) с
      юнит-тестом на то, что подпись связана с контролом
- [x] 2.3 Применить его на вкладке снабжения — ряд там уже однороден, поэтому проверка:
      `make check-visual` не показывает изменений вертикальных позиций
- [x] 2.4 Вынести единицы измерения из подписей в сами поля (`suffix`): восемь подписей
      несли единицу в скобках, и она делала подпись шире поля. Ключи `units.tiles`,
      `units.days`, `units.perMonth` в оба словаря; `narrow` подвинут до 12ch. Проверка —
      `locales.test.ts`, `make check-i18n` и что `onChange` по-прежнему отдаёт число
- [x] 2.5 Применить на вкладке подбора: `Goal`, `Trains at most`, оба переключателя и поле
      фильтра по движку встают в общую схему; добавить проверку в `visual`, что подписи ряда
      стоят на одной линии и контролы — на одной
- [x] 2.6 Применить на вкладке каталога, вернув подписи трём спискам без них; добавить ключи в
      `en.json` / `ru.json`, проверить `i18n/__tests__/locales.test.ts`
- [x] 2.7 Вынести подсказку про цель «Вывоз» (`.goal-hint`) из ряда отдельной строкой под ним,
      к строке сводки расчёта: во всю ширину она укладывается в одну строку вместо двух;
      `visual`-утверждение, что при её появлении и исчезновении поля ряда не смещаются
- [x] 2.8 Добавить значки в экстрактор ванильной графики и прогнать `make data-opengfx2`.
      `SPR_IMG_SUBSIDIES` (679) берётся по номеру из базового набора. Кнопки электрифицированного
      пути там нет вовсе — она в наборе-надстройке, который грузится как NewGRF, поэтому
      адресуется парой «блок Action 5 + смещение», а смещение считается как
      `SPR_BUILD_EW_ELRAIL − SPR_ELRAIL_BASE` (взят вид сбоку: он читается в строке лучше
      вертикального `NS`). Проверить, что PNG легли в `web/public/icons/` и открываются
- [x] 2.9 Заменить подписи переключателей электрификации и субсидии на эти значки с подсказкой
      (`Tooltip`) и `aria-label` с тем же текстом; тест на то, что переключатель находится по
      названию (`getByRole('switch', { name })`)
- [x] 2.10 `visual`-утверждение, что значки нарисованы цветами палитры и стоят на одной линии с
      остальными контролами ряда
- [x] 2.11 Вынести заголовок страницы в общую форму и дать вкладке дохода рейса собственный
      заголовок; проверка — `visual`-утверждение, что заголовок на всех вкладках имеет
      одинаковые отступы сверху и слева
- [x] 2.12 Пустое состояние: вкладка снабжения до выбора предприятия показывает ту же рамку, что
      списки; ключи в оба словаря, проверить `locales.test.ts` и `visual`
- [x] 2.13 Стрелка одна и буквально одного размера везде. Одного размера боксов оказалось
      мало: библиотека рисует у списка двойной шеврон, у счётчика одинарный, а нарисованный
      в CSS — третью фигуру. Поэтому общий не бокс, а сам рисунок: одинарный шеврон вынесен
      в токен-маску `--skin-arrow-shape`, размер — `--skin-arrow` (10px, столько места внутри
      кнопки счётчика), направление — поворотом. `visual`-утверждение сверяет, что маска и
      размер у всех стрелок одни
- [x] 2.14 Подчеркнуть ссылки — и голые `a`, и `Anchor` библиотеки; подчёркивание цвета самой
      ссылки, тень по тем же правилам. `visual`-утверждение, что у ссылки есть
      `text-decoration-line: underline` и цвет линии совпадает с цветом текста
- [x] 2.15 Убрать глиф `▴`/`▾` из `SortableTh.tsx` и SVG-шеврон Mantine, проверить, что тесты
      сортировки в `components/table/__tests__` проходят

- [x] 2.16 Свести отступы внутри переключателя к метрике игры: у Mantine их два — `gap` тела
      и `padding` подписи, вместе больше, чем ряд оставляет между целыми полями. Токен
      `--skin-hsep-normal` (hsep_normal 2), ряд получает `--skin-hsep-wide` (6).
      Проверка — зазор внутри виджета меньше зазора между виджетами

## 3. Списки

- [x] 3.1 Добавить в описание колонки признаки `numeric` и `unit`; юнит-тест на то, что
      заголовок собирается с единицей, а ячейка — без неё
- [x] 3.2 Разметить колонки подбора, каталога, снабжения и партии: числовые — вправо,
      текстовые — влево, денежные — как были; `visual`-утверждение на выравнивание
- [x] 3.3 Убрать единицы из форматтеров ячеек (`km/h`, `days`, `passengers`, `years`), проверить
      `npx vitest run` и что `display-units` продолжает показывать выбранную систему
- [x] 3.4 Увеличить горизонтальные отступы ячеек (сейчас 3px) до метрики игры для зазора между
      колонками списка, задав их от `--skin-scale`; `visual`-утверждение на итоговый отступ
- [x] 3.5 Прижать кнопку действия к правому краю её колонки во всех трёх списках с кнопками
      (подбор, каталог, маршруты партии); `visual`-утверждение, что зазор справа от кнопки
      равен отступу ячейки, а не остатку ширины колонки
- [x] 3.6 Убрать пробелы вокруг слеша в значениях: скорость под грузом/порожняком
      (`OptimizerPage.tsx:574`), рейтинг `total / 255` (`:78`), пороги в подсказке (`:278`),
      штраф за время в пути (`RoutePage.tsx:182`); проверка — юнит-тест на формат пары
- [x] 3.7 Привести слеш в словарях к одному виду — 10 строк с пробелами в `en.json` и столько
      же в `ru.json` (`combined.*`, `supply.*`, `consist.stats.*`, `settings.costsWhenStoppedHint`);
      `units.mph` и `settings.speedUnit.imperial` не трогать. Проверка — `locales.test.ts` и
      grep, что ` / ` в словарях не осталось
- [x] 3.8 Свести пять пар подписей, называющих одну величину по-разному (`opt.trips` vs
      `combined.tripsPerYear`, `opt.incomeTrip` vs `combined.incomePerTrip`, `opt.profitYear` vs
      `combined.profitPerYear`, `table.running` vs `combined.runningCost`, `route.production` vs
      `supply.production`): один способ записи, сокращение — только по ширине колонки
- [x] 3.9 Дать удерживаемым колонкам видимый край. Распорка не понадобилась: при прокрутке
      до упора последняя колонка выходит из-под удерживаемой сама (замер — перекрытие 0)
- [x] 3.10 Выровнять начало названия машины в ячейке со спрайтом (одинаковое место под спрайт);
      `visual`-утверждение, что названия строк начинаются с одной вертикали

## 4. Скин

- [x] 4.0a Добавить `visual`-утверждение, что начертание на странице одно: у кнопок, их
      подписей, заголовков таблицы и полей `font-weight` равен 400. Требование «Начертание
      SHALL быть одно» в спеке есть, а проверки нет — жирный на кнопках вернулся незамеченным
      и держался, пока его не увидел человек
- [x] 4.0b Вписать в поле его собственные части: стрелки счётчика Mantine размерит по своей
      шкале, и обойма стояла на 34px против 28px у поля, свисая с обеих граней. Проверка
      мерит, а не перечисляет: `visual`-утверждение обходит потомков каждого контрола и
      падает, если хоть один выходит за его края (проверено внесением поломки — падает на
      пяти вкладках)
- [x] 4.0 Свести высоты элементов управления к одной метрике: сейчас поле 36px, группа кнопок
      31.25px, кнопка-значок 28px, кнопка вкладки 30px, переключатель 18px — пять высот из
      размерной шкалы Mantine, тогда как требование «Интерфейс выглядит как окно OpenTTD»
      предписывает брать высоты из метрик игры, умноженных на `--skin-scale`.
      `visual`-утверждение на одинаковую высоту контролов ряда

- [x] 4.1 Назначить `brown` вкладке цепочек и `dark-green` вкладке снабжения в `tabs.ts`;
      проверка — `visual/routes.ts` подхватывает их сам, `make check-visual` проходит
- [x] 4.2 Добавить в `visual` утверждение, что каждая группа из `WINDOW_COLOURS` закреплена
      хотя бы за одной вкладкой (иначе мёртвая тема заведётся снова)
- [x] 4.3 Заменить штриховку недоступности на стрелках счётчика приёмом, соразмерным их
      размеру; `visual`-утверждение на цвет из палитры
- [x] 4.4 Выступа ползунка за дорожку больше нет: замер даёт 0 со всех сторон — ушёл вместе
      с правкой отступов переключателя (2.16)
- [x] 4.5 Замер по всем четырём группам: недоступный переключатель читается везде
      (контраст 4.47…5.5). Кнопка-«subtle» стоит на 1.97…3.06 — но это ссылка по виду, и
      её распознаваемость решена подчёркиванием (2.14), а не подъёмом контраста, который
      в non-goals

## 5. Отдельные места

- [x] 5.1 Перерисовать график дохода по образцу игрового: тёмное вдавленное поле, сплошная
      сетка по обеим осям, числа на оси без знака валюты, толстая линия (отметок в точках нет —
      кривая считается по дню, их там сотни), цвет из палитры, легенда сбоку; `visual`-утверждения на фон поля, цвет линии и на то,
      что верхняя метка оси ничем не перекрыта
- [x] 5.2 Вынести знак валюты и единицу времени из меток осей в подпись оси или заголовок
      графика; ключи в оба словаря, проверить `locales.test.ts`
- [x] 5.3 Задать графу цепочек цвета палитры атрибутами dot и шрифт скина через CSS, убрать
      белый фон, поставить контейнер в рамку панели и убрать пустое поле над графом;
      `visual`-утверждение на фон и цвет узла
- [x] 5.4 Снять из `visual/exemptions.ts` записи, которые после 5.3 прикрывают уже
      исправленное; проверка — `make check-visual` проходит без них
- [x] 5.5 Разрешить перенос текста в кнопках и подписях боковой панели дохода рейса, чтобы
      содержимое не выходило за рамку (сейчас кнопка 324px в панели 320px);
      `visual`-утверждение, что ни один элемент панели не выходит за её правый край — и на
      английской, и на русской локали
- [x] 5.6 Разделители в подвале уже стояли (запятые, тире, точка) — терялись они потому, что
      ссылки не отличались от текста; видны с подчёркиванием (2.14). Прежняя формулировка:
      добавить разделители во вторую строку подвала; `visual`-утверждение, что между
      соседними ссылками есть видимый разделитель
- [x] 5.7 Добавить маршрут-заглушку (`path="*"`) на главную вкладку; тест на то, что неизвестный
      путь ведёт на `/optimizer`

## 6. Завершение

- [x] 6.1 Пройдено на русской локали: ряды контролов ровные на всех вкладках. Найдено и
      починено — широкий каталог уносил страницу вбок, потому что колонка грида не сжимается
      меньше своего содержимого без `min-width: 0`
- [x] 6.2 Записать изменения в `CHANGELOG.md` в секцию `[Unreleased]` (на английском)
- [x] 6.3 Прогнать `make test` и `make verify` (сборка, oxlint, vitest, тесты пайплайна, `check-i18n`,
      `check-visual`) и убедиться, что всё зелёное

## 7. По итогам ревью

- [x] 7.1 `CHANGELOG.md` приведён к Keep a Changelog: раздела `Internal` в формате нет, и
      `scripts/next-version.sh` его не разбирает — записи ушли в `Changed`, пустые строки
      внутри списков убраны
- [x] 7.2 Дубли в словарях сведены: `units.tiles` и `units.days` повторяли слово в слово
      `consist.stats.tiles` и `combined.days`, и обе пары использовались вперемешку — остались
      `units.*`, вызовы переведены
- [x] 7.3 Склейка суффикса единицы вынесена в `unitSuffix()` рядом с `withUnit()` — она
      повторялась пятью хунками
- [x] 7.4 Ширина поля берётся типизированным `fieldWidth()`, а не сырым `data-width="narrow"`:
      тип прикрывал только половину вызовов
- [x] 7.5 Ширины по шкале проставлены трём полям ряда подбора, которые их не имели (груз,
      принимающее предприятие, фильтр по локомотиву)
- [x] 7.6 Ячейки дистанции и выпуска на вкладке снабжения получили `cell-num` — заголовок был
      выровнен вправо, а поле под ним влево
- [x] 7.7 Ползунок переключателя ограничен габаритом дорожки своим правилом, а не побочным
      эффектом; `.mantine-Switch-track` добавлен в проверку «часть не выходит за контрол»
- [x] 7.8 График получил обозначение линии сбоку (`withLegend`), как требует спека
- [x] 7.9 Мелочи: `--skin-line-height` вместо чужого `--mantine-line-height`, мёртвая ссылка на
      `index.css` в комментарии, черновой абзац в `extract_vanilla.py`, две одинаковые пустые
      рамки на вкладке снабжения сведены в одну, `TABS[0].path` вместо зашитого `/optimizer`,
      «Поездов max» → «Поездов, макс.»
- [x] 7.10 Проверки, обещанные в задачах: ряд контролов (одна линия подписей, одна линия
      контролов, ширины из шкалы), список (числовые колонки вправо, кнопка действия у края,
      имена машин с одной вертикали), подчёркивание по роли. `type.visual.test.ts` разросся
      за пределы своего имени — разделён на `type`, `controls` и `gui-icon`
- [x] 7.11 Спека приведена к решениям, принятым по ходу: стрелка — шеврон библиотеки, а не
      треугольник (сплошной треугольник был отвергнут в работе); отметки на кривой не
      рисуются — их сотни на график по дню; шрифт графа остаётся отрисовщика, потому что он
      меряет по нему габариты узлов; в proposal добавлены плотная раскладка dot и правило
      подчёркивания по роли

## 8. Второй проход ревью

- [x] 8.1 Ширина у поля, собранного `Field`, не работала: атрибут ложится на корень
      `Input.Wrapper`, а правило целило во внутренний `.mantine-Input-wrapper`, которого там
      нет. «Цель» меряли содержимым (255px вместо 189px). Правило ловит оба случая, проверка
      рядов — тоже
- [x] 8.2 Врезка каталога носила кегль заголовка страницы (`order={2}`) — стала `order={3}`,
      как на соседней вкладке
- [x] 8.3 Вкладка партии не имела заголовка вовсе — поймано новой проверкой «страница
      титулована там же, где остальные»
- [x] 8.4 Пустые состояния каталога и дохода рейса встали в ту же рамку, что у списков
- [x] 8.5 Единица года ушла в `units.*` (последний дубль такого рода), подпись выпуска
      источника перестала носить единицу внутри строки — она в заголовке колонки
- [x] 8.6 `Field` писал `data-width` мимо собственного хелпера; геометрия значка задавалась
      пропами `h`/`w` вместо скина — `.gui-icon` описан в `skin.css` от `--skin-font`
- [x] 8.7 Отступы в `RoutePage` после обёртки во фрагмент выправлены, два комментария об одном
      сведены в один
- [x] 8.8 Проверки: заголовок страницы и пустое состояние (`page.visual.test.ts`), тёмное поле
      графика с читаемыми метками и граф без цветов graphviz (`chart.visual.test.ts`)
- [x] 8.9 `goods.visual.test.ts` сравнивал округлённые координаты точно и падал от сдвига в
      пиксель — сверяет расхождение с допуском в пиксель: строка отстоит на 23px, и пиксель
      округления строкой не является

## 9. Третий проход ревью

- [x] 9.1 `CLAUDE.md` разошёлся с кодом: единственным примером «чужого цвета» в нём стоял граф
      цепочек, который этот change как раз перекрасил, а `EXEMPTIONS` обнулил. Стандарт
      переписан: чужой отрисовщик ещё не значит чужой цвет — graphviz пишет свои
      презентационными атрибутами, и они проигрывают любому правилу CSS
- [x] 9.2 Три новые проверки (`rows`, `list`, `link`) передавали тело в `page.evaluate`
      строкой-шаблоном: браузерный код не видел ни tsc, ни линтер, и в нём было два
      обращения к nullable без проверки. Переписаны типизированными функциями, как в остальном
      наборе
- [x] 9.3 Ряды вкладки партии стояли вне общей схемы (`w={260}`, `w={280}` мимо шкалы) и вне
      проверки — получили класс ряда и ширину из шкалы; проверка рядов смотрит все ряды
      вкладки, а не первый
- [x] 9.4 Проверка значков не могла упасть: на пяти маршрутах из семи список пуст, и цикл
      ничего не утверждал — теперь маршруты со значками названы, и отсутствие значка на них
      роняет проверку
- [x] 9.5 Проверка графа сверяла цвета зашитой регуляркой с палитрой из `extract_firs.py` —
      смена заливки в Python оставила бы её зелёной. Сверяется с токенами скина и смотрит
      только на то, что рисует: `<g>` и `<svg>` несут заливку, которой не пишут
- [x] 9.6 Проверки, обещанные задачами: панель держит содержимое (и на русском, где строки
      длиннее), единица в заголовке против единицы в поле, слеш в словарях без пробелов, край
      залипающей колонки и прокрутка до упора
- [x] 9.7 `CHANGELOG.md` получил `### Added`: change отгружает новый набор данных
      (`vanilla_gui.json`), значки и новые ключи, а `scripts/next-version.sh` считал разряд по
      заголовкам и выдавал patch. Теперь 0.14.0
- [x] 9.8 Артефакты догнали код: `design.md` обещал распорку у залипающей колонки (её не
      понадобилось), спека в сценарии обещала отметки на кривой, которые сама же запрещает,
      цвет стрелки описан как следующий месту, а общими названы рисунок и размер

## 10. Четвёртый проход ревью

- [x] 10.1 Выключенная «Снабжение» выглядела включённой: штриховка недоступного пункта задавала
      цвет, но не сам рисунок — `.mantine-SegmentedControl-label[data-disabled]` внесён в
      правило, которое его рисует
- [x] 10.2 Колонка `#` в оптимизаторе и колонки «Поездов» и «Расстояние, тайлов» на вкладке
      партии остались без `cell-num` — числа стояли влево. Проверка списка теперь судит по
      содержимому ячеек, а не по классу, поэтому пропущенную колонку видно
- [x] 10.3 Третье название одной величины: `opt.production` звался «Выпуск, /мес» при
      «Выпуск источника» в подсказке и «Выпуск» в колонке — сведено к «Выпуску источника»
- [x] 10.4 Декодер GRF получил свой набор тестов (`pipeline/tests/test_grf_sprites.py`):
      распаковка прогонов с прозрачными промежутками и отказ на порченых данных, разбор
      цепочки номеров спрайтов. Ими проверено понимание формата, а не факт работы
- [x] 10.5 Проверка, обещанная задачей 2.7: подсказка о цели приходит и уходит, а поля ряда
      стоят на месте. Доказана возвратом подсказки в ряд — падает
- [x] 10.6 Мелочи стандарта: мёртвое `color` в `.filters label` (правило ниже забирало его
      всегда), два правила повторяли выражение токена вместо самого токена, тип имён значков
      выводится из данных, выравнивание в `RoutePage` перешло с пропа `align` на класс,
      кейс края залипающей колонки ходил на одну вкладку из двух, тест маршрута спрашивает
      имя первой вкладки у оболочки
- [x] 10.7 Спека получила сценарий про недоступный пункт в ряду переключателя (находка 10.1
      попадала в требование о недоступных состояниях, но ни один его сценарий её не описывал),
      а набор — проверку под него: недоступная цель несёт штриховку и не читается так же, как
      доступные соседи. Доказана снятием селектора — падает

## 11. Пятый проход ревью

- [x] 11.1 Пагинация была покрашена скином, но осталась вне его геометрии — 32px против 28px у
      всех остальных контролов. Встала в общее правило высоты и в список, который проверка
      сторожит
- [x] 11.2 Слеш с пробелами нашёлся ещё в двух местах, собранных в разметке, а не в словарях:
      «Груз за рейс» на вкладке дохода и вес порожняком/под грузом в каталоге составов.
      Задача 3.6 обещала юнит-тест на формат пары — написан (`__tests__/slashes.test.ts`),
      им обе строки и найдены; самозакрывающийся тег `} />` посимвольно выглядит так же,
      поэтому слеш перед `>` не считается
- [x] 11.3 Числовые колонки трёх подвкладок «Партии» стояли влево («Построен», «Рейтинг»,
      «Вывезено»). Проверка их не видела: подвкладки монтируются только открытыми
      (`keepMounted={false}`) — теперь она их открывает и судит каждый список
- [x] 11.4 Ползунок переключателя висел на верхнем крае дорожки: 14px в 18px, весь зазор
      снизу. Два правила спорили — верхнее ограничивало высоту, нижнее задавало `top` и
      `bottom`, и `bottom` не с чем было делать. Высота отдана библиотеке, ползунок стоит по
      центру (12×12, по 3px сверху и снизу); проверка теперь смотрит не только на выход за
      габарит, но и на перекос
- [x] 11.5 Заголовок читался «Выпуск источника, /мес.», хотя спека называет эту же подпись
      «Выпуск источника/мес.»: единица, начинающаяся со слеша, читается как «на» и запятой
      перед собой не требует — правило добавлено в `withUnit()`, одно на все вызовы
- [x] 11.6 Три одинаковых блока «подсказка + переключатель со значком» сведены в `IconSwitch`;
      `Field` сузился до того, что делает (ветка с `id` не использовалась, а `htmlFor` указывал
      в пустоту — проверено в браузере); рамка списка больше не требует детей, когда рисует
      пустое сообщение; два токена отступа сведены в один
- [x] 11.7 Языки файлов по правилу CLAUDE.md: тест единиц переведён на русский вслед за
      каталогом, тесты декодера GRF — на английский (набор `pipeline/tests` разноязычный).
      Заодно снят мёртвый параметр `width` у их хелперов
- [x] 11.8 Проверка пустого состояния не могла упасть — судила о том, что рисует сам
      `TableFrame`. Теперь смотрит на рамку: в ней либо строки, либо непустое сообщение,
      и оно нарисовано на её поле
- [x] 11.9 Затенение имени (`painted` из хелперов) и функция, названная как константа,
      переименованы; `design.md` описывает механику, которая в итоге сделана: единица в
      заголовке, выравнивание классом на ячейке — признак у колонки требовал бы объекта-описания,
      которого у списков нет (ADR 0003), а забытую ячейку ловит проверка по содержимому
