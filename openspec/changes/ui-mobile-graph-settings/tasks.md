## 1. Спайк: касания в Chromium

- [ ] 1.1 Стенд по D1 на прод-сборке (`preview()`), без правок приложения: под `touch-action: pan-x pan-y` залогировать pointer-события одного и двух пальцев (включая второй палец, положенный после начала прокрутки первым, — `cancelable` у `touchmove`), поданных CDP `Input.dispatchTouchEvent` (два `touchPoints`), и `page.touchscreen`; проверить `matchMedia('(pointer: coarse)')` в контексте `hasTouch: true` / `isMobile: true`; проверка — в `design.md` заполнен раздел «Результат спайка»: ответы на четыре вопроса D1, выбранная ветка (A или B) и способ эмуляции coarse для D7; скрипт стенда не закоммичен

## 2. Граф: жесты и стартовый вид

- [ ] 2.1 `zoomPan.ts`: `pinchView(...)` и `startView(content, viewport, focus?)` по D2/D3; проверка — `graph/__tests__/zoomPan.test.ts`: пинч вдвое вокруг середины оставляет точку графа под серединой, сдвиг середины двигает вид; `startView` для большого контента даёт `k = LABELS_FROM` и центр графа (или `focus`) в центре полотна, для маленького — `fit` с `k ≤ 1`
- [ ] 2.2 `useZoomPan`: набор указателей, мышь и перо как раньше, один палец не начинает жест, два пальца — пинч (или ветка B по итогу 1.1), без `setPointerCapture` на касании, `pointercancel` снимает указатель; `.graph-canvas { touch-action: pan-x pan-y }` с новым комментарием; проверка — `GraphCanvas.test.tsx`: существующие кейсы клика, захвата после сдвига и «перетаскивание — не выбор» зелёные; новые — один `touch`-указатель со сдвигом не меняет `transform` слоя; два указателя, разведённые вдвое, увеличивают `k` вдвое и следующий `click` не меняет выбор; касание без сдвига выбирает узел
- [ ] 2.3 `useZoomPan(content, focus)` стартует через `startView`, `GraphCanvas` передаёт центр выбранного узла; тест подписей в `GraphCanvas.test.tsx` переписан («opens at natural size with labels»), добавлен кейс большого контента (подписи есть на старте, после Fit — `hidden`); проверка — `cd web && npx vitest run src/features/firs`
- [ ] 2.4 Визуальные проверки графа: `graph.visual.test.ts` — на Steeltown после загрузки `data-labels` не `hidden`, Fit прячет, 1:1 возвращает; `.first()` в кейсах клика и перетаскивания заменён выбором видимого узла (бокс внутри `.graph-canvas`, `elementFromPoint` в центре — сам узел), ассерты `.firs-side` count 0 после снятия выбора (клик по фону, Escape) переписаны: `.firs-side .graph-legend` есть, `.node-card` нет (D3, D4); в контексте касаний (по итогу 1.1) один палец по полотну меняет `scrollY` и не меняет `transform`, два пальца, положенные на полотно, меняют масштаб и не меняют `scrollY`, касание узла выбирает его; клик мышью и колесо — прежние кейсы зелёные; проверка — `npx vitest run -c vitest.visual.config.ts graph`

## 3. Граф: легенда и прокрутка к карточке

- [ ] 3.1 `GraphLegend.tsx` по D4 (цвета только токенами, `CargoIcon` груза-примера, образцы узла, дубля, «На …», «Требует/Производит», ребра и затенения, строка управления); `FirsPage` рендерит `aside.firs-side` всегда: легенда без выбора, карточка при выборе; проверка — юнит-тест `FirsPage`: без выбора в колонке легенда, после клика по узлу — карточка, после Escape — снова легенда; после `setLocale('ru')` подписи легенды русские; корень легенды — `.graph-legend` (на него опираются ассерты 2.4)
- [ ] 3.2 Прокрутка к карточке по D4 (только на смену `selectedNode`, не на монтирование; геометрия + `scrollIntoView({block:'nearest'})`, `behavior` по `prefers-reduced-motion`); проверка — юнит-тест с замоканными `getBoundingClientRect`/`matchMedia`: карточка ниже окна → вызов с `smooth`, при reduce → `auto`, карточка в окне → вызова нет, снятие выбора → вызова нет, возврат на вкладку с выбором (стор уже с `selectedNode`, монтирование `FirsPage`) → вызова нет, а следующая смена выбора → вызов; визуальный кейс в `graph.visual` при 400×900: после клика по узлу бокс `.firs-side` пересекает окно, при 1440×900 `scrollY` не меняется
- [ ] 3.3 Легенда в проверке палитры и специмен; проверка — `palette.visual` и `window-colour.visual` на `/firs` зелёные, `exemptions.ts` пуст, `page.visual` (содержимое внутри `.firs-side`) зелёный в en и ru

## 4. Липкие боковые панели

- [ ] 4.1 `SavegameImportLauncher` пишет `--savegame-launcher-height` рядом с шириной и снимает при размонтировании; токены `--skin-sticky-top` (зазор — существующий `--skin-hsep-wide`, `--skin-gap` не существует и не заводится) и `--skin-launcher-height-fallback` в блоке отступов оболочки `:root` в `skin.css` рядом с `--skin-launcher-width-fallback`; проверка — юнит-тест лаунчера: после монтирования на `documentElement` обе переменные, после размонтирования обеих нет
- [ ] 4.2 `.consist-side`/`.firs-side`: `top`, `max-height`, `overflow-y: auto`, `overscroll-behavior: contain` на широком окне, сброс в `@media (max-width: 1000px)` по D5; проверка — новый `side-panel.visual.test.ts`: 1440×900 и 30 машин — панель не выше `innerHeight − top`, прокручивается внутри, итоги достижимы, после прокрутки страницы верх панели ниже нижнего края `.savegame-launcher` (en и ru); короткий состав — без своей прокрутки; 400×900 — `static`, без своей прокрутки; `/firs` с выбранным узлом — верх панели ниже кнопки
- [ ] 4.3 `routes.ts`: поле `scrollsY` у вкладки (`consist-side` на `/consist`, `firs-side` на `/firs`), `layout.visual.test.ts` сверяет `scrollsY` с ним, комментарий ссылается на третье исключение; `launcher.visual.test.ts` — кейс «липкая панель под кнопкой»; проверка — `layout.visual`, `launcher.visual`, `page.visual`, `focus.visual` и `dropdown.visual` на `/consist` зелёные

## 5. Подсказка «ещё колонки →»

- [ ] 5.1 `components/table/useOverflowHint.ts` по D6 (`data-more-end`, `--table-pinned-end`, RO + `scroll`); проверка — юнит-тест хука на jsdom: переполнение и `scrollLeft = 0` → атрибут есть; `scrollLeft` у конца → нет; `scrollWidth = clientWidth` → нет; после события `scroll` и колбэка RO значение пересчитано; ширина sticky-ячейки с `left: auto` попадает в переменную, без `pinEdges` — 0
- [ ] 5.2 `TableFrame`: обёртка `.table-frame`, `span.table-more[aria-hidden]`, CSS в `skin-mantine.css` токенами, `keepClearOfPinnedCells` учитывает видимую подсказку; grep `> .table-wrap` / `.table-wrap` в `web/src` и перевод затронутых селекторов; проверка — `TableFrame`-тесты и `components/table/__tests__` зелёные, `skin-palette.test.ts` зелёный, у `.table-more` нет `tabindex`, она `aria-hidden`
- [ ] 5.3 Визуальные проверки; проверка — `table.visual.test.ts` на `/optimizer` и `/consist`: в начале прокрутки `.table-more` видна и не пересекает бевель последней удерживаемой колонки; после прокрутки до упора её нет и последнее значение строки целиком видно (прежний ассерт); после расширения рамки до ширины таблицы подсказки нет; `focus.visual` — рамка фокуса ячейки у правого края не пересекает `.table-more`; `list.visual`, `layout.visual`, `kit.visual` зелёные

## 6. Цели касания

- [ ] 6.1 Разметка: в `OptimizerPage.tsx` и специмене `KitPage.tsx` `Checkbox` в `td.cell-pick` обёрнут в `<label className="cell-pick__hit">` без текста, `aria-label` у `Checkbox` прежний (D7); в DOM Mantine 9.5.1 подтвердить элементы касания из D7 (`.mantine-Switch-body` — `label`, стрелки `NumberInput` без `overflow: hidden`, корни `ActionIcon`/`Button` — `overflow: hidden`), `grep -rn "loading" web/src --include=*.tsx` не находит пропа `loading` у `ActionIcon`/`Button`; проверка — запись «элемент касания» по каждому виду в `design.md` D7; `OptimizerPage.test.tsx` и `comparison.visual` зелёные; в юнит-тесте флажок по-прежнему находится `getByRole('checkbox', { name: /Compare/ })`, клик по `label.cell-pick__hit` переключает сравнение
- [ ] 6.2 Блок `@media (pointer: coarse)` в `skin-mantine.css` и `skin.css`: `overflow: visible` на корнях `ActionIcon` в строках и `.language-switch`, `::after` 24px на них, на `.mantine-Switch-body` и `label.cell-pick__hit`, в колонке действия область прижата к правому краю кнопки, стрелки `NumberInput` расширяются наружу; бокс не меняется; проверка — новый `touch-targets.visual.test.ts` (эмуляция coarse по итогу 1.1) меряет действующую область: для каждого вида восемь точек в 11px от центра лежат вне видимого бокса и `elementFromPoint` в них отдаёт элемент касания или его потомка (без coarse — не отдаёт), точка посередине между соседними областями (две стрелки; `NumberInput` и `×` в панели состава) — ровно один элемент, `getBoundingClientRect` элементов совпадает с замером без coarse, боксы потомков `ActionIcon`/`.language-switch` внутри корня, `scrollWidth` `.table-wrap` на `/optimizer` под coarse равен замеру без coarse; касание в 10px от центра флажка сравнения переключает его
- [ ] 6.3 Десктоп не изменился; проверка — `controls.visual`, `rows.visual`, `footer-switch.visual`, `clipping.visual` зелёные без правок тестов

## 7. Settings: оглавление

- [ ] 7.1 Массив групп `{id, titleKey}`, `id` у девяти `Fieldset`, `nav.settings-toc` со ссылками, `scroll-margin-top: var(--skin-sticky-top)`, доводка к хешу после ленивой загрузки (D8); проверка — юнит-тест `SettingsPage`: у каждой группы `id`, ссылок девять, `href` каждой указывает на существующий `id` в порядке страницы, подпись JGRPP — текст legend без переключателя
- [ ] 7.2 Вид и поведение в браузере; проверка — визуальный кейс на `/settings`: клик по ссылке «Транспорт»/«Vehicles» ставит legend группы в окно ниже `.savegame-launcher`; Tab до ссылки, Enter, Tab — фокус в первом контроле группы; при 400px в en и ru ссылки перенесены, `scrollsX` нет, `clipping.visual` и `link.visual` зелёные

## 8. Settings: отметка «изменено» и сброс по одной

- [ ] 8.1 `DEFAULT_CURRENCY`/`DEFAULT_SPEED_UNIT` в `settingsStore.ts`, начальное состояние и `reset()` читают их; проверка — `grep -n "'GBP'\|'metric'" web/src/state/settingsStore.ts` показывает только объявления констант; `features/settings/__tests__/reset.test.tsx` и `display-units` тесты зелёные
- [ ] 8.2 Хуки `useGameDefault`/`useCalcDefault`/`useDisplayDefault` с показанным значением для `firsEconomy` и `trackType` (D9); проверка — юнит-тест хуков: `vehicleCosts` 1 → changed, reset → 0; `trackType: 'RAIL'` у набора без RAIL → не changed; `firsEconomy` неизвестной экономики → не changed
- [ ] 8.3 `SettingRow`/`NestedSettingRow` проп `setting`, `SettingChangedMark` (текст «изменено» + кнопка с `aria-label` `settings.resetOne`) в колонке подписи, в legend JGRPP — после переключателя; подключить ко всем строкам `game`/`calc`/валюты/скорости, язык — без; стили в `skin.css`; проверка — `SettingRow.test.tsx`: без `changed` отметки и кнопки нет; с `changed` кнопка названа «Reset "<label>" to default», контрол строки по-прежнему назван `label`; `controlNames.test.tsx` зелёный
- [ ] 8.4 Покрытие `settingDefaults.test.tsx` по D9: перебор всех ключей `DEFAULT_GAME_SETTINGS`, `DEFAULT_CALC_SETTINGS`, `currency`, `speedUnit` с включёнными родителями — ровно одна кнопка сброса с именем этой настройки («Reset "<label>" to default»; кнопки включённых родителей `jgrpp`/`trainSet`/`firs`/`inflation`/`basecostGrf` не считаются), нажатие возвращает умолчание в сторе; кейсы языка, скрытого JGRPP (значение сохранено, отметки нет, после включения — есть), вложенного множителя Base Costs, сброса `basecostGrf` без изменения множителей, импорта через `applySettings` с годом 1990 (отметка есть, сброс → 1950); проверка — тест зелёный, `brakingSettings`, `newgrfSection`, `importFirst` зелёные
- [ ] 8.5 Специмен изменённой строки настройки на `/kit` с устойчивым признаком разметки; проверка — `kit.visual`, `rows.visual`, `focus.visual` (кнопка сброса с клавиатуры), `clipping.visual` на `/settings` при 400px в en и ru зелёные
- [ ] 8.6 `settings-effect.test.ts` не требует правок: сброс — не настройка; проверка — `cd web && npx vitest run -t "settings"` зелёный, `git diff --stat web/src/engine` пуст

## 9. Строки интерфейса

- [ ] 9.1 en/ru: `firs.legend.*` (заголовок, узел предприятия, бейдж груза и цвет из игры, дубль, «На …», «Требует/Производит», направление рёбер, затенение, управление мышью/колесом/двумя пальцами/клавиатурой), `table.moreColumns`, `settings.toc`, `settings.changed`, `settings.resetOne` (`Reset "{name}" to default` / `Сбросить «{name}» к значению по умолчанию`), `settings.resetOneShort`; названия групп в оглавлении — существующие ключи legend; проверка — `i18n/__tests__/locales.test.ts` зелёный

## 10. Завершение

- [ ] 10.1 `CHANGELOG.md` → `[Unreleased]`, дописать в существующие `### Added` / `### Changed` / `### Fixed`, не дублируя заголовков: Added — two-finger pan and pinch on the chain graph, graph legend, settings table of contents, per-setting "changed" mark and reset, "more columns →" hint on wide lists; Changed — one finger scrolls the page over the graph, the graph opens at a scale with labels, side panels of the consist builder and the chain graph fit the window and scroll inside, narrow windows scroll to the node card; Fixed — the import button no longer covers the sticky side panels, touch targets of small controls are at least 24px on touch screens; на английском; проверка — `scripts/next-version.sh` выдаёт minor
- [ ] 10.2 Прогнать `make test`, `make check-i18n`, `cd web && npx tsc -b && npx oxlint src`, затем отдельно `caffeinate -i make check-visual` (без параллельных браузеров); проверка — всё зелёное
