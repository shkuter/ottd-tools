## 1. Общая сборка рейса

- [x] 1.1 Вынести сборку `routeParams` из `features/route/RoutePage.tsx` в
      `features/route/useRouteParams.ts`: хук читает `routeStore`, `consistStore`,
      `settingsStore`, чинит груз через `useActiveCargo` и возвращает `RouteInputs` —
      `{ cargoList, cargo, entries, payment, routeParams }`. `RoutePage.tsx` уже вызывает хук,
      самого файла ещё нет. Проверка: `npx vitest run features/route` зелёный, числа панели
      прибыльности не изменились

## 2. Каркас вкладки «Сеть»

- [x] 2.1 Добавить `{ path: '/network', label: 'nav.network' }` в `tabs.ts` сразу после
      `/income` (без `windowColour` — базовый серый) и маршрут с ленивым импортом в `App.tsx`.
      Проверка: вкладка видна в ряду, `/network` открывается прямой ссылкой
- [x] 2.2 Завести `features/network/NetworkPage.tsx` с корнем `.page-network`, заголовком
      второго уровня и справкой о рейсе (груз, дистанция, состав) со ссылкой на «Доход рейса»;
      при несобранном рейсе справка говорит, чего не хватает. Проверка: тест на сценарии
      «Справка о рейсе» и «Рейс не задан» из `specs/network-tab`
- [x] 2.3 Прописать корень вкладки в `__tests__/visual/routes.ts` (`'/network': { ready:
      '.page-network' }`). Проверка: `ROUTES` собирается без исключения, `npx vitest run
      __tests__/routes` зелёный

## 3. Переезд блоков

- [x] 3.1 Перенести `NetworkMaintenance.tsx`, `CorridorUpgrade.tsx`, `SignalDensity.tsx` в
      `features/network/` и поставить их на `NetworkPage` в порядке «обслуживание → коридор →
      сигналы», рейс — из `useRouteParams()`. Проверка: блоков на «Доходе рейса» больше нет, а
      на одном и том же вводе (количества сети, коридор, состав) годовое содержание, дельта
      перевода и рекомендованное число сигналов равны тем, что блоки давали до переезда
- [x] 3.2 Перенести тесты трёх блоков в `features/network/__tests__/` без изменения ожиданий
      (`PrefillNote.test.tsx` остаётся в `features/route/__tests__` — он про мосты рейса и
      оптимизатора). Проверка: `npx vitest run features/network` зелёный
- [x] 3.3 Снять с панелей классы `route-network` / `route-corridor` / `route-signals` (правил у
      них после переезда нет), дать каждой `id` из общего реестра `NETWORK_ANCHORS` и перевести
      затронутые правила `skin.css` (строки 437, 795–797, 917, 933–935), перенеся
      `.network-inputs` в секцию сети. Проверка:
      `grep -r "route-network\b\|route-corridor\|route-signals" web/src` пуст (`route-network-note`
      — имя указателя, который остаётся на вкладке маршрута, и под это исключение не подпадает)
- [x] 3.4 Задать вкладке свою раскладку: `.page-network` — одна колонка (не грид `320px 1fr`
      из `.page-route`, `skin.css:775`), у переехавших панелей снимается `grid-column: 2`.
      Проверка: слева от панелей нет пустого жёлоба, `make check-visual` зелёный
- [x] 3.5 Привести `.route-controls` (`skin.css:782–787`, `grid-row: 1 / span 4` — счёт панелей
      колонки 2) к тому, что осталось на «Доходе рейса» после отъезда трёх панелей. Проверка:
      колонка управления не растянута над пустотой, `make check-visual` зелёный
- [x] 3.6 Добавить на «Доход рейса» строку-указатель со ссылкой на вкладку «Сеть» на месте
      уехавших блоков. Проверка: тест на сценарий «Указатель на прежнем месте»

## 4. Сводка «что подрезать»

- [x] 4.1 Написать `features/network/actions.ts`: чистая функция над результатами
      `networkMaintenance`, `corridorUpgrade` и `signalPlan` — список действий (что сделать,
      сумма в год, доля от годового содержания, якорь блока), отсортированный по убыванию
      суммы; попадают только посчитанные и положительные. Проверка: юнит-тесты на порядок
      («сигналы дороже коридора»), на отсев отрицательной дельты и на разреженную линию
- [x] 4.2 Поставить сводку сверху вкладки: годовой итог владения, самая дорогая статья с долей,
      список действий ссылками на якоря блоков. Проверка: тесты на сценарии «Итог и самая
      дорогая статья», «Сводка и блок не расходятся», «Действие ведёт к своему блоку»
- [x] 4.3 Пустые состояния сводки: количества не введены — просит их назвать; рейс не задан —
      показывает владение и называет, чего не хватает; подрезать нечего — говорит это прямо.
      Проверка: тесты на три сценария из `specs/network-tab`

## 5. Мост из партии

- [x] 5.1 Перевести мост «сеть компании» (`features/savegame/CompanyNetworkCard.tsx`) на
      `/network`. Проверка: тест моста ведёт на вкладку «Сеть», пометка `prefillOrigin` на
      количествах на месте

## 6. Строки интерфейса

- [x] 6.1 Дозавести строки в `i18n/en.json` и `ru.json`: подписи сводки владения,
      формулировки действий «что подрезать» и её пустых состояний. `nav.network`,
      `networkPage.*` (заголовок, справка о рейсе, ссылка) и `route.network*` (указатель на
      «Доходе рейса») уже заведены. Проверка: `npx vitest run i18n` зелёный, `make check-i18n`
      без расхождений

## 7. Документация

- [x] 7.1 Описать вкладку «Сеть» в `README.md` и `README.ru.md` одним коммитом: пункт списка
      вкладок и вынос из него трёх блоков, которые сейчас описаны внутри «Route income» /
      «Доход рейса» (`README.md:18-26`, `README.ru.md:18-26`). Проверка: структура разделов,
      порядок пунктов и ссылки в двух файлах совпадают
- [x] 7.2 Поправить Consequences в
      `docs/adr/0006-owning-track-costs-what-the-player-counted.md:40`: «The route income tab
      carries two panels…» — после переезда панели стоят на вкладке «Сеть», а их теперь три.
      Проверка: ADR описывает нынешнее размещение, решение и его мотив не переписаны
- [x] 7.3 Завести в `CONTEXT.md` термины, которые вводит вкладка: список «что подрезать» и
      самая дорогая статья — в том же формате, что соседние записи раздела Network upkeep
      (английский термин, русский в скобках, `_Avoid_`). Проверка: у обоих есть русская пара и
      оговорка, чего они не значат

## 8. Завершение

- [x] 8.1 Дописать запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском: новая вкладка
      «Network», переезд трёх блоков, сводка с ранжированием действий. Проверка: запись стоит
      под `### Added`, разряд по ней читается как minor
- [x] 8.2 Прогнать `make test` и `make check-visual`. Проверка: оба зелёные
