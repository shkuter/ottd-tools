## 1. Серия и ранний уход в данных

- [ ] 1.1 Извлекать в `pipeline/extract_xussr.py` голову серии, признак синхронизации
  надёжности и точку отсчёта возраста серии (покупаемая голова — её дата, фиктивная — начало
  партии). Проверка: кейс в `pipeline/tests/` утверждает, что четыре типа Тᴷ несут одну голову
  с синхронизацией, ЧМЭ3 — голову без синхронизации, а сама голова в `items` отсутствует
- [ ] 1.2 Извлекать те же поля плюс `retire_early` в `pipeline/extract_iron_horse.py`.
  Проверка: кейс в `pipeline/tests/test_known_values.py` утверждает `retire_early = -10` у
  известной машины и непустую голову у машины из группы вариантов
- [ ] 1.3 Пересобрать данные (`make data`) и убедиться, что менялись только новые поля.
  Проверка: `git diff --stat` показывает файлы данных, `make check-i18n` проходит
- [ ] 1.4 Провести новые поля через `web/src/types.ts` и `dataset.ts`. Проверка:
  `npx tsc --noEmit` проходит, у ванили поля пустые

## 2. Модель доступности

- [ ] 2.1 Добавить в `web/src/engine/availability.ts` границу появления с годом ожидания и
  исключением для машин старше `game.startingYear`. Проверка: тесты на машину 1874 года при
  годе расчёта 1875 (пометка), на машину с ещё не наступившей датой (нет в списке) и на машину
  старше начала партии (без пометки)
- [ ] 2.2 Добавить границы списания по разыгранным срокам — числа и кламп берутся из
  design.md, раздел о модели без партии. Проверка: тест на вагоне, чей срок модели истёк, но
  нижняя граница не достигнута, и тест на машине со сроком модели меньше восьми лет (второй
  срок равен нулю)
- [ ] 2.3 Учесть `retire_early`: при ненулевом значении верхняя граница считается без третьего
  срока. Проверка: тест на машине Iron Horse — граница равна первому и второму срокам плюс 120
  месяцев
- [ ] 2.4 Считать возраст машины серии от точки отсчёта серии, и только при синхронизации
  надёжности. Проверка: тест на партии с 1862 года и годе расчёта 1875 — типы серии Тᴷ вне
  продажи, ЧМЭ3 (серия без синхронизации) не задета
- [ ] 2.5 Скрывать машину с вместимостью, которой нечего возить в текущем наборе грузов, через
  существующий `canCarryIn` (`dataset.ts`). Проверка: тест — пищевые цистерны отсутствуют в
  экономике Steeltown и присутствуют в экономике, где их груз есть; локомотив без вместимости
  остаётся
- [ ] 2.6 Добавить настройку `neverExpireVehicles` в `GameSettings` (`engine/settings.ts`,
  `state/settingsStore.ts`, поле на вкладке настроек) и снимать ею границы списания. Название
  взять из `vendor/openttd/src/lang/russian.txt` (`STR_CONFIG_SETTING_NEVER_EXPIRE_VEHICLES`).
  Проверка: `make test` проходит
- [ ] 2.7 Добавить кейс новой настройки в `web/src/engine/__tests__/settings-effect.test.ts`.
  Проверка: тест падает, если настройку не прокинуть в фильтр
- [ ] 2.8 Перевести `web/src/savegame/mapping.ts` с показа `vehicle.never_expire_vehicles`
  справкой на чтение её в настройку. Проверка: тест импорта читает флаг из сейва в
  `game.neverExpireVehicles`
- [ ] 2.9 Заменить дублирующееся правило в `web/src/engine/optimize.ts` и
  `web/src/features/consist/ConsistPage.tsx` вызовом новой функции. Проверка: тест, что
  каталог, подбор и список снабжения дают один и тот же набор машин для одного года

## 3. Ответ импортированной партии

- [ ] 3.1 Написать `web/src/savegame/extract/engn.ts`: записи чанка `ENGN`, доступные компании
  игрока и не скрытые ею. Проверка: тест на фикстуре `xussr-1872.sav` — число доступных машин
  меньше числа записей чанка
- [ ] 3.2 Перевести индексы записей в id каталога через `readEngineIds` и сопоставление
  `savegame/mapping.ts`; машины неизвестных GRF пропускать. Проверка: тест на той же фикстуре
  утверждает конкретные id и то, что разбор не падает на чужом GRF
- [ ] 3.3 Положить множество id и год партии в снапшот, подняв версию его схемы. Проверка:
  тест в `web/src/savegame/__tests__/snapshot.test.ts` — снапшот прошлой версии не читается,
  новый несёт список
- [ ] 3.4 Применять список в списках машин, когда год расчёта равен году партии. Проверка:
  тест на фикстуре — «Паровоз Тᴷ (0-3-0) 1й тип» отсутствует, вагон, продающийся в партии, но
  снятый формулой, присутствует
- [ ] 3.5 Проверить импорт сейва без списка доступности. Проверка: тест на сейве, где чанка
  нет, — импорт проходит, списки считаются формулой

## 4. Что видит пользователь

- [ ] 4.1 Показать пометку на машине под вопросом (появление или списание) и строку о том, что
  список взят из партии. Проверка: тест рендера показывает пометку и строку
- [ ] 4.2 Выбирать представителя пункта среди продающихся машин. Проверка: тест — пункт со
  снятым представителем показывает другую свою машину, а пункт, где не продаётся ни одна,
  исчезает
- [ ] 4.3 Добавить новые строки в `web/src/i18n/en.json` и `ru.json`. Проверка:
  `web/src/i18n/__tests__/locales.test.ts` проходит

## 5. Завершение

- [ ] 5.1 Дописать запись в `CHANGELOG.md` в `## [Unreleased]` на английском, пометив её
  `**BREAKING**` (список машин меняется). Проверка: секция содержит запись
- [ ] 5.2 Прогнать `make data` и `make test`. Проверка: обе команды проходят, рабочее дерево
  не содержит незапланированных правок данных
