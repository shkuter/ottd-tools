## 1. Общий модуль группировки (engine/purchase.ts)

- [x] 1.1 Создать `web/src/engine/purchase.ts`: `purchaseKey(train, capacityIndex, game)` (вид,
  тип путей, имя, вместимость, длина, дата появления, вес, а в режиме без FIRS — ещё и грузы
  по умолчанию, см. 1.4), тип `PurchaseEntry`
  (`{ key, train, members }`), `purchaseEntries(trains, capacityIndex, game)` и
  `purchaseRepresentatives(trains, capacityIndex, game)`. Проверяемо: `npx vitest run purchase`
  зелёный, модуль не импортирует сторы.
- [x] 1.2 Перенести туда `preferWagon` из `optimize.ts` под именем `preferTrain`
  (нерандомизированный → меньший идентификатор) и использовать его при выборе представителя
  пункта.
- [x] 1.3 Переключить `optimize.ts` на импорт `preferTrain` из нового модуля; расчётный
  профиль оставить как есть. Проверяемо: `npx vitest run engine` зелёный, выдача прежняя.

- [x] 1.4 Класть грузы по умолчанию в ключ только при `ironHorse && !firs` — единственный
  режим, где их читает `canCarryIn`. Проверяемо: General Flat Wagon — один пункт с FIRS и
  несколько без него.

## 2. Каталог конструктора

- [x] 2.1 В `ConsistPage.tsx` строить список из `purchaseRepresentatives(activeTrains(game),
  calc.capacityIndex, game)` в отдельном `useMemo` до фильтров; фильтры, сортировка и пагинация
  работают поверх него.
- [x] 2.2 Обновить комментарий про размер каталога (`~1650 vehicles` → пункты списка покупки).
- [x] 2.3 Проверить в браузере: каталог отдаёт 965 записей при полном наборе, фильтр по грузу
  и поиск по имени работают, добавление в состав считает те же числа. Проверено: 1648 → 965
  с FIRS, 1648 → 990 без FIRS, ваниль 116 → 116; в браузере «Coil Carrier» в 2000 году — две
  строки вместо восьми, «Mail Van» — три вместо десятка, пагинация и фильтр по грузу считают
  пункты, добавленный в состав вагон даёт те же вес и цену, что строка каталога.

## 3. Список «под вопросом»

- [x] 3.1 Перевести `doubtful.ts` на `purchaseEntries` из общего модуля, свой `purchaseKey`
  удалить; `ids` группы брать из `members`.
- [x] 3.2 Проверить `doubtful.test.ts`: чекбокс по-прежнему гасит семейство разом, метро
  surface и tube теперь два пункта. Обновить тест, если он опирался на их склейку.

## 4. Тесты

- [x] 4.1 `web/src/engine/__tests__/purchase.test.ts`: на реальном `trains.json` семейство
  Coil Carrier схлопывается в одну запись на поколение, движки не схлопываются, ванильный
  набор не меняется.
- [x] 4.2 Тот же файл: представитель — нерандомизированная машина; пункт из одной
  рандомизированной машины представлен ею.
- [x] 4.3 Инвариант: внутри каждого пункта совпадают все числа расчёта (мощность, ТЭ,
  скорость, вместимости, цена, содержание, loading speed, рефит, `model_life`,
  `dual_headed`). Гоняется для обоих режимов своим набором полей: грузы по умолчанию входят в
  проверку только там, где их читает `canCarryIn`. Тест падает, если обновление набора сделает
  пункт неоднородным.

## 5. Завершение

- [x] 5.1 `make verify` зелёный целиком (data + check-i18n + test + build; 241 тест vitest).
  Перегенерация данных изменила в `meta.json` только дату сборки — откачено, данные прежние.
- [x] 5.2 Запись в `CHANGELOG.md` → `[Unreleased]` (patch, на английском).
