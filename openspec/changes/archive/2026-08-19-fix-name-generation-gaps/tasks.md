## 1. Уникальность имён

- [x] 1.1 `pipeline/extract_firs_ru.py`: после сборки словарей проверить, что внутри каждой
      экономики нет двух грузов и двух предприятий с одинаковым отображаемым именем; при
      совпадении — `SystemExit` с обоими id и их строками-источниками
- [x] 1.2 Прогнать генерацию и выписать полный список коллизий (ожидается одна: `sawmill` /
      `timber_yard`); если их больше — развести каждую в 1.4
- [x] 1.3 `web/src/i18n/__tests__/locales.test.ts`: тест на уникальность имён внутри экономики
      (падает на закоммиченных словарях с коллизией)
- [x] 1.4 `pipeline/ru_overrides.json`: запись для `STR_IND_TIMBER_YARD` — «Лесопилка» →
      «Лесной склад» (английское `Timber Yard`; игровое имя `sawmill` не трогаем)

## 2. Правки для любого источника

- [x] 2.1 `pipeline/extract_firs_ru.py`: перенести `apply_overrides` за `translate()` — правки
      применяются к готовому имени, ключ остаётся id строки, формат `ru_overrides.json` не
      меняется
- [x] 2.2 Сообщения об ошибках: «строка не используется» и «значение разошлось с `from`» должны
      называть настоящую причину для строк из локали игры (сейчас `TTD_*` падает с «is None»)
- [x] 2.3 `pipeline/tests/test_known_values.py`: кейс на правку строки из локали игры и кейс на
      устаревшую правку (сборка падает)

## 3. Воспроизводимость и порядок сборки

- [x] 3.1 `Makefile`: пин `OPENTTD_REF` для `vendor/openttd`, как у остальных источников
- [x] 3.2 `pipeline/validate.py` + `pipeline/common.py`: ревизия OpenTTD попадает в `meta.json`
      рядом с `firs_ru` (через существующий `vendor_meta()`)
- [x] 3.3 `Makefile`: `verify: data check-i18n test build`; `data` зависит от `fetch-firs-ru`
- [x] 3.4 Проверить смену `OPENTTD_REF` на чистом клоне; если существующий `vendor/openttd`
      не переключается, зафиксировать это в `CLAUDE.md` рядом с описанием `make fetch`

## 4. Проверка

- [x] 4.1 `make data` — словари перегенерированы, изменились только имена из 1.4
- [x] 4.2 `web/src/data/meta.json` показывает ревизию OpenTTD; футер приложения её отображает
- [x] 4.3 `CHANGELOG.md` `[Unreleased]`: `Fixed` — коллизия имён, правки для имён из локали
      игры, порядок целей `verify` (по-английски)
- [x] 4.4 `make verify` целиком

## 5. Всплывшее при `make verify` (по решению 19.08)

- [x] 5.1 `engine/consist.ts`: `consistPhysics`/`consistStats` принимают `readonly ConsistEntry[]`
      — они только читают состав, а `trip.ts` передаёт readonly-массив
- [x] 5.2 `state/__tests__/reset.test.ts`: привязка хранилища вынесена в дженерик-хелпер —
      перебор разнотипных сторов схлопывал их состояния в пересечение типов
- [x] 5.3 `pipeline/extract_vanilla.py`: метки грузов читаются и как `{'PASS'}` (релизы),
      и как `{"PASS"}` (мастер) — без этого пин на 15.3 не собирается
