## 1. Глоссарий и допущения

- [x] 1.1 Поправить `CONTEXT.md`, статья `Cargo per trip`: убрать неверное «the train waits to
      be filled rather than running light», описать две ветки загрузки и то, что физика в обеих
      считается на полном составе
- [x] 1.2 Добавить в `CONTEXT.md` термины `Loading branch` (ветка загрузки) и `Accumulation
      wait` (ожидание накопления) с пометкой о допущении равномерного деления потока

## 2. Модуль ожидания

- [x] 2.1 Создать `web/src/engine/waiting.ts`: темп накопления из отдаваемого за год потока и
      множителя длины дня, время накопления вместимости на парк, замкнутая форма
      `max(физический круг, парк × вместимость ÷ темп)`
- [x] 2.2 Тесты `waiting.test.ts`: медленный источник (ожидание > 0), быстрый источник
      (ожидание = 0), удвоение парка (круг растёт, годовой вывоз тот же), множитель длины дня
      (ожидание растёт пропорционально), поток = 0 (ожидание не определено, не `NaN`)

## 3. Ветки в расчёте рейса

- [x] 3.1 Ввести ветку загрузки параметром `tripMoney` (`waitForFullLoad`), по умолчанию —
      текущее поведение «уезжает с накопленным»
- [x] 3.2 В ветке ожидания: круг из `waiting.ts`, груз за рейс равен вместимости, пересчёт
      рейсов за год и интервала от нового круга
- [x] 3.3 Вынести в результат рейса поля обеих веток, чтобы вызывающий код мог их сравнить, не
      считая маршрут дважды
- [x] 3.4 Тест-регрессия: при ветке без ожидания все числа `trip.ts` совпадают с текущими до
      последнего знака (снять эталон до правки)
- [x] 3.5 Тесты веток: расхождение на медленном источнике, совпадение на быстром, равенство
      годового вывоза в обеих ветках

## 4. Перебор в оптимизаторе

- [x] 4.1 Считать обе ветки в переборе, `tripSetup` при этом вычислять один раз
- [x] 4.2 Выбирать ветку по цели через `GoalStrategy`, не дублируя логику сравнения
- [x] 4.3 Добавить в `OptimizeResult` признак выигравшей ветки и различимость веток (при
      незаданном выпуске — «неразличимы»)
- [x] 4.4 Тесты `optimize`: ветка без ожидания выигрывает по вывозу, ветка ожидания выигрывает
      по прибыли, при выпуске = 0 признак не выставляется
- [x] 4.5 Проверить, что удвоение перебора не роняет отзывчивость выдачи (требование
      «Отзывчивость выдачи» в `train-optimizer`) — разовый замер, результат в `design.md`,
      раздел «Risks / Trade-offs»; автотеста нет намеренно, замер зависит от машины

## 5. Интерфейс

- [x] 5.1 Показать признак ветки в строке оптимизатора (значок или пометка в подсказке, без
      новой колонки)
- [x] 5.2 Добавить выбор ветки на вкладку «Доход рейса» как состояние формы, не настройку
- [x] 5.3 Новые UI-строки в `web/src/i18n/en.json` и `ru.json`, прогнать
      `i18n/__tests__/locales.test.ts`

## 6. Сверка с игрой

- [x] 6.1 Завести эталонный сценарий из партии в экономике Steeltown как тест: обе ветки,
      ожидаемое расхождение интервала. Сверка снята с сейва Londworth Transport, июнь 1989
      (каучук с причала Nentbourne на резиновый завод Sardham, 2 поезда × 14 вагонов): маршрут
      идёт без приказа полной загрузки и воспроизводит фактический приход на станцию с
      точностью 2 %, поэтому ветка ожидания на нём сверяется расчётом, а не наблюдением
- [x] 6.2 Сверить числа обеих веток с партией и записать расхождения; при расхождении больше
      допустимого — не подгонять формулу, а зафиксировать наблюдение в change

## 7. Завершение

- [x] 7.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском, с пометкой
      `**BREAKING**`
- [x] 7.2 Прогнать `make test` и `make verify`

## 8. Правки по ревью

Ревью диффа `9b1cf03` по двум осям (стандарты репозитория и соответствие спеке). Правки кода —
через `/opsx:apply`.

- [x] 8.1 `web/src/index.css:265` — `var(--text-dim, inherit)` заменить на `var(--muted)`:
      токена `--text-dim` в проекте нет, поэтому пометка проигравшей ветки не гаснет
- [x] 8.2 `web/src/state/routeStore.ts` — комментарии перевести на английский (`openspec/config.yaml`:
      код и комментарии — на английском); заодно старый русский JSDoc у `manualDays`
- [x] 8.3 `CHANGELOG.md` — запись `**BREAKING**` про сдвиг чисел перенести из `### Added` в
      `### Changed` (Keep a Changelog); разряд версии от этого не меняется
- [x] 8.4 `web/src/engine/waiting.ts` — шапка модуля осталась от первой редакции и утверждает,
      что доля вывоза берётся по физическому интервалу и годовой вывоз в ветках равен; описать
      решение «доля вывоза по интервалу своей ветки»
- [x] 8.5 `web/src/features/route/RoutePage.tsx` — физика считается дважды (`tripEconomics` на
      подготовку и ещё раз на итог); перейти на `tripSetup` + `tripMoney`, как в оптимизаторе
- [x] 8.6 Цепочка «поток → доля вывоза → груз за рейс» собрана заново и в `RoutePage.tsx`, и в
      `optimize.ts` — вынести общий хелпер в `engine/` (формулы живут в `engine/`, не в UI)
- [x] 8.7 Долю вывоза на вкладке дохода считать от той же скорости, что в `optimize.ts`
      (предельная скорость состава, а не установившаяся гружёная) — сценарий «Тот же маршрут на
      двух вкладках»
- [x] 8.8 Имена и лишнее наружу: мемо `trip` в `RoutePage.tsx` (обращение `trip.trip`),
      неиспользуемое поле `offeredPerYear`, экспорт `accumulationRatePerDay` и
      `Accumulation.ratePerDay` только под тесты
- [x] 8.9 Учесть ожидание в возрасте груза: доход ветки ожидания считать по `daysLoaded +
      waitDays`, сверив такт старения по `vendor/openttd/src/vehicle.cpp`; тест на то, что
      длинное накопление удешевляет доставку, и запись в `CHANGELOG.md`
- [x] 8.10 Подсказка ветки в оптимизаторе: `opt.branchNoWait` даёт только слова — добавить числа
      проигравшей ветки (интервал между визитами и груз за рейс) в обе строки `en.json` / `ru.json`
- [x] 8.11 `docs/adr/0001-delivered-share-cuts-money-only.md` — раздел Consequences устарел
      («вкладка дохода не знает про поток производства»); дописать ревизию с датой
- [x] 8.12 Переименовать `web/src/engine/__tests__/steeltown-rubber.test.ts` под сейв, с
      которого снят эталон (Londworth), — имя вводит в заблуждение
- [x] 8.13 Прогнать `make test` и `make verify` после правок

## 9. Правки по второму ревью

Ревью фичи целиком (`4557ee5...HEAD`) после раздела 8. Правки кода — через `/opsx:apply`.

- [x] 9.1 `opt.assumptionProduction` в `web/src/i18n/en.json` и `ru.json` утверждает, что вкладка
      дохода считает «a single train on the full output» — после веток это неправда; переписать
      обе локали под расхождение «весь парк против одного состава»
- [x] 9.2 Остаток формул в `web/src/features/route/RoutePage.tsx` (`flowPerYear = production × 12`,
      `cargoPerDay`, замыкание `estimateStationRating`) вынести в `engine/` — сейчас это копия
      `optimize.ts`, и совпадение вкладок держится на дисциплине, а не на коде
- [x] 9.3 `daysPerEconomyYear(game) × effectiveDayLength(game)` повторяется в четырёх местах
      (`trip.ts` дважды, `waiting.ts`, `RoutePage.tsx`) — общий хелпер `engineDaysPerYear(game)`
- [x] 9.4 `AccumulationParams` / `WaitingSettleParams` / `BranchFlowParams` в `waiting.ts` несут
      один и тот же набор `physicalRoundTripDays / capacity / fleetSize / game`, а `BranchFlows`
      дважды повторяет пару «рейтинг + поток» — свести к общим типам
- [x] 9.5 `underfilled()` в `web/src/engine/optimize.ts` возвращает `branchesDiffer`: имя обещает
      недогруз, тело говорит про различимость веток — переименовать
- [x] 9.6 `CHANGELOG.md` — пункт про сверку с партией Londworth стоит в `### Added`, хотя это
      тест, а не то, что видит пользователь
- [x] 9.7 Возраст груза в ветке ожидания: заменить `waitDays / 2` на
      `waitDays × (физический круг + waitDays ÷ 2) ÷ (физический круг + waitDays)` — накопленное
      за время рейса грузится разом и стареет всё ожидание; тест на обе доли, запись в
      `CHANGELOG.md`
- [x] 9.8 `web/src/engine/__tests__/londworth-rubber.test.ts` считает рейтинг от
      `setup.loadedSpeedInternal`, а обе вкладки — от `loadedPhysics.maxSpeedInternal`: сверка с
      партией калибрует не тот вход
- [x] 9.9 Тест на сценарий «Тот же маршрут на двух вкладках»: доля вывоза, интервал и груз за
      рейс маршрута с парком в один состав совпадают со строкой оптимизатора
- [x] 9.10 Кнопка «→» (`OptimizerPage.tsx`) переносит только груз, экономику и дистанцию —
      добавить выпуск предприятия и выигравшую ветку строки, иначе перенесённый маршрут считается
      другой моделью
- [x] 9.11 Тест на страховочный предел проходов в `settleWaitingBranch`: выход — совпадение
      рейтинга, предел не должен подменять сходимость
- [x] 9.12 Прогнать `make test` и `make verify` после правок

## 10. Правки по третьему ревью

Ревью фичи целиком после раздела 9. Правки кода — через `/opsx:apply`.

- [x] 10.1 UI-строка `combined.accumulationWait` («Ожидание загрузки» / «Waiting for the load»)
      расходится с термином глоссария `CONTEXT.md` — `Accumulation wait` / «ожидание
      накопления»; в преамбуле глоссария сказано, что русская колонка — это то, что говорит
      интерфейс, поэтому свести строку и термин
- [x] 10.2 Шапка `web/src/engine/__tests__/tabs-agree.test.ts` обещает, что тест воспроизводит
      расчёт вкладки (`tripSetup` + `settleBranchFlows` + `tripMoney`), а он зовёт ту же
      `routeWithFlow()`, что и вкладка, — описать гарантию как она есть
- [x] 10.3 `routeStationRating()` не принимает `vehicleAgeYears`, поэтому вызов рейтинга собран
      руками ещё в двух местах (`londworth-rubber.test.ts`, `tabs-agree.test.ts`) — принять
      возраст, чтобы хелпер покрывал реальные случаи
- [x] 10.4 `tripsPerYear` считается в `web/src/engine/trip.ts` дважды, причём вторая формула без
      защиты `roundTripDays > 0 && Number.isFinite` — свести к одной
- [x] 10.5 `Math.round(deliveredShare * 100)` повторён на обеих вкладках — общий `percent()` в
      `web/src/components/format.ts`
- [x] 10.6 `TripParams` и `TripMoneyParams` дублируют `cargoPerTrip` / `fleetSize` /
      `subsidised`, а change добавил в оба ещё и `waitForFullLoad` / `offeredPerYear` — свести
      к общему типу, чтобы они не расходились при следующей правке
- [x] 10.7 `web/src/engine/optimize.ts` кладёт `offeredPerYear: waitingOffered` в общий объект
      `money`, хотя ветка без ожидания его игнорирует, — передавать только той ветке, которая
      его читает
- [x] 10.8 Тест «Тот же маршрут на двух вкладках» констрейнит только ветку без ожидания: в его
      прогоне ни одна строка не выигрывает в ветке ожидания — добавить кейс с выигравшей веткой
      ожидания (сценарий «Перенос при заданном производстве» для неё сейчас не покрыт)
- [x] 10.9 Тест предела проходов допускает 16 при пределе 8 — пиннить фактический предел, плюс
      проверка, что на реальном маршруте выход происходит по совпадению рейтинга, а не по
      пределу
- [x] 10.10 `trip-branches.test.ts` — комментарий «Half the wait» остался от редакции до правки
      формулы возраста груза
- [x] 10.11 `londworth-rubber.test.ts` — «~4 points of rating higher»: расхождение с партией это
      ~10 пунктов рейтинга (≈4 п.п. доли вывоза), как записано в `design.md`
- [x] 10.12 `engineDaysPerYear()` читается как «дней в году у локомотива» — в этом домене
      `engine` значит локомотив (`OptimizeResult.engine`); либо переименовать, либо завести
      термин «engine day» в `CONTEXT.md`
- [x] 10.13 `sourceCannotFillConsist()` возвращает `branchesDiffer` — имя обещает свойство
      источника, тело проверяет прокси-признак; согласовать имя и комментарий с телом
- [x] 10.14 Прогнать `make test` и `make verify` после правок
