## 1. Инструментарий

- [x] 1.1 Добавить `playwright` в `devDependencies` `web/package.json` и скрипт
  `test:visual` (`vitest run --config vitest.visual.config.ts`). Проверяемый результат:
  `npm ci` в чистом клоне не качает браузеры (у пакета нет install-скрипта; браузер ставится
  отдельно, `npx playwright install chromium`).
- [x] 1.2 Создать `web/vitest.visual.config.ts`: include `src/**/*.visual.test.ts`, без
  `setupFiles`, увеличенный `testTimeout` и `hookTimeout`. Проверяемый результат: конфиг
  запускается и находит файлы проверок.
- [x] 1.3 В `web/vite.config.ts` добавить `**/*.visual.test.ts` в `test.exclude` поверх
  `configDefaults.exclude` (импорт из `vitest/config`). Проверяемый результат: `npx vitest run`
  показывает прежнее число файлов и не пытается открыть браузер.
- [x] 1.4 Добавить в `Makefile` цель `check-visual` (зависит от `build`, запускает
  `npm run test:visual`) и включить её в `verify` вместо явного `build`. Проверяемый результат:
  `make check-visual` собирает `web/dist` и запускает прогон; `make verify` собирает один раз.
- [x] 1.5 Добавить `vitest.visual.config.ts` в `include` `web/tsconfig.node.json`, иначе
  `tsc -b` его не проверяет. Проверяемый результат: `npx tsc --noEmit -p tsconfig.node.json`
  видит файл и проходит.

## 2. Помощник запуска

- [x] 2.1 Написать помощник поднятия страницы (`web/src/__tests__/visual/harness.ts`):
  программный `preview()` из vite, запуск Chromium Playwright через `chromium.launch()`
  с учётом `CHROME_PATH`, фиксированный viewport 1440×900, чистый контекст, инъекция стиля,
  гасящего переходы и анимации, и закрытие всего в `afterAll`. Проверяемый результат: пробный
  файл открывает `/kit` и получает непустой `document.title`.
- [x] 2.2 Внятное падение вместо стека: нет `web/dist` — сообщение про `make build`; не найден
  Chrome — сообщение про `CHROME_PATH` и установку браузера. Проверяемый результат: оба случая
  воспроизводятся вручную и дают одну понятную строку.
- [x] 2.3 Написать сборщик вычисленных стилей, уходящий в `page.evaluate`: обход всех
  элементов, снятие `color`, `background-color`, цветов границ и обводки, `fill` / `stroke`,
  цветов внутри `background-image`, `text-shadow`, признаков прокрутки и короткого пути в DOM.
  Проверяемый результат: на `/kit` возвращается массив из сотен записей, путь читается глазами.
- [x] 2.4 Нормализация цвета в `#rrggbb`: `rgb()` / `rgba()`, пропуск полностью прозрачного,
  отметка полупрозрачного, падение на незнакомой форме записи. Юнит-тест на нормализатор рядом
  с остальными (обычный `vitest`, без браузера).
- [x] 2.5 Объявить множество допустимых цветов (`gradients` + `named`) в `web/src/skin.ts`,
  чтобы его брали обе проверки — и по правилам скина, и по отрисованной странице, — и завести
  список исключений «поддерево + цвет + причина» по образцу `ALLOWED` в
  `skin-palette.test.ts`. Проверяемый результат: множество непустое, размер совпадает с числом
  цветов в `opengfx2_palette.json`.

## 3. Разведка

- [x] 3.1 Прогнать сборщик по `/kit` во всех цветовых группах и по всем вкладкам, выписать
  цвета вне палитры в `.scratch/skin-browser-checks/found.md`: где встретился, кто рисует.
- [x] 3.2 Разнести найденное на два списка: рисует скин — чинится (группа 6); рисует сторонний
  отрисовщик по своим данным (graphviz, recharts, UA-стиль) — идёт в исключения с причиной.
  Проверяемый результат: у каждого цвета из 3.1 есть решение.

## 4. Проверки на странице элементов

- [x] 4.1 `palette.visual.test.ts`: ни одного цвета вне палитры на `/kit` во всех цветовых
  группах. Сообщение об ошибке называет путь, свойство и цвет.
- [x] 4.2 `shadow.visual.test.ts`: тень есть у всякого цветного текста и её нет ровно у текста
  цвета `TC_BLACK` и цвета `--skin-muted` / `--skin-disabled` текущей группы.
- [x] 4.3 `theme.visual.test.ts`: переключение групп через `SegmentedControl`, открытие
  выпадающего списка, показ подсказки и вызов уведомления; поверхность и текст портальных
  элементов сверяются с токенами выбранной группы.
- [x] 4.4 `dropdown.visual.test.ts`: у выбранного пункта списка фон `PC_BLACK`, вертикальные
  отступы нулевые, высота равна соседнему пункту.
- [x] 4.5 `disabled.visual.test.ts`: в `background-image` недоступной кнопки и недоступного
  поля присутствует цвет штриховки своего виджета (`--skin-button-hatch` / `--skin-hatch`)
  в каждой цветовой группе.

- [x] 4.6 Полосы прокрутки: снимать `scrollbar-color` и цвета `::-webkit-scrollbar*` у тех
  элементов, у которых полоса действительно есть (и у документа), и подавать их в ту же сверку
  с палитрой. Проверяемый результат: цвет вне палитры, назначенный ползунку, ловится.

## 5. Проверки по вкладкам

- [x] 5.1 `layout.visual.test.ts`: обход всех вкладок с чистым профилем; на каждой ровно один
  вертикальный скроллер — сам документ (исключение `/kit` по требованию спеки).
- [x] 5.2 В том же файле: прокручиваемые вбок контейнеры есть только у каталога машин и графа
  цепочек.
- [x] 5.3 `window-colour.visual.test.ts`: `data-window` на `<html>` и вычисленный
  `--skin-window` соответствуют таблице маршрутов — вкладка настроек в своей группе, остальные
  в базовой.
- [x] 5.4 Распространить проверку палитры (4.1) на обход вкладок, с исключениями из 3.2.

- [x] 5.5 Список вкладок для обхода строить из таблицы оболочки (`web/src/tabs.ts`, её же
  читает `App`), а не переписывать рядом. Проверяемый результат: вкладка, добавленная в
  таблицу и не описанная в проверках, роняет их с сообщением, что дописать.

## 6. Правки, которые найдёт проверка

- [x] 6.1 Починить в `web/src/skin.css` / `web/src/skin-mantine.css` расхождения из списка
  «рисует скин» (3.2). Новых требований к виду не вводить: правится только то, что расходится
  с уже зафиксированным в `ui-shell`. Проверяемый результат: проверки из групп 4–5 зелёные.

## 7. Документация

- [x] 7.1 Дописать в `CLAUDE.md` (раздел о скине): чем проверяется вид, что нужен системный
  Chrome, почему проверка не в CI и куда добавлять исключение.
- [x] 7.2 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском.

## 8. Проверка

- [x] 8.1 Прогнать `make test` (данные не меняются, `make data` не нужен) и `make check-visual`;
  оба зелёные. Убедиться, что `npx vitest run` не тянет браузер.
