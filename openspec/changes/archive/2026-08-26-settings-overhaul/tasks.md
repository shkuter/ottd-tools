## 1. Единый год расчёта

- [x] 1.1 Пометить `CalcSettings.priceYear` как единственный год приложения: комментарий у
  поля в `engine/settings.ts` объясняет, что он решает и список покупки, и цены при
  инфляции; проверка — `npx tsc -b` проходит, значение по умолчанию прежнее (1950)
- [x] 1.2 Удалить `year` из `SearchParams` (`state/searchParams.ts`) и `setYear` из
  `optimizerStore` / `industrySupplyStore`; проверка — сборка падает ровно в местах, где год
  читался, и после правки `npx tsc -b` чист
- [x] 1.3 Завести одно поле года на все вкладки (`components/YearField.tsx`): читает и пишет
  `calc.priceYear`, набранное коммитит по уходу из поля и по Enter, а не на каждое нажатие;
  подключить на вкладке подбора; проверка — тесты, что ввод года меняет настройку и что
  промежуточные годы (1, 19, 197) в неё не попадают
- [x] 1.4 Подключить то же поле на вкладке снабжения
  (`features/industry-supply/IndustrySupplyPage.tsx`); проверка —
  `features/industry-supply/__tests__/inputs.test.ts` зелёный после правки
- [x] 1.5 Убрать локальный `useState(1950)` из `features/consist/ConsistPage.tsx` в пользу
  того же поля; проверка — тест, что выставленный год каталога переживает перемонтирование
  страницы (сценарий «Год каталога переживает перезагрузку»)
- [x] 1.6 Добавить `version: 1` с миграцией, выбрасывающей осиротевший ключ `year`, в
  `optimizerStore` и `industrySupplyStore`; проверка — тест, что стор, поднятый из
  сохранённого состояния со старым `year`, этого поля не содержит

## 2. Наборы NewGRF выключены по умолчанию

- [x] 2.1 Поставить `ironHorse: false` и `firs: false` в `DEFAULT_GAME_SETTINGS`; проверка —
  тест на свежий калькулятор: активны ванильные машины и грузы, вкладка цепочек FIRS скрыта
- [x] 2.2 Поднять ранний выход `migrate` в `settingsStore` до `version >= 2` и выстроить
  ветки по возрастанию (`version < 1` — прежняя, `version < 2` — новая); проверка — тест,
  что состояние, сохранённое как v1, доходит до ветки v2, а не возвращается нетронутым
- [x] 2.3 Написать саму ветку `version < 2`: `ironHorse` и `firs` в `false`, остальное поле
  в поле как было; проверка — тест в `state/__tests__/settingsStore.test.ts` на сохранённое
  состояние v1 с включёнными наборами: после миграции наборы выключены, прочие настройки
  и разделённые множители running cost из v1 прежние
- [x] 2.4 Разобрать расхождения в тестах движка (`engine/__tests__/*`), опиравшихся на
  дефолтные наборы: тесту, которому нужен Iron Horse или FIRS, набор передаётся явным
  аргументом; проверка — `npx vitest run src/engine` зелёный, ни один эталон не переписан
  «под результат»
- [x] 2.5 То же для тестов вкладок и сторов (`features/*/__tests__/*`, `state/__tests__/*`);
  проверка — `npx vitest run src/features src/state` зелёный
- [x] 2.6 То же для тестов импорта и снапшота (`savegame/__tests__/*`,
  `features/savegame/__tests__/*`); проверка — `npx vitest run src/savegame` зелёный
- [x] 2.7 Добавить кейсы на `ironHorse` и `firs` в `engine/__tests__/settings-effect.test.ts`
  от новой базы (все наборы выключены); проверка — файл проходит и падает при возврате
  переключателя в мёртвое состояние
- [x] 2.8 Пересмотреть существующие кейсы того же файла, которым новая база ломает эталон.
  Сделано общей базой `SETS` (наборы включены) у `snapshot()`, а не индивидуальными
  `base` / `baseCalc`: без наборов мёртвым становится не только `firsEconomy`, но и
  большинство кейсов — им нужны машины и грузы этих наборов. Кейс, которому нужна ваниль
  (`basecostTrainRunningElectric`), гасит наборы сам; проверка — с пустым `SETS` падают 12
  кейсов, с наборами файл зелёный

## 3. Раздел настроек NewGRF

- [x] 3.1 Ввести признак вложенности у `Row` в `features/settings/SettingsPage.tsx` и класс
  `.setting-row--nested` в `index.css` + `skin.css` (токены `--skin-*`, без hex); проверка —
  `__tests__/skin-palette.test.ts` зелёный

- [x] 3.2 Собрать раздел NewGRF из трёх равноправных блоков: перенести переключатель Base
  Costs из `legend` отдельного `Fieldset` в общий раздел, множители — во вложенный блок под
  ним, сохранив существующее условие `game.basecostGrf`; проверка — отдельного `Fieldset`
  под Base Costs в разметке не осталось, а множители по-прежнему пропадают при выключенном
  наборе

- [x] 3.3 Показать экономику FIRS тем же приёмом вложенности, сохранив условие `game.firs`;
  проверка — сценарии «Экономика FIRS», «Множители Base Costs» и «Отключённый набор» из
  delta-спеки `ui-shell` выполняются на странице

- [x] 3.4 Проверить новый вид на отрисованной странице настроек во всех темах окна; проверка —
  `make check-visual` проходит (маршрут `/settings` в наборе визуальных проверок уже есть)

- [x] 3.5 Перенести индекс вместимости вагонов Iron Horse из раздела «Расчёт» в параметры
  своего набора: при выключенном Iron Horse он ни на что не влияет, а требование «Параметры
  отключённого набора SHALL NOT показываться» распространяется и на него; проверка — тест,
  что строка вложена под Iron Horse и пропадает вместе с ним

## 4. Импорт сохранения

- [x] 4.1 Тест на набор, отсутствующий в сохранении: `grfSettings()` предлагает его выключить,
  и после применения каталог ванильный; проверка — новый кейс в `savegame/__tests__` зелёный
- [x] 4.2 Тест на то, что перенесённый год виден на вкладках: после `applyImport` партии 1975
  года настройка года равна 1975 и списки покупки считаются на него; проверка — новый кейс
  зелёный

## 5. Подписи и документация

- [x] 5.1 Поставить `Base Costs` значением ключа `settings.basecostGrf` в `en.json` и
  `ru.json`; проверка — `i18n/__tests__/locales.test.ts` и `make check-i18n` зелёные

- [x] 5.2 Исправить подпись и подсказку года в обеих локалях: `settings.priceYearHint` сейчас
  утверждает «Имеет значение только при включённой инфляции», а год решает и список покупки;
  проверка — обе локали правлены, `locales.test.ts` зелёный

- [x] 5.3 Дописать запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском, с пометкой
  `**BREAKING**` на выключенных по умолчанию наборах и на миграции сохранённых настроек;
  проверка — `scripts/next-version.sh` считает разряд minor

- [x] 5.4 Починить запись нуля при очистке поля года: `clampGameYear` принимает значение поля
  как есть, потому что очищенный `NumberInput` отдаёт `''`, а `Number('')` — это 0, законный
  год диапазона. Нарушение существующего требования `pricing` «Очистка поля оставляет прежнее
  значение», задевающее и вкладку Settings; проверка — кейсы на `''`, пробелы и мусор в
  `settings-effect.test.ts` и тест на очищенное поле на вкладке

- [x] 5.5 Свести правку года в одно правило (`components/useYearField.ts`): все пять полей
  года — три на вкладках, год расчёта и год начала партии в настройках — коммитят по уходу из
  поля, Enter и размонтированию и не сужают диапазон; проверка — тесты, что год не теряется
  при переключении вкладки без расфокусировки, не пишется посимвольно ни на вкладке, ни в
  настройках, и что год вне 1860–2050 сохраняется
- [x] 5.6 Записать решения захода в `docs/adr/0004-the-calculator-assumes-nothing.md`:
  «наборы выключены по умолчанию» и «год один на калькулятор» переживут change и объясняют
  поведение, которое иначе выглядит произволом; проверка — ADR ссылается на change и назван в
  `design.md`

- [x] 5.7 Убрать предупреждение об Iron Horse и инфляции там, где Iron Horse выключен:
  фатальную ошибку GRF даёт набор, а ванильная партия с инфляцией законна, и она теперь
  конфигурация по умолчанию; проверка — тесты, что баннера нет без набора и что он есть с ним
- [x] 5.8 Увести прямые ссылки на вкладки FIRS туда, где есть что показать: при выключенном
  FIRS `/firs` рисовал граф набора, которым калькулятор не считает, а `/supply` — пустой
  список предприятий; проверка — тест, что обе ссылки приводят на главную вкладку

- [x] 5.9 Вынести `fixture()` из `savegame/__tests__/chunks.test.ts` в отдельный модуль:
  импорт из тест-файла заставлял десять файлов повторно прогонять его три теста (30 лишних
  прогонов); проверка — набор savegame идёт 127 тестов вместо 151, а сами тесты чанков
  по-прежнему выполняются

## 6. Проверка

- [x] 6.1 Прогнать `make test`, затем `make verify`; проверка — оба зелёные, включая
  `make check-visual`
