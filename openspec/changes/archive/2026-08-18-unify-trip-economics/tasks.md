## 1. Подготовка

- [x] 1.1 Снять снимок чисел оптимизатора (круг, рейсы, доход, содержание, прибыль,
      окупаемость, `cargoPerTrip`, `trainsNeeded`, интервал, рейтинг) для нескольких грузов,
      дистанций, объёмов производства и наборов настроек — во временный файл в scratchpad

## 2. Общий модуль

- [x] 2.1 Создать `web/src/engine/trip.ts`: `tripEconomics(params)` и `tripLoadingDays()`
      с моделью из design.md
- [x] 2.2 Перевести `evaluate` в `web/src/engine/optimize.ts` на `tripEconomics()`, оставив
      в оптимизаторе только перебор, `cargoPerTrip`/`trainsNeeded`, рейтинг и отбор
- [x] 2.3 Удалить блок `profit` из `web/src/features/route/RoutePage.tsx`, заменив на вызов
      `tripEconomics()` с `loadedDaysOverride: route.manualDays`

## 3. Проверка

- [x] 3.1 Сверить снимок 1.1 с числами оптимизатора после правки — расхождений нет
- [x] 3.2 Написать `web/src/engine/__tests__/trip.test.ts`: порожнее плечо быстрее гружёного;
      стоянка по самому медленному вагону и не зависит от числа вагонов; `gradualLoading=false`
      → нулевая стоянка; длина дня 2 → рейсов вдвое больше; ручное время при равных скоростях
      даёт равное порожнее плечо; убыточный состав → `paybackYears === null`
- [x] 3.3 Проверить в браузере (dev-сервер): вкладка «Доход рейса» показывает круг и прибыль
      для перенесённого из оптимизатора состава, совпадающие с таблицей оптимизатора
- [x] 3.4 Дописать `## [Unreleased]` → `Changed` в `CHANGELOG.md` (по-английски), отметив
      изменение чисел на вкладке рейса
- [x] 3.5 `make test`, `npx tsc --noEmit`, `npx oxlint` — без новых замечаний
