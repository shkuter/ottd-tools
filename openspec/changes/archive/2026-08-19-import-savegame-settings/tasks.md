## 1. Модель цен: раздельные множители содержания

- [x] 1.1 Заменить `basecostTrainRunning` в `GameSettings` на `basecostTrainRunningSteam`,
      `basecostTrainRunningDiesel` и `basecostTrainRunningElectric`; `basecostRunningFactor()`
      принимает running-класс машины
- [x] 1.2 Прокинуть выбор множителя в `engine/costs.ts` по тому же `running_cost_base`, по
      которому выбирается базовая цена содержания
- [x] 1.3 Расширить `BASECOST_MULTIPLIERS` до 16k / 32k / 64k (шкала Base Costs Mod 5.0)
- [x] 1.4 Добавить кейсы в `web/src/engine/__tests__/settings-effect.test.ts` на все три новые
      настройки (паровой множитель меняет содержание движка, дизельный — вагона,
      электрический — ванильного электровоза)
- [x] 1.5 Миграция `persist` в `state/settingsStore.ts`: старое `basecostTrainRunning`
      переносится во все три новых множителя; тест на то, что мигрированное состояние даёт
      прежние числа
- [x] 1.6 UI настроек: три поля вместо одного, строки в `i18n/en.json` и `ru.json`
      (названия — из локали игры)

## 2. Ридер сейвов

- [x] 2.1 Добавить зависимости `xz-decompress` и `lzo1x`; проверить, что `optionalDependencies`
      пакета `lzo1x` не попадают в сборку SPA
- [x] 2.2 `savegame/reader.ts`: поток байтов — gamma-длины, целые, строки; юнит-тесты на
      граничные значения gamma (1, 2, 3, 4 и 5 байт)
- [x] 2.3 `savegame/decompress.ts`: определение тега, ветки `OTTN` / `OTTZ` (нативный
      `DecompressionStream`) / `OTTX` (ленивый импорт `xz-decompress`) / `OTTD` (блочная
      обёртка игры поверх `lzo1x`); тест на каждый формат
- [x] 2.4 `savegame/chunks.ts`: обход чанков, пропуск незнакомых по правилам формата,
      поддержка `CH_EXT_HDR` из JGRPP, разбор табличного заголовка в список полей
- [x] 2.5 Отклонение неподдерживаемых файлов: чужой тег, `PATS` не табличный, неизвестный
      тип чанка — с текстами ошибок через `t()`
- [x] 2.6 Синтетические фикстуры: round-trip на `OTTN`, `OTTZ` и `OTTD` (энкодера xz в
      наборе нет, поэтому `OTTX` проверяется настоящим сейвом из задачи 2.7) и подделанный
      позиционный `PATS` для проверки границы версий
- [x] 2.7 Фикстура сыгранной партии `londworth-1975.sav` (`OTTX`, 448 КБ: 1649 машин,
      144 списка приказов, 100 станций, 1 субсидия) и smoke-тест: файл разбирается целиком,
      тяжёлые чанки пропускаются без ошибок, число записей в них совпадает с ожидаемым

## 3. Извлечение данных

- [x] 3.1 `savegame/extract/pats.ts`: имя настройки → значение, включая массивы и строки
- [x] 3.2 `savegame/extract/ngrf.ts`: активные GRF с grfid, версией и параметрами
- [x] 3.3 `savegame/extract/date.ts` и `extract/ecmy.ts`: год партии и накопленные множители
      инфляции (числа с фиксированной точкой, 65536 = ×1) — множители идут только в
      справочный список
- [x] 3.4 `savegame/mapping.ts`: таблица «имя настройки в игре → поле `GameSettings` /
      `CalcSettings` + преобразование значения», плюс список справочных настроек
- [x] 3.5 Реестр известных наборов: grfid → роль набора и, если разбирали, карта параметров —
      Iron Horse (param 0 → `capacityIndex`), FIRS (param 0 → экономика), Base Costs Mod 5.0
      (параметры 15, 16, 42, 43, 44 → множители, множитель = 2^(значение − 8), нижняя граница
      1/256); набор с известной ролью, но без карты параметров попадает в справочный список,
      набор вне реестра не влияет ни на одно значение
- [x] 3.6 Положить в репозиторий фикстуру `londworth-1860.sav` (OTTX/JGRPP, 213 КБ, начало
      той же партии: 0 машин, 0 списков приказов) и записать ожидаемые значения; тест сверяет весь путь
      распаковка → чанки → значения: `jgrpp` = да, `dayLengthFactor` = 5,
      `startingYear` = 1860, `vehicleCosts` = high, `constructionCost` = high,
      `inflationInterest` = 4, `priceYear` = 1860, `capacityIndex` = 2 (Iron Horse param 0),
      экономика FIRS = steeltown (param 0 = 3), Base Costs: покупка локомотивов ×2 и вагонов
      ×2 (параметры 15 и 16 = 9), содержание по всем трём running-классам ×1 (параметры 42, 43, 44 = 8)

## 4. Экран импорта

- [x] 4.1 Разбор в Web Worker с динамическим импортом модуля `savegame/`
- [x] 4.2 Выбор файла и состояния «читаем / ошибка / готово» на вкладке настроек
- [x] 4.3 Список различий: применяемые значения (текущее → из сейва) и справочный блок
      (настройки без модели, накопленная инфляция партии, опознанные наборы без карты
      параметров); случай «расхождений нет»
- [x] 4.4 Применение по подтверждению: `settingsStore` плюс синхронный выбор экономики FIRS
      в `firsStore` и `routeStore`
- [x] 4.5 Строки экрана в `i18n/en.json` и `ru.json`, включая названия справочных настроек
- [x] 4.6 Тест стора: подтверждение применяет значения, отказ оставляет настройки прежними

## 5. Завершение

- [x] 5.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском
- [x] 5.2 Прогон `make test` и `make verify`
