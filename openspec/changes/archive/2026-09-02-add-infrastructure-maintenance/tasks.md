## 1. Данные: множители обслуживания путей

- [x] 1.1 Добавить множитель обслуживания типа пути в `pipeline/extract_vanilla.py`
      (`table/railtypes.h`) и проверить, что `vanilla_trains.json` даёт 8 / 12 / 16 / 24 для
      RAIL / ELRL / MONO / MGLV
- [x] 1.2 Добавить множитель обслуживания в `pipeline/extract_iron_horse.py`, читая
      **атрибут** `railtype.maintenance_cost` объекта (а не объявленный kwarg), и проверить,
      что у узкоколейки в `trains.json` стоит 5, а не 7 — как уходит в GRF
- [x] 1.3 Подставлять ванильный множитель по лейблу типу, у которого набор оставил `None`
      (RAIL / ELRL Iron Horse), и проверить сравнением двух наборов, что значения совпали
- [x] 1.4 Дописать поле в `pipeline/validate.py` (обязательное, положительное) и убедиться,
      что `make data` проходит
- [x] 1.5 Закрепить множители тестом `pipeline/tests/test_known_values.py` (ваниль: 8 и 12;
      Iron Horse: узкоколейка 5, метро 10, LGV 16) и прогнать его точечно

## 2. Настройки

- [x] 2.1 Добавить `infrastructureMaintenance` и `linearMaintenance` в `GameSettings`
      (`engine/settings.ts`) со значениями по умолчанию как в игре; проверить, что
      `npx vitest run engine` зелёный
- [x] 2.2 Новые настройки приходят к старым пользователям выключенными: ветка `migrate` не
      нужна — `merge` стора уже достраивает поля из дефолтов, версия не бампается (смысл
      сохранённого не менялся); тест на состояние, сохранённое каждой прошлой версией
- [x] 2.3 Кейс на `infrastructureMaintenance` в `engine/__tests__/settings-effect.test.ts`
- [x] 2.4 Кейс на `linearMaintenance` там же, со своей базой (`base`), чтобы кейс не
      «проходил» за счёт соседнего флага
- [x] 2.5 Множитель Base Costs для цен инфраструктуры (`basecostInfrastructure`): поле,
      контрол в блоке Base Costs на вкладке настроек, свой кейс в `settings-effect.test.ts`
- [x] 2.6 Обе настройки партии — на вкладку настроек (линейный рост под флагом `jgrpp`),
      проверить, что расчёт панели идёт по ним

## 3. Движок

- [x] 3.1 Добавить базовые цены обслуживания (путь 10, дорога 10, вода 8, станция 100) в
      `BASE_PRICES` (`engine/costs.ts`) и убедиться, что существующие тесты цен не сдвинулись
- [x] 3.1a Общий хелпер базовой цены (`basePriceAfterMultipliers`) с клампом нуля к единице,
      как в `economy.cpp RecomputePrices`; тест на множитель Base Costs 1/16
- [x] 3.2 Написать в `engine/infrastructure.ts` `IntSqrt` игры (округление к ближайшему) и
      выбор множителя роста по настройке; проверить `IntSqrt` таблицей значений, включая
      числа, где округление вверх и вниз расходятся
- [x] 3.3 Реализовать месячную стоимость категории целыми: сдвиги через
      `Math.floor(x / 2 ** n)`, множители типов путей из данных набора, ванильная таблица
      множителей дорог; проверить тестом на одной категории против формулы исходников
- [x] 3.4 Собрать категории (путь по типам, сигналы, станции, дороги и трамвай по типам,
      каналы) и годовую сумму `monthly × 12`; размер сети — общий по путям, раздельный по
      дорогам и трамваям, собственный у сигналов, станций и каналов; проверить тестом, где
      непусты дорога и трамвай одновременно
- [x] 3.5 Тест на восьми эталонных строках партии — все совпадают до целых
- [x] 3.6 Тест на усечение малых количеств: 6 клеток пути дают 10 800 в год, а не 11 137
- [x] 3.7 Тест ванильной ветки против формулы исходников на синтетических количествах, в том
      числе на числе, где `IntSqrt` округляет вверх
- [x] 3.8 Тест на большой сети с множителем Base Costs — результат не обрезан до int32
- [x] 3.9 Тест: множитель сложности расходов транспорта меняет суммы, строительной — нет;
      множитель инфляции цен общий с ценами машин

## 4. Вкладка «Доход рейса»

- [x] 4.1 Добавить количества в `state/routeStore.ts` (поля + `merge`, чтобы старое
      сохранение получило пустую сеть); тест `routeStore.test.ts` на обе ветки
- [x] 4.2 Блок «Обслуживание сети» на вкладке: поля количеств рельсовых категорий, строка на
      каждую непустую категорию, годовой итог; проверить, что итог равен сумме строк и что
      пустые категории не показываются
- [x] 4.3 Показ «статья выключена» при выключенной настройке; проверить, что суммы нулевые
- [x] 4.4 Строки интерфейса в `en.json` / `ru.json`: «Обслуживание инфраструктуры» — из
      русской локали игры (`russian.txt:1659`), «Линейный рост стоимости обслуживания» — наше
      имя (русской строки у JGRPP нет); проверить `i18n/__tests__/locales.test.ts`

## 5. Импорт сохранения

- [x] 5.1 Читать `economy.infrastructure_maintenance` и `economy.linear_maintenance` из
      сохранения; отсутствующая настройка (партия не на JGRPP) читается как выключенный
      линейный рост — проверить тестом парсера на сейве без неё
- [x] 5.2 Показывать обе среди переносимых настроек и применять при подтверждении; проверить
      тестом вкладки «Партия»
- [x] 5.3 Предзаполнение количеств из партии и сверка итога с окном инфраструктуры вынесены
      в отдельный change (решение 02.09.2026): сохранение счётчиков не содержит — игра
      пересчитывает их обходом карты при загрузке (`sl/company_sl.cpp`
      AfterLoadCompanyStats), а карту тайлов импорт не читает вовсе

## 6. Завершение

- [x] 6.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском (Added)
- [x] 6.2 Прогнать `make data` и `make test`; оба зелёные
