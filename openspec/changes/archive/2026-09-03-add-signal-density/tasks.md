## 1. Настройки партии

- [x] 1.1 Добавить в `GameSettings` (`web/src/engine/settings.ts`) поля `brakingModel`
      (`'original' | 'realistic'`, дефолт `'original'`) и `trainAccBrakingPercent` (5…200,
      дефолт 100) с JSDoc на исходники JGRPP, плюс хелпер `realisticBrakingApplies(game)` рядом
      с `linearMaintenanceApplies`; проверка — `npx vitest run` в `web/` проходит, а хелпер вне
      JGRPP возвращает `false` при сохранённом `'realistic'`
- [x] 1.2 Показать обе настройки в JGRPP-секции `web/src/features/settings/SettingsPage.tsx`
      (`Select` для модели по образцу модели ускорения, `NumberInput` для коэффициента
      масштабирования); проверка — вне JGRPP строк нет, внутри переключение меняет сохранённое
      состояние
- [x] 1.3 Перенести `vehicle.train_braking_model` из `INFO_SETTINGS` в читаемые настройки
      `web/src/savegame/mapping.ts` и убрать ключи `savegame.info.trainBrakingModel/
      brakingOriginal/brakingRealistic` из обоих словарей; проверка —
      `web/src/savegame/__tests__/extract.test.ts` видит настройку среди переносимых
- [x] 1.4 Завести чтение `vehicle.train_acc_braking_percent` в `savegame/mapping.ts` **с нуля**:
      настройки нет ни в переносимых, ни в справочном списке; отсутствие в сохранении читается
      как 100 %; проверка — тест извлечения на сейве с иным значением и на сейве без него

## 2. Тормозной путь в физике

- [x] 2.1 Добавить `brakingDeceleration(c, game, accelerationType)` в
      `web/src/engine/physics.ts` с обеими ветками модели ускорения (формулы — раздел 2
      design.md); `airDrag()` остаётся внутренней — она нужна только этому модулю; проверка —
      юнит-тест на ветку `original` (деление до умножения, как в игре) и на предел
      `cached_deceleration` 120/168/216 по типу пути
- [x] 2.2 Добавить `brakingDistanceTiles(c, game, speedInternal, descentUnits, accelerationType)`
      — `v²/(32×decel)`, перепад в мировых единицах игры;
      проверка — сверка единиц с комментарием игры (замедление 130 и 160 км/ч дают ≈6,2 клетки)
      и путь на подъёме **равен** пути по ровной линии
- [x] 2.3 Добавить ветку спуска через uncapped-замедление, гейтя её реалистичной моделью
      ускорения (`train_cmd.cpp:912`); проверка — на спуске путь длиннее при `accelerationModel
      = 'realistic'` и равен ровному при `'original'`
- [x] 2.4 Извлечь `acceleration_type` пути экстрактором (`table/railtypes.h`, ваниль: 0/0/1/2;
      Iron Horse объявляет один ACC_MODEL_RAIL на все свои типы — тот же обычный, 0) и считать по нему предел замедления и ветку машин
      на магнитной подвеске (`power_w / 25`, `train_cmd.cpp:1334-1339`); проверка — тесты
      пайплайна на значения поля и тест движка на три разных предела
- [x] 2.5 Учесть множитель массы грузов (`freightTrains > 1`) в тормозной длине состава —
      только в ветке `AM_REALISTIC`, как в игре (`train_cmd.cpp:1288-1298`); проверка — тест:
      при реалистичной модели ускорения множитель 2 меняет путь, при оригинальной — нет

## 3. Движок плотности

- [x] 3.1 Создать `web/src/engine/signals.ts` со счётом полезного интервала: реалистичная модель —
      тормозной путь + длина состава + 1,5 клетки обзора, оригинальная — длина состава;
      проверка — тест на обе ветки и на то, что интервал больше тормозного пути
- [x] 3.2 Добавить `signalPlan(...)`: текущий интервал, рекомендованное число сигналов,
      годовое содержание сейчас и при рекомендации той же моделью содержания
      (`categoryMonthlyCost`, множитель сигналов открыт из `infrastructure.ts`), экономия,
      флаг разреженности; проверка — тест на содержание партии (1 612 → 723 = 11 345 400) и на
      то, что предлагает сама модель (738 голов, 11 154 000)
- [x] 3.3 Обработать края: сеть без длины не считается, сеть без сигналов считается, статья
      обслуживания выключена — нули и причина; проверка — по тесту на каждый край

## 4. Состояние и интерфейс

- [x] 4.1 Завести в `web/src/state/routeStore.ts` поле «худший спуск, уровней высоты» (дефолт 0)
      с доливкой через существующий `merge`; проверка — `state/__tests__/routeStore.test.ts`:
      состояние, сохранённое без поля, читается с нулём
- [x] 4.1a Сказать в блоке, что при оригинальной модели ускорения введённый спуск в расчёте не
      участвует; проверка — UI-тест: при `accelerationModel = 'original'` подсказка видна, а
      тормозной путь не меняется от значения спуска
- [x] 4.2 Создать `web/src/features/route/SignalDensity.tsx` по образцу `CorridorUpgrade.tsx`
      (поле спуска, таблица: скорость, с которой считается торможение, тормозной путь, полезный
      интервал, текущий интервал, рекомендованное число, содержание сейчас → при рекомендации,
      экономия) и смонтировать третьим блоком в `RoutePage.tsx`; проверка — блок виден на
      вкладке, скорость показана в единице настроек и совпадает с панелью выше
- [x] 4.3 Добавить предупреждение о разреженности, объяснение для оригинальной модели
      торможения и правило «интервал вдоль пути = куски между соседними головами»; проверка —
      UI-тест: при интервале вдвое реже полезного показано предупреждение и не обещана экономия
- [x] 4.4 Добавить класс `.route-signals` в `web/src/skin.css` рядом с `.route-network`;
      проверка — `make check-visual` проходит, блок стоит в той же колонке
- [x] 4.5 Выровнять ряды полей по инпутам (`align-items: flex-end` у `.network-inputs`):
      подпись в две строки сдвигала своё поле вниз во всех трёх панелях вкладки; проверка —
      в браузере верх всех полей ряда совпадает, `make check-visual` проходит
- [x] 4.6 Вынести строку сводной таблицы в `components/SummaryRow.tsx` вместо второй копии
      локального `Row`; проверка — оба блока рисуют таблицы как раньше, тесты панелей зелёные

## 5. Строки

- [x] 5.1 Добавить группу `signals.*` в `web/src/i18n/en.json` и `ru.json` (структура — как
      `corridor.*`: заголовок, метки полей, правило счёта, `need*`, строки таблицы, подсказка);
      проверка — `i18n/__tests__/locales.test.ts` проходит
- [x] 5.2 Назвать настройки дословно как игра: «Модель торможения поездов» / «Коэффициент
      масштабирования ускорения/торможения поезда», варианты «оригинальное» и «реалистичное»
      (`vendor/openttd-patches/src/lang/extra/russian.txt:94,130,139`,
      `vendor/openttd/src/lang/russian.txt:1446`); в подсказке коэффициента сказать, что расчёт
      берёт только тормозную половину, а разгон в модели рейса не участвует; проверка — строки
      совпадают с локалью игры дословно

## 6. Тесты и приёмка

- [x] 6.1 Написать `web/src/engine/__tests__/signals.test.ts`: сверка единиц тормозного пути,
      тяжёлый состав тормозит дольше, спуск удлиняет только при реалистичной модели ускорения,
      маглев считается своей веткой, экономия партии 11 345 400, обе модели торможения дают
      разные интервалы; проверка — набор зелёный
- [x] 6.2 Добавить кейсы `brakingModel` и `trainAccBrakingPercent` в
      `web/src/engine/__tests__/settings-effect.test.ts` со своей базой и строкой `signals:` в
      снимке; проверка — тест падает, если настройку не прокинуть в расчёт
- [x] 6.3 Написать `web/src/features/route/__tests__/SignalDensity.test.tsx` по образцу
      `NetworkMaintenance.test.tsx`; проверка — набор зелёный
- [x] 6.4 Сверить полезный интервал на данных партии и записать полученные числа в тест;
      расхождения с исходной карточкой не вышло: длинный состав партии (два локомотива и сорок
      вагонов, 11 клеток) даёт интервал 13,8 клетки, то есть диапазон 12–16 из карточки, а на
      сети партии модель просит 738 голов и обещает 11 154 000 — два процента от того, что
      игрок нашёл вручную (723 головы, 11 345 400). Короткий состав просит сигналы чаще, и это
      не противоречие: полезный интервал принадлежит поезду, а не сети; проверка — все три
      случая зафиксированы тестом

## 7. Документация и закрытие

- [x] 7.0 Описать блок в `README.md` и `README.ru.md` одним коммитом, тем же абзацем, что
      описывает соседние панели вкладки; проверка — оба файла называют его одинаково
- [x] 7.1 Добавить в `CONTEXT.md` рядом с «Track piece» и «Load threshold» термины
      **полезный интервал** (расстояние между сигнальными головами, ниже которого уплотнение
      перестаёт окупаться) и **тормозной путь**, оговорив соотношение клеток вдоль пути и
      кусков; проверка — термины употребляются в спеке и UI-строках в том же значении
- [x] 7.2 Дописать запись в `CHANGELOG.md`, секция `## [Unreleased]` → `### Added`, на
      английском; проверка — запись описывает блок и две настройки
- [x] 7.3 Прогнать `make verify` (data + check-i18n + тесты + check-visual); проверка — всё
      зелёное
