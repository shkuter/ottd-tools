## 1. Общий груз

- [x] 1.1 `state/upgrade.ts`: ступень 2 `carryConsistCargoIntoRoute` по D3 (`UPGRADE_VERSION = 2`, лесенка `if (version < 2)`); проверка — `state/__tests__/upgrade.test.ts`: груз только у конструктора переносится, при сохранённом грузе рейса ничего не меняется, чистый профиль не падает, проверка через живой `useRouteStore` после `import('../upgradeOnLoad')`; тест совпадения ключей расширен consist и route
- [x] 1.2 `state/consistStore.ts`: убрать `cargoLabel`/`setCargoLabel` из состояния, действий и `partialize` (D2); проверка — `consistStore.test.ts` обновлён, сохранённый JSON со старым полем гидратируется без ошибок
- [x] 1.3 `ConsistPage.tsx`: панель состава читает и пишет груз рейса через `useActiveCargo`, вместимость — по нему; убрать сброс в `null` и пустой выбор (`allowDeselect={false}`, ключ `consist.none` — удалить, если не используется); проверка — новый `features/consist/__tests__/sharedCargo.test.tsx`: смена груза на панели меняет `routeStore.cargoLabel`, и наоборот; груз вне набора на панели заменяется первым
- [x] 1.5 Дельта-спека `cargo-sets` («Груз выпал там, где он необязателен») по D2 — входит в артефакты change; проверка — `openspec validate ui-flow-safety --strict`
- [x] 1.4 Все писатели груза (`OptimizerPage` «→», `applyBridge.ts` мосты маршрута и графа) пишут один груз в `routeStore`; проверка — `applyBridge.test.ts` и тест «→» ниже зелёные, `grep setCargoLabel` показывает только `routeStore`

## 2. Отмена замены состава

- [x] 2.1 `state/consistReplacement.ts`: `captureConsistAndRoute`, `restoreConsistAndRoute`, `replaceConsist` по D1; проверка — стор-тест: снимок → запись нового → восстановление возвращает entries и все поля рейса; пустой прежний состав → `null`
- [x] 2.2 `components/consistReplacedNotice.tsx`: уведомление со своим `id` на каждую замену (прежнее скрывается явно перед показом) и хелпер `replaceConsistWithUndo`, через который идут все пути замены, `autoClose` 10 с, кнопкой «Вернуть прежний состав»; проверка — юнит-тест с `<Notifications/>`: кнопка есть, щелчок восстанавливает и скрывает уведомление; после второй замены на экране одно уведомление, и его кнопка возвращает состав, бывший до второй замены; с fake timers через 10 с уведомления нет
- [x] 2.3 Подключить к «→» оптимизатора, мосту маршрута в `RoutesTab` и кнопке очистки `ConsistPage` (заменив `notify.consistCleared`); проверка — тесты: «→» поверх непустого состава показывает уведомление и отмена возвращает состав и рейс; поверх пустого — уведомления нет; мост маршрута — то же; очистка — то же
- [x] 2.4 Визуальная проверка: уведомление с кнопкой в стиле игры — рамка окна, кнопка с плашкой `--skin-button`, никаких цветов вне палитры; проверка — `make check-visual`

## 3. Подтверждение сброса

- [x] 3.1 Перенести `.btn-danger` в `skin-mantine.css` (`.mantine-Button-root.btn-danger`, hover/active) на токене убытка (D4); проверка — визуальная: computed цвет кнопки «Сбросить всё» равен `--skin-loss`, а не цвету текста кнопки; `skin-palette.test.ts` зелёный
- [x] 3.2 Окно подтверждения сброса в `SettingsPage` по D4 (перечень, «Отмена», опасная кнопка, партия — только если импортирована); проверка — `reset.test.tsx`: первое нажатие ничего не стирает и открывает окно; «Отмена»/Esc закрывают без изменений; подтверждение стирает и перезагружает; без партии она не названа

## 4. Переходы и подписи

- [x] 4.1 i18n en/ru по D5 (`opt.apply`, `game.toIncome`, `combined.needConsist`, `firs.*` про Supply/Game, новые ключи ссылок, уведомления и окна сброса, «Открыть партию»); проверка — `locales.test.ts` зелёный и юнит-тест: en-словарь не содержит «Consist tab», «Supply tab», «Game tab», «Open in Profitability»
- [x] 4.2 `ConsistPage`: ссылка «Посчитать доход рейса →» под итогами непустого состава; проверка — тест: при непустом составе ссылка ведёт на `/income`, при пустом её нет
- [x] 4.3 `TableFrame.emptyMessage` → `ReactNode`; `RoutePage`: пустая прибыльность при пустом составе ведёт ссылкой на «Конструктор состава» внутри рамки пустого состояния; проверка — тест: ссылка на `/consist` с названием вкладки из `nav.consist` внутри `.table-empty`

## 5. Импорт и вкладка партии

- [x] 5.1 `SavegameImportResult`: кнопки «Закрыть» и «Открыть партию» фазы `saved` в `.window-actions` (D6), в окне и на экране настроек; проверка — `SavegameImportLauncher.test.tsx`: после Apply кнопка открывает `/game` и закрывает окно; `SavegameImportPanel.test.tsx` обёрнут в `MemoryRouter` и проверяет кнопку
- [x] 5.2 `features/savegame/game.ts` `differingSettings`: пропуск множителей Base Costs при выключенном наборе на обеих сторонах (D7); проверка — `game.test.ts`: оба выключены ×1/×2 → не названы; текущий включён ×2 → названы

## 6. Завершение

- [x] 6.1 `CHANGELOG.md` → `[Unreleased]`: новая секция `### Added` (отмена замены состава, подтверждение сброса, переход к партии, ссылки между вкладками); в уже существующие `### Changed` (общий груз состава и рейса, панель состава без пустого выбора) и `### Fixed` (ложное расхождение Base Costs, подписи вкладок, опасная кнопка) — дописать, не дублируя заголовки; на английском; проверка — `scripts/next-version.sh` выдаёт minor
- [x] 6.2 Прогнать `make test`, `make check-i18n`, `cd web && npx tsc -b && npx oxlint src` и `caffeinate -i make check-visual` (без параллельных браузеров); проверка — всё зелёное
