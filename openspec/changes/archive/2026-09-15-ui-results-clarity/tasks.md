## 1. Best train: колонка цели, «Ранжировано по», подсказка выпуска

- [x] 1.1 `OptimizerPage`: колонка числа действующей цели сразу после Wagons в `thead` и `tbody`, Supply на месте (D1); проверка — `OptimizerPage.test.tsx`: для profit, cheapest и transported ячейка `opt-goal-figure` идёт сразу после второй ячейки вагонов, число `td` в строке и `th` (с учётом `colSpan`) совпадает; `equivalence.test.tsx` зелёный
- [x] 1.2 Строка `data-testid="ranked-by"` над таблицей по `activeGoal` (D1), комментарий у `better` в `engine/optimize.ts` ссылается на `opt.rankedBy.*`; проверка — тест: текст для каждой из четырёх целей называет показатель цели и ведущие ключи порядка (прибыль, цена покупки, вагон, размер флота — в порядке `better`), хвост из числа вагонов и идентификаторов не называет; при выбранном «Вывозе» и выпуске 0 строка называет прибыль; пересортировка по колонке строку не меняет
- [x] 1.3 Текст `opt.goalNeedsProduction` говорит «Output»/«Выпуск» и звучит как приглашение («задайте выпуск, чтобы ранжировать по …»); разметка не меняется — подсказка уже `<p className="hint goal-hint">` (`OptimizerPage.tsx` ~584); проверка — `OptimizerPage.test.tsx`: текст причины содержит название поля `opt.production` и не содержит «production»/«производств»; регэкспы `/needs the industry output/i` на ~284 и ~290 заменены новым текстом; `title`/`aria-describedby` на опциях прежние

## 2. Закрепление ранга и локомотива

- [x] 2.1 `components/table/`: хук `usePinnedLeadingColumns` и проп `TableFrame pinLeading` (D2), CSS `.pin-leading`/`.pin-lead`/`.pin-lead-last` в `skin-mantine.css`, правила `:first-child` ограничены `:not(.pin-leading)`; проверка — юнит-тест хука на jsdom с заданными `getBoundingClientRect`: накопленные `left` по столбцам с учётом `colSpan`, класс края у ячейки N-го столбца, откат на один столбец при сумме > половины ширины — у столбцов 2..N сняты `left` и `.pin-lead`, край у первого; обратный переход при расширении
- [x] 2.2 Best train: `<TableFrame pinEdges pinLeading={4}>`; проверка — `table.visual.test.ts` для `/optimizer`: тест меряет сумму ширин четырёх первых столбцов `S` и ставит рамке две ширины от порога `2·S` — заметно шире (например `2·S + 200px`) и заметно уже (`2·S − 200px`, но не меньше ширины первого столбца с запасом); в широкой номер, отметка, спрайт и имя не сдвигаются при прокрутке, не перекрывают друг друга, бевель только у имени, последняя колонка держится, после сортировки и «показать ещё» `left` пересчитан; в узкой держится только номер, у ячеек столбцов 2..4 нет инлайн-`left` и `.pin-lead`, бевель у номера; возврат к широкой восстанавливает четыре колонки; `list.visual.test.ts` зелёный
- [x] 2.3 Специмен списка с `pinLeading` на `/kit` (`KitPage.tsx`) с устойчивым признаком разметки; проверка — `kit.visual.test.ts` и `table.visual` находят его и проходят в нём ту же проверку прокрутки

## 3. «Как считается»

- [x] 3.1 `state/uiStore.ts` (ключ `ottd-tools-ui`, `version: 1`, вне `PERSISTED_STORES`) и `components/HowComputed.tsx` (кнопка с `aria-expanded`/`aria-controls`, `<Collapse expanded={open}>` с умолчаниями `keepMounted`/`activity`, без `inert`) по D3; проверка — `components/__tests__/HowComputed.test.tsx`: свёрнут по умолчанию — текст в DOM, но `not.toBeVisible()` и обёртка `aria-hidden="true"`; Enter/Space раскрывают — текст `toBeVisible()`, `aria-expanded` меняется; Tab не заходит в свёрнутый текст (ссылка внутри не получает фокус); записанный ключ несёт `version: 1`; открытость двух id хранится раздельно и переживает повторную гидратацию стора; `resetPersistedState()` ключ `ottd-tools-ui` не стирает
- [x] 3.2 Best train и Route income: абзацы по списку D3 уходят в `HowComputed id="optimizer"` под таблицей и `id="route"` в конце панели прибыльности; проверка — тесты страниц: по умолчанию текст `opt.assumption*`, легенды «?» и `combined.assumptions` `not.toBeVisible()` (он остаётся в DOM — `queryByText(...) === null` не годится), после раскрытия `toBeVisible()`; `goal-hint`, `vehicle.listFromGame`, флажки исключения и `route.networkNote` видны без раскрытия (`NetworkNote.test.tsx` зелёный)
- [x] 3.3 Network: `HowComputed` в конце каждого из трёх блоков по D3, короткое правило `network.pieceRuleShort` описанием поля длины коридора и полей кусков пути в блоке обслуживания; проверка — `NetworkMaintenance/CorridorUpgrade/SignalDensity.test.tsx`: пояснения (`network.hint`, `corridor.pieceRule`/`assumptions`, `signals.spacingRule`/`originalModel`) по умолчанию `not.toBeVisible()`, после раскрытия `toBeVisible()`; `corridor.manualDays`, `signals.descentInactive`, `network.disabled`, сообщения о недостающем вводе и `network.pieceRuleShort` у поля длины коридора и у каждого поля кусков пути `toBeVisible()` без раскрытия
- [x] 3.4 Вид блока в `skin.css` на токенах скина и специмен на `/kit`; проверка — `make check-visual`: `kit.visual`, `palette.visual`, `focus.visual` зелёные, у заголовка виден фокус, цвета — из токенов группы окна

## 4. Network: вывод первым и ширины

- [x] 4.1 `CorridorUpgrade`: `yearlyDelta` первой строкой; `SignalDensity`: `saving` первой (D4); проверка — тесты блоков: первая строка `tbody` — годовая дельта / экономия, остальные строки в прежнем порядке
- [x] 4.2 `.network-inputs`: `data-width` по шкале вместо `width: 140px`, ступени полей по D4 во всех трёх блоках; проверка — `controls.visual` и `clipping.visual` в en и ru: ширины равны токенам шкалы, значение с единицей не обрезано

## 5. Деньги, Supply, график, время рейса

- [x] 5.1 `money()` с минусом U+2212 перед всей записью, `-0` без знака (D5); `holdsFigures` в `list.visual.test.ts` можно не трогать (денежные ячейки опознаются по `cell-money`); проверка — `format.test.ts`: GBP `−£98`, RUB в ru `−7 840 ₽`, `-0.4` → `£0`
- [x] 5.2 `IndustrySupplyPage`: подпись `supply.state.unserved` для входа с `outcome.unserved` (D6); проверка — новый `features/industry-supply/__tests__/unservedLabel.test.tsx`: вход с расстоянием без составов подписан «nothing can haul it», `inputs.test.ts` и `summary.test.ts` без изменений зелёные
- [x] 5.3 График: без легенды, `xAxisLabel`/`yAxisLabel`, заголовок без валюты (D7); проверка — `chart.visual.test.ts`: нет `.recharts-legend-wrapper`/легенды Mantine, две подписи осей цвета `--skin-field-text` внутри плашки и не пересекают метки; отметка рейса и `chart-mark` на месте
- [x] 5.4 Route income: «По скорости состава» сразу после поля времени и только при ручном времени и собранном составе, `description` поля по источнику (D8); проверка — новый `features/route/__tests__/manualDays.test.tsx`: при авто — описание «по скорости», кнопки нет; ввод числа — описание «вручную», кнопка есть и стоит следующим элементом после поля; нажатие возвращает авто и убирает кнопку

## 6. Числа по языку

- [x] 6.1 `i18n/plural.ts` и `countLabel` (D9); проверка — `i18n/__tests__/plural.test.ts`: ru 1 → one «год», 3 → few «года», 5 → many «лет», 1,5 → other «1,5 года», 1 при `digits=1` и значении 1.04 → one; en 1 → one, 2 → other; нет ключа категории → откат на `.other`
- [x] 6.2 Подключить формы: окупаемость и дни в `RoutePage`, круг рейса в `CorridorUpgrade`, сводка импорта из трёх частей `countLabel` вместо ключа `savegame.snapshotSummary` (ключ удалён, D9); проверка — тесты: ru окупаемость 1 → «1 год», en сводка с одним маршрутом → «1 route» (`snapshotSummary.test.ts`); `grep "t('units.years')"` остаётся только в заголовках через `withUnit`
- [x] 6.3 `buildTheme(locale)` с `decimalSeparator` из `Intl.NumberFormat` (`allowedDecimalSeparators` не задаётся — умолчание `['.', ',']`), `export const theme = buildTheme('en')` сохраняется для `__tests__/theme.test.ts`, провайдер в `main.tsx` пересобирает тему по `useLocale()` (D9); проверка — `theme.test.ts` зелёный без правки импорта и получает кейс: `buildTheme('ru')` даёт `decimalSeparator: ','`, `buildTheme('en')` — `'.'`; юнит-тест с провайдером: в ru поле показывает `43,2`, после `setLocale('en')` — `43.2` без перезагрузки; набор `43.2` и набор `43,2` в ru оба дают 43.2 в `onChange`
- [x] 6.4 Слова при числах внутри фраз (D9): `supply.bottleneckFleet`, `opt.supplyHintFleet`, `networkPage.consistVehicles`/`consistWithEngine`, `networkPage.actionSignals`, `opt.branchWait`/`branchNoWait`/`supplyHintInterval` подставляют число вместе со словом (`count.trains`, `count.vehicles`, `count.signalHeads`, `count.days`); `locales.test.ts` собирает основы `count.*` из всех словарей; проверка — тесты на единицу в en: «1 train», «1 vehicle», «1 head», «1 day» (`summary.test.ts`, `NetworkPage.test.tsx`, `actions.test.ts`, `optimizer/__tests__/rowHints.test.ts`)

## 7. Строки интерфейса

- [x] 7.1 en/ru: `opt.goalNeedsProduction` (выпуск, приглашение), `opt.rankedBy.{profit,cheapest,transported,supply}`, `howComputed.title`, `network.pieceRuleShort` (одно короткое правило на оба блока), `supply.state.unserved` («nothing can haul it» / «нечем возить»), `route.daysManual`/`daysAuto`/`daysNoConsist`, `count.*` со всеми категориями (ru лет: «год»/«года»/«лет»/«года», en: «year»/«years»); снять валюту из заголовка графика, где она была частью ключа; проверка — `locales.test.ts` (с новой проверкой категорий форм) зелёный

## 8. Завершение

- [x] 8.1 `CHANGELOG.md` → `[Unreleased]`, дописать в существующие секции, не дублируя заголовки: `### Added` — строка «Ranked by» на Best train, свёрнутые блоки «How it's computed»; `### Changed` — колонка цели после вагонов, закреплённые ранг и локомотив, итог первой строкой в блоках Network, график без легенды с подписями осей, кнопка «From consist speed» у поля времени с пометкой ручного времени, «nothing can haul it» в Supply, приглашение задать Output; `### Fixed` — минус U+2212 в суммах, формы слов при числах, десятичная запятая в ru, ширины полей Network; на английском; проверка — `scripts/next-version.sh` выдаёт minor
- [x] 8.2 Прогнать `make test`, `make check-i18n`, `cd web && npx tsc -b && npx oxlint src`, затем отдельно `caffeinate -i make check-visual` (без параллельных браузеров); проверка — всё зелёное
