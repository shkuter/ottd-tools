## 1. Разделение панели импорта

- [x] 1.1 Вынести состояние импорта из `SavegameImportPanel` в хук `useSavegameImport()`
  (фазы `idle | reading | error | ready | saved`, `readFile`, `apply`, `reset`; ленивые
  импорты `savegame/client` и `savegame/apply` остаются внутри) — проверка: существующий
  `web/src/features/settings/__tests__/SavegameImportPanel.test.tsx` зелёный без правок
  ожиданий
- [x] 1.2 Вынести отрисовку результата в `SavegameImportResult` (ошибка, различия со
  справочным блоком и подтверждением, сводка о снапшоте); панель настроек собирается из
  вступления, кнопки выбора файла и этого компонента — проверка: тот же тест зелёный, разметка
  панели прежняя

## 2. Кнопка в углу окна

- [x] 2.1 Добавить ключи в `web/src/i18n/en.json` и `ru.json`: подпись кнопки
  (`savegame.importButton` — «Загрузить сейв» / «Load savegame»), подпись кнопки закрытия окна
  и `aria-label` креста — проверка: `web/src/i18n/__tests__/locales.test.ts` зелёный
- [x] 2.2 Собрать компонент кнопки импорта: `position: fixed` в правом верхнем углу, открывает
  файловый диалог одним действием (`Button component="label"` + скрытый `input`), подпись не
  зависит от того, импортирован ли сейв; отрисовать его в `web/src/App.tsx` вне шапки —
  проверка: тест рендерит приложение на вкладке оптимизатора и находит кнопку; второй случай —
  та же подпись при сохранённом снапшоте
- [x] 2.3 Завести резерв места в шапке: `ResizeObserver` на кнопке пишет её ширину в
  `--savegame-launcher-width` на `<html>`, `.app-header` в `web/src/skin.css` получает
  `padding-right` от этой переменной со значением по умолчанию на первый кадр; шапка остаётся
  не липкой — проверка: визуальная проверка в `web/src/__tests__/visual/` утверждает, что
  прямоугольник кнопки не пересекается **ни с одним** элементом шапки — заголовком,
  подзаголовком и каждой вкладкой — на широком окне и на узком (400px), где шапка переносится
  на несколько строк
- [x] 2.4 Проверить видимость кнопки на всех маршрутах, включая `/settings` и `/kit`
  (маршрут `/kit` объявлен в `App.tsx`, в `web/src/tabs.ts` его нет — брать список маршрутов, а
  не вкладок), и что она остаётся на месте при прокрутке, а шапка уезжает — проверка: тест по
  списку маршрутов плюс визуальная проверка положения кнопки и шапки после прокрутки
- [x] 2.5 Проверить полноту подписи на узком окне: подпись не обрезана и не заменена значком —
  проверка: визуальная проверка сравнивает ширину кнопки с шириной её текста при ширине окна
  400px

## 3. Модальное окно

- [x] 3.1 Показывать `SavegameImportResult` в `Modal` из Mantine, пока фаза импорта не `idle`;
  заголовок окна — `savegame.title`, закрытие крестом, `Esc` и кнопкой зовёт `reset()` —
  проверка: тест выбирает файл на вкладке подбора (окно открылось с различиями), жмёт `Esc`
  (окно закрылось, настройки не изменились) и утверждает, что вкладка под окном осталась в
  прежнем состоянии — выбранные на ней значения не сброшены
- [x] 3.2 После подтверждения окно остаётся со сводкой о сохранённом снапшоте и кнопкой
  закрытия — проверка: тест подтверждает импорт в окне и видит сводку, пока окно не закрыто
  явно
- [x] 3.3 Завести токен затемнения `--skin-overlay` в блоке токенов `web/src/skin.css`
  (правило `:root, [data-window]` — рядом с прочими производными токенами; само затемнение
  одинаково во всех группах, это чёрный палитры, а цветовой группе следует окно) — проверка:
  тест утверждает, что окно, открытое при разных цветовых группах, залито цветом
  `--skin-window` своей группы и что группы дают разные цвета
- [x] 3.4 Одеть `Modal` в скин: новый блок правил `mantine-Modal-*` в
  `web/src/skin-mantine.css` (бевель окна, полоса заголовка, крест, затемнение через токен),
  цвета только токенами — проверка: `web/src/__tests__/skin-palette.test.ts` зелёный, hex
  руками не вписан
- [x] 3.5 Содержимое выше экрана прокручивается внутри окна, страница под ним не
  прокручивается — проверка: визуальная проверка открывает окно с заведомо высоким содержимым
  и утверждает, что кнопки подтверждения достижимы, а прокрутка страницы под окном не
  двигается
- [x] 3.6 Добавить специмен модального окна на страницу `/kit` (кнопка открытия, образец
  содержимого, устойчивый `data-testid`) и покрыть его визуальной проверкой цвета — проверка:
  `make check-visual` зелёный, `web/src/__tests__/visual/exemptions.ts` остаётся пустым

## 4. Порядок разделов на странице настроек

- [x] 4.1 Перенести `Fieldset` с `legend={t('savegame.title')}` в
  `web/src/features/settings/SettingsPage.tsx` первым блоком, перед настройками игры —
  проверка: тест утверждает, что раздел импорта идёт раньше раздела настроек игры

## 5. Документация и прогон

- [x] 5.1 Дописать запись в `CHANGELOG.md`, секция `[Unreleased]` → `### Changed` (на
  английском; `### Added` скрипт версий читает как minor, а изменение — patch) — проверка:
  `scripts/next-version.sh` выдаёт patch-версию
- [x] 5.2 Обновить `README.md` и `README.ru.md` одним изменением — упоминание о том, что импорт
  сейва доступен с любой вкладки — проверка: структура разделов и порядок пунктов в обоих
  файлах совпадают (сверить построчно `grep '^#' README.md` и `README.ru.md`)
- [x] 5.3 Прогнать `make test` и `make check-visual` — проверка: оба зелёные

## 6. Шапка и вид окна (правки по ходу работы)

- [x] 6.1 Перестроить шапку в два ряда: название с подписью, под ними меню вкладок во всю
  ширину, вкладки слева по своей ширине; резерв под кнопку перенести с шапки на ряд с
  названием — проверка: визуальная проверка непересечения (она же ловит и перенос строк), плюс
  проверка живой смены языка на экране настроек, где резерв меняется без перезагрузки
- [x] 6.2 Крест закрытия окна — справа в полосе заголовка — проверка: визуальная проверка
  положения креста относительно заголовка и правого края окна
- [x] 6.3 Полоса заголовка и ряд кнопок остаются на месте, прокручивается только содержимое
  между ними; ряд кнопок растянут на всю ширину и отбит гранью — проверка: визуальная
  проверка прокрутки содержимого при неподвижных полосах
- [x] 6.4 Ширина окна `min(1100px, 94vw)` — таблица различий стоит без горизонтальной
  прокрутки на настольном экране и не выходит за его края — проверка: визуальная проверка
  отсутствия горизонтальной прокрутки внутри окна
- [x] 6.5 Проверить, что открытое окно перекрывает кнопку, которая его открыла — проверка:
  точка в центре кнопки принадлежит не кнопке
