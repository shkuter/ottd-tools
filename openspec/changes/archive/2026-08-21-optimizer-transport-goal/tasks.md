## 1. Экономика круга (engine/trip.ts)

- [x] 1.1 Добавить в `TripParams` необязательные `deliveredShare` (по умолчанию 1) и
  `fleetSize` (по умолчанию 1); умножить `incomePerTrip` на долю, а `buyCostTotal`,
  `runningCostPerYear` и годовой доход — на размер парка. Проверяемо: при умолчаниях все числа
  прежние, `npx vitest run trip` зелёный.
- [x] 1.2 Тест: доля 0.5 вдвое уменьшает `incomePerTrip`, но не трогает `capacity`,
  `roundTripDays` и `tripsPerYear`.
- [x] 1.3 Тест: `fleetSize = 2` удваивает цену покупки, содержание и прибыль, а `paybackYears`
  не меняется.

## 2. Перебор и ранжирование (engine/optimize.ts)

- [x] 2.1 Добавить в `OptimizeParams` цель (`'profit' | 'transported'`) и `maxTrains`; при
  `productionPerMonth = 0` цель принудительно считать `'profit'`.
- [x] 2.2 Переписать `evaluate` по порядку из design.md п. 4: `cargoPerTrip` делится на число
  поездов парка, доля вывоза считается от интервала `roundTripDays / k` и передаётся в
  `tripEconomics` вместе с `fleetSize = k`.
- [x] 2.3 Добавить в `OptimizeResult` вывоз за год (`hauledPerYear`) и признак «парк ограничен
  пределом» (`fleetLimited`).
- [x] 2.4 При цели «Вывоз» перебирать `k` от минимума до `maxTrains` (при `trainsNeeded >
  maxTrains` — единственный кандидат `k = maxTrains` с `fleetLimited`), при цели «Прибыль»
  оставить `k = trainsNeeded`.
- [x] 2.5 При цели «Вывоз» перебирать все допустимые длины состава; правило текущего перебора
  (короче полной станции только при переполнении потока) сохранить для цели «Прибыль».
- [x] 2.6 Параметризовать `better()` целью: вывоз (с допуском) → прибыль → цена покупки →
  нерандомизированный вагон; тем же компаратором пользуются группировка и финальная сортировка.

## 3. Тесты движка

- [x] 3.1 Тест: при заданном производстве прибыль строки меньше, чем при том же входе без
  учёта доли вывоза, ровно во столько раз, во сколько доля меньше единицы.
- [x] 3.2 Тест: смена цели меняет порядок выдачи — первая строка при цели «Вывоз» имеет
  наибольший `hauledPerYear` и не обязана быть самой прибыльной.
- [x] 3.3 Тест: при `maxTrains`, которого не хватает на поток, `hauledPerYear` равен провозной
  способности разрешённого парка и выставлен `fleetLimited`.
- [x] 3.4 Тест: при равном вывозе выигрывает вариант с меньшим числом поездов (лишний поезд не
  добавляет вывоза, но режет прибыль).
- [x] 3.5 Тест: при `productionPerMonth = 0` цель «Вывоз» даёт ту же выдачу, что и «Прибыль».
- [x] 3.6 Тест: поток делится между поездами парка — при удвоении числа поездов груз за рейс
  вдвое меньше (пока вместимость не ограничивает), вывоз за год тот же, а окупаемость хуже.

## 4. Состояние и интерфейс

- [x] 4.1 Добавить в `state/optimizerStore.ts` поля `goal` (по умолчанию `'profit'`) и
  `maxTrains` (по умолчанию 4) с сеттерами; проверить, что старый localStorage подхватывается
  и значения не сбрасываются.
- [x] 4.2 Добавить в `OptimizerPage` `SegmentedControl` цели и поле «Поездов не больше»
  (видно только при цели «Вывоз»); цель «Вывоз» недоступна при нулевом производстве, рядом —
  объяснение почему.
- [x] 4.3 Показывать деньги строки как деньги парка и колонку абсолютного вывоза при цели
  «Вывоз»; помечать строки с `fleetLimited`.
- [x] 4.4 Обновить подсказки: деньги урезаны долей вывоза и относятся к парку, вкладка «Доход
  рейса» считает один состав по полному потоку и с оптимизатором не сойдётся.

## 5. Локализация

- [x] 5.1 Добавить новые строки в `web/src/i18n/en.json` и `ru.json` (цель и её варианты, поле
  предела парка, колонка вывоза за год, пометка ограниченного парка, объяснение блокировки
  цели, обновлённые подсказки); прогнать `npx vitest run locales`.

## 6. Производительность

- [x] 6.1 Замерить время `optimizeConsists` в браузере при цели «Вывоз» на худшем входе
  (поздний год, станция 7 тайлов, распространённый груз) с CPU-троттлингом 4x и 6x; записать
  числа в change.
- [x] 6.2 Если замер показывает заметную задержку ввода — снизить стоимость перебора (шаг по
  длине состава с добором вокруг лучшей либо дебаунс полей); иначе задачу закрыть с пометкой
  «не требуется».
- [x] 6.3 Дедуплицировать вагоны, тождественные по всем полям, которые читает расчёт;
  представителем группы брать тот вариант, который выбрал бы тай-брейк.
- [x] 6.4 Сделать компаратор выдачи транзитивным (округление ключей вместо допуска), чтобы
  выдача не зависела от порядка машин во входных данных; тест на это.
- [x] 6.5 Кэшировать физику связки между вызовами `optimizeConsists` со сбросом по всему, от
  чего физика зависит; тест на протухание кэша.
- [x] 6.6 Показывать выдачу порциями («Показать ещё» / «Показать все»), чтобы первая отрисовка
  не строила все строки со спрайтами.

## 7. Документация

- [x] 7.1 Создать `CONTEXT.md` с глоссарием: поток, доля вывоза, вывоз, парк.
- [x] 7.2 Создать ADR в `docs/adr/` на решение «доход умножается на долю доставки, а поток,
  физика и длина состава остаются полными».
- [x] 7.3 Дописать запись в `CHANGELOG.md` в секцию `[Unreleased]` (на английском), пометив
  `**BREAKING**`.

## 8. Проверка

- [x] 8.1 Прогнать `make test` целиком; убедиться, что `trip.test.ts`, `settings-effect.test.ts`
  и `rating.test.ts` зелёные.
- [x] 8.2 Проверить вкладку в браузере: обе цели, блокировка при нулевом производстве,
  сохранение выбора после перезагрузки страницы.

## 9. Правки по код-ревью 20.08.2026

Уточняют модель поверх уже сделанных задач 1.1, 2.2 и 2.4: те описывают долю вывоза
множителем дохода и перебор парка от полного выпуска — обе формулировки заменены
решениями 1, 3 и 4 в `design.md`.

- [x] 9.1 Перенести долю вывоза внутрь расчёта груза за рейс: `offeredPerYear = flowPerYear ×
  deliveredShare`, `cargoPerTrip = min(capacity, offeredPerYear / (k × tripsPerYear))`; убрать
  умножение `incomePerTrip` на долю. Проверяемо: в строке с `fleetLimited` прибыль больше не
  занижена в `1/share` раз и соответствует показанному `hauledPerYear`.
- [x] 9.2 Начать перебор парка с одного поезда: минимум определяется отдаваемым потоком, а не
  полным выпуском. При цели «Прибыль» брать наименьший парк, увозящий отдаваемое (ранний выход),
  при цели «Вывоз» — перебор до `maxTrains`.
- [x] 9.3 Тесты на оба сценария: «Парк — узкое место» (деньги по фактической загрузке) и
  «Меньший парк увозит отдаваемое» (вариант есть в выдаче и выигрывает при цели «Прибыль»).
- [x] 9.4 Обновить `docs/adr/0001-delivered-share-cuts-money-only.md`: доля режет поток, а не
  доход; описать случай `fleetLimited`, из-за которого решение поменялось.
- [x] 9.5 Поправить в `CONTEXT.md` статью про `fleetLimited`: пометка ставится по тому, что парк
  не увозит отдаваемое, а не по числу поездов на полный выпуск.
- [x] 9.6 Вынести «Fixed» в `CHANGELOG.md` в свою секцию Keep a Changelog вместо префикса пункта
  внутри `### Changed` — по секциям считает разряд `scripts/next-version.sh`.
- [x] 9.7 Развести в интерфейсе три разных «Вывоза» (доля, цель поиска, единиц в год) и назвать
  колонку числа поездов парком, как в глоссарии.
- [x] 9.8 Полировка по стандартам: кэш физики убрать из модульной области в объект, создаваемый
  вызывающей стороной; ключевать мемоизацию рейтинга результатом `speedRating()`, а не
  повтором его тела; сбрасывать порцию выдачи по самому массиву результатов, а не по его длине.

## 10. Правки по код-ревью 21.08.2026

Расхождения с уже написанными требованиями (10.1–10.3) и полировка по стандартам (10.4–10.6).
Модель не меняется, числа выдачи — только там, где выбор вагона был недетерминированным.

- [x] 10.1 Выбирать представителя группы дедуплицированных вагонов детерминированно: тем же
  тай-брейком, что и победителя (нерандомизированный, среди равных — меньший `id`), а не
  «первый встреченный». Проверяемо: `[...trains].reverse()` не меняет вагон ни в одной строке.
- [x] 10.2 Вернуть `wagon.id` в ключ теста «выдача не зависит от порядка машин»: сейчас он
  исключён и тест не проверяет половину сценария из спеки.
- [x] 10.3 Переносить кнопкой «→» те же значения задачи, по которым посчитана строка
  (дебаунсированные), а не живые поля ввода. Проверяемо: перенос сразу после правки дистанции
  даёт на вкладке «Доход рейса» цифры показанной строки.
- [x] 10.4 Перевести на английский комментарии, добавленные этим изменением в `index.css`,
  `engine/__tests__/engine.test.ts`, `engine/__tests__/trip.test.ts` и `engine/optimize.ts`.
- [x] 10.5 Собрать проверки цели в один объект (свип парка, свип длин, компаратор) вместо
  четырёх разрозненных `if (goal === …)` в `optimize.ts`.
- [x] 10.6 Вынести кэш физики (`OptimizerCache`, ключ эпохи, `SETUP_CACHE_LIMIT`) из
  `optimize.ts` в отдельный модуль: файл меняется по трём независимым причинам.
- [x] 10.7 Прогнать `make test` и проверить вкладку в браузере: обе цели, перенос строки «→».

## 11. Правки по код-ревью 21.08.2026 (второй заход)

Первая задача меняет модель: правило перебора парка при цели «Прибыль» стояло на посылке,
которую доля вывоза сделала неверной (замер и обоснование — в `design.md`, ревизия «третье
код-ревью»). Остальное — согласование артефактов, полировка и долги по тестам.

- [x] 11.1 Убрать ранний выход из перебора парка при цели «Прибыль»: перебирать `k` от одного
  до `maxTrains` при обеих целях, победителя выбирать компаратором. Проверяемо: на входе из
  замера (уголь, 1938, 300 клеток, станция 7, 500/мес) строка `kraken` показывает парк с
  наибольшей прибылью, а не первый, увозящий отдаваемое.
- [x] 11.2 Тест: при цели «Прибыль» больший парк выигрывает, когда укорачивание интервала
  поднимает долю вывоза настолько, что прибыль растёт (регресс на 11.1).
- [x] 11.3 Показывать поле «Поездов не больше» при обеих целях; проверить подсказку вкладки —
  предел больше не принадлежит цели «Вывоз».
- [x] 11.4 Отсекать короткие составы при цели «Прибыль» по отдаваемому потоку, а не по полному
  выпуску (`sweepsShorter`/`stopsSweep` сравнивают с `flowPerYear`).
- [x] 11.5 Считать доступность цели «Вывоз» по дебаунсированному производству, чтобы в окне
  дебаунса колонка вывоза не показывала числа, посчитанные при производстве 0.
- [x] 11.6 Тест на сценарий «Короткий состав выигрывает по вывозу»: при цели «Вывоз» короткий
  состав обгоняет состав на всю станцию.
- [x] 11.7 Прогонять числа в `t('opt.showMore')`, `showAll`, `shownOf` через формат локали
  (`num()`), а не через `String()`.
- [x] 11.8 Полировка по стандартам: узкий тип параметров для `tripMoney` вместо всего
  `TripParams` (и один литерал вместо двух); переименовать `startEpoch` в имя, говорящее о
  сбросе; собирать ключ эпохи внутри `optimizeCache`, чтобы «от чего зависит физика» жило в
  одном месте; считать `rank()` в `better()` один раз.
- [x] 11.9 Дописать `CHANGELOG.md` (выбор парка при цели «Прибыль» — `**BREAKING**`), прогнать
  `make test` и проверить вкладку в браузере: предел парка при обеих целях.
