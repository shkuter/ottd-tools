## 1. Формула рейтинга

- [x] 1.1 В `web/src/engine/rating.ts` добавить множитель длины дня в `StationRatingParams`
  (рядом с `jgrpp`, который остаётся для порогов возраста) и хелпер длины периода поверх
  `RATING_PERIOD_DAYS`; заменить в `estimateStationRating` обе подстановки константы
  (число периодов и накопление ждущего груза) на вычисленную длину периода.
- [x] 1.2 В `web/src/engine/optimize.ts` передать в `estimateStationRating` множитель из
  `effectiveDayLength(game)` — тот же, которым уже делится `cargoPerDay`.
- [x] 1.3 Назвать хелпер `effectiveRatingPeriodDays` (по образцу `effectiveDayLength`, чтобы
  он не отличался от константы одним лишь регистром) и сделать `dayLengthFactor` обязательным
  полем без значения по умолчанию — забытый аргумент не должен молча возвращать довиправочные
  числа. Обновить вызов в `optimize.ts` и все тесты.

## 2. Тесты

- [x] 2.1 В `web/src/engine/__tests__/rating.test.ts` оставить ассерт `RATING_PERIOD_DAYS === 2.5`
  как базовую длину периода и добавить кейс: при множителе 5 интервал 89,8 дня даёт 7 периодов
  и долю вывоза около 72 % (эталон партии из proposal.md), при множителе 1 — прежние числа.
- [x] 2.2 В `web/src/engine/__tests__/settings-effect.test.ts` починить кейс на
  `dayLengthFactor`: база кейса должна отличаться от эталона ровно этой настройкой
  (`base: { jgrpp: true }`), иначе кейс «проходит» из-за самого флага JGRPP.
- [x] 2.3 Проверить, что ванильная ветка не поехала: при `jgrpp: false` результаты
  `estimateStationRating` не изменились относительно текущих значений в тестах.
- [x] 2.4 Зафиксировать эталон партии из proposal.md: при множителе 5 доля вывоза попадает в
  окрестность игровых 72 % (допуск нужен, потому что состав партии точно неизвестен), а при
  периоде 2,5 дня на том же интервале она заметно ниже. Сейчас тест пиннит только своё
  число 0,696, и главный аргумент изменения регрессией не прикрыт.
- [x] 2.5 Убрать `deliveredShare` из общего снимка `settings-effect.test.ts` вместе с
  `productionPerMonth` у кейса: проводку множителя в рейтинг сторожит отдельный кейс в
  `engine.test.ts`, а в общем снимке доля позволила бы будущему кейсу «пройти» через рейтинг,
  а не через свою настройку. В `engine.test.ts` вынести `cargoPerDay` эталона в одну
  константу и считать её через `daysPerEconomyYear(game)`, а не зашитым 365.

## 3. Сопровождение

- [x] 3.1 Дописать запись в `CHANGELOG.md` в секцию `[Unreleased]` (на английском, `### Fixed`)
  с пометкой `**BREAKING**` — иначе `scripts/next-version.sh` посчитает разряд как patch, а
  изменение объявлено minor (на 0.x скрипт сам понижает major до minor). В записи отметить, что
  цифра меняется при замедленной экономике JGRPP.
- [x] 3.2 Прогнать `make test` (правок данных нет, `make data` не нужен).
- [x] 3.3 Уточнить формулировку в `CHANGELOG.md`: «Вывоз» вырастет по сравнению с прежним
  поведением калькулятора **при том же множителе длины дня**, а не по сравнению с меньшим
  множителем — сравнение разных множителей между собой немонотонно.
