## 1. Данные

- [x] 1.1 Выгрузить длину окна в тиках в `meta` набора (`meta.supply_window_ticks`) в
      `pipeline/extract_firs.py` — одно значение на весь набор. Режим приёма и тип предприятия
      не дублировать: `accept_mode` уже лежит по экономикам, тип правила выводится из `type`
- [x] 1.2 Выгрузить пороги и бонусы накопительного пула: базовые значения параметров NewGRF и
      множитель типа предприятия
- [x] 1.3 Эталоны в `pipeline/tests/test_known_values.py`: окно 6912 тиков, вторичка с
      обязательными входами, вторичка «любые три из пяти», шахта 16/80, порт 128/640, бонусы
      производства 150 % и 250 %
- [x] 1.4 Прогнать `make data` и убедиться, что диффом в `industries.json` затронуты только
      новые поля

## 2. Модуль снабжения

- [x] 2.1 Создать `web/src/engine/supply.ts`: конверсия вторички по сумме входных ratio под
      потолком 8 и выпуск по каждому выходному грузу — с домножением на выходной ratio под тем
      же делителем 8
- [x] 2.2 Отношение интервала к окну и минимальный парк, удерживающий предприятие поданным
      (`trainsForWindow`)
- [x] 2.3 Накопительный пул: объём за окно против двух порогов, бонус производства по уровню
- [x] 2.4 Пороги промежуточной зоны — именованные константы с пометкой о сверке в игре
- [x] 2.5 Признак «правило неизвестно» для типов вне двух правил (`IndustryTertiary`,
      `IndustryPrimaryNoSupplies`, городские производители) — отдельный результат, а не
      молчаливый ноль
- [x] 2.6 Перевод единиц в одном месте: окно в тиках против интервала в движковых днях
      (`DAY_TICKS = 74`); проверить тестом, что `dayLengthFactor` отношение не двигает
- [x] 2.7 Тесты `supply.test.ts`: все входы поданы, один выпал, «любые три из пяти» (пять,
      шесть и семь входов), выходной ratio урезает выпуск, граница отношения, пул по обоим
      порогам, неизвестное правило, выпуск не задан (не `NaN`)

## 3. Обратный индекс грузов

- [x] 3.1 Построить в `web/src/dataset.ts` индекс «груз → принимающие предприятия» по активной
      экономике (`activeEconomy(game)`), с кешем по `economy.id`
- [x] 3.2 Тест: индекс согласован с `accepts` в данных, груз без потребителей даёт пустой список

## 4. Цель и колонка

- [x] 4.1 Добавить цель `supply` в `OptimizeGoal` и `GoalStrategy` с лексикографическим
      сравнением, включая выбор ветки загрузки под этой целью и ответы `sweepsShorter` /
      `stopsSweep` (все допустимые длины, перебор до предела парка)
- [x] 4.2 Считать снабжение для строки выдачи и вынести результат в `OptimizeResult`
- [x] 4.3 Колонка в таблице Best-train: значение, сортировка, цветовая отметка
- [x] 4.4 Подсказка колонки: интервал, длина окна, минимальный парк, оговорка про остальные
      входы предприятия
- [x] 4.5 Выбранный получатель в `web/src/state/optimizerStore.ts` рядом с `goal`: persist и
      чтение через функцию, падающую на автоподстановку, когда получателя в текущей экономике
      нет (приём из ADR-0002)
- [x] 4.6 Выбор получателя в интерфейсе: автоподстановка единственного, список при нескольких,
      недоступность цели при отсутствии потребителей
- [x] 4.7 Сортировка выдачи по любой колонке: состояние сортировки на вкладке, щелчок по
      заголовку, третий щелчок — возврат к порядку поиска
- [x] 4.8 Тесты сортировки: порядок меняется, состав и числа строк — нет; возврат к исходному
- [x] 4.9 Тесты `optimize`: ранжирование по цели `supply`, выбор ветки загрузки под ней,
      недоступность цели без выпуска, прочерк в колонке без выпуска, замена получателя после
      смены экономики

## 5. Локализация

- [x] 5.1 Строки колонки, цели и подсказки в `web/src/i18n/en.json` и `ru.json`, прогнать
      `i18n/__tests__/locales.test.ts`

## 6. Завершение

- [x] 6.1 Обновить глоссарий `CONTEXT.md`: у термина **Goal** третья цель, рядом с уже
      описанными supply window / supply ratio / trainsForWindow
- [x] 6.2 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском
- [x] 6.3 Прогнать `make data`, `make test`, `make verify`

## 7. Правки по ревью реализации

- [x] 7.1 Показать накопительный пул: значение колонки для получателя с пулом — достигнутый
      уровень, а не отношение к окну
- [x] 7.2 Подсказка для пула: объём за окно, оба порога, бонус производства уровня и оговорка,
      что учтён вклад только этого маршрута
- [x] 7.3 Не считать пул без выпуска источника: `deliveredPerWindow` не берётся из провозной
      способности парка, `pool` в результате поиска остаётся пустым (сейчас даёт уровень 2)
- [x] 7.4 Оговорку про остальные входы показывать только когда входов больше одного
- [x] 7.5 Отдельный текст для первичного предприятия без припасов вместо общего «правило
      неизвестно»
- [x] 7.6 Строки локали для пула в `en.json` / `ru.json`
- [x] 7.7 Тесты: уровень пула в строке выдачи, отсутствие пула без выпуска, подсказка при одном
      входе, вердикт получателя с пулом не следует отношению к окну
- [x] 7.8 Убрать дубль расчёта отношения и вердикта: `rowSupply` считает их перед
      `assessSupply`, который считает их заново — перенести сборку в `engine/supply.ts`
- [x] 7.9 Убрать неиспользуемые экспорты: реэкспорт `SupplyTarget` из `optimize.ts`,
      `resolveDestination` из публичного API `dataset.ts` (`secondaryOutput` остаётся —
      он фиксирует формулу выпуска против исходников FIRS)
- [x] 7.10 Глоссарий `CONTEXT.md`: промежуточный вердикт (`marginal`) и порог `MARGINAL_RATIO`
- [x] 7.11 Закрепить язык тестов в `openspec/config.yaml` и `CLAUDE.md`: описания `describe` /
      `it` — на русском, остальной код и комментарии — на английском
- [x] 7.12 Прогнать `make verify` и проверить вкладку в браузере

## 8. Правки по второму ревью

- [x] 8.1 Подсказка пула называет минимальный парк и интервал вместе с объёмом, порогами и
      бонусом — «дополнительно», а не «вместо»
- [x] 8.2 Подсказка заголовка колонки следует правилу получателя: для пула — про пороги и
      производство, для конверсии — про окно (сейчас один текст на оба случая)
- [x] 8.3 Значение колонки снабжения — одна функция, которую зовут и ячейка, и `SORT_VALUES`
      (сейчас определено дважды и может разъехаться)
- [x] 8.4 Строки без сортируемого значения уходят в конец при обоих направлениях (сейчас
      `Infinity` всплывает наверх при убывании)
- [x] 8.5 Тест сортировки на строки без значения переписать так, чтобы он мог упасть: сейчас
      обнуляет `supply` у всех строк и проверяет только длину
- [x] 8.6 Третье значение `SupplyRule` — «припасы не помогают» отдельно от «правило
      неизвестно», чтобы интерфейс не расклеивал их по `industry.type`
- [x] 8.7 Русский комментарий в `OptimizerPage.tsx` перевести на английский (единственный в
      продуктовом коде)
- [x] 8.8 Переписать правило языка тестов в `openspec/config.yaml` и `CLAUDE.md` под реальную
      практику: комментарии внутри тестов бывают русскими, часть файлов англоязычна целиком,
      поэтому правило — следовать языку файла, а не вводить новую норму
- [x] 8.9 Русские `it` в англоязычном `dataset.test.ts` привести к языку файла
- [x] 8.10 Обновить доккомментарий над `supplyScore` / `RankKeys` (говорит о трёх числах, полей
      пять)
- [x] 8.11 Прогнать `make verify` и проверить обе подсказки в браузере
