# Tasks: savegame-bridges

## 1. Привязка станция → поставщики в снапшоте

- [x] 1.1 Читать ж/д площадку станции в `extract/stnn.ts` (`train_station.tile/.w/.h`,
      дескриптор — `SlStationNormal`,
      `vendor/openttd/src/saveload/station_sl.cpp:591-595`; полного `rect` в сейве нет —
      он `NOSAVE`). Проверка: юнит-тест на
      существующих фикстурах — у ж/д станции площадка непустая и объемлет её опорный тайл,
      у автобусной остановки пустая.
- [x] 1.2 Читать площадку индустрии в `extract/indy.ts` (`location.tile/.w/.h`,
      `saveload/industry_sl.cpp:152-155`). Проверка: юнит-тест на фикстурах — площадка
      известной индустрии совпадает с картой партии.
- [x] 1.3 Привязка в `snapshot.ts`: `SnapshotStation.supplierIds` по пересечению площадки
      индустрии с площадкой станции + радиус 4 (`CA_TRAIN`, `station_type.h:117`) +
      `station.catchment_increase` из настроек сейва (дефолт 0). Проверка: тесты —
      индустрия в охвате числится поставщиком, далёкая нет, станция без ж/д части и сейв
      без полей площадки дают пустую привязку без ошибки.
- [x] 1.4 Бамп `SNAPSHOT_SCHEMA_VERSION` → 3 в `snapshotStore.ts`. Проверка: существующий
      тест дропа рекорда чужой версии проходит с новым значением; ручной сценарий — старый
      снапшот после обновления не читается, вкладка просит импорт.

## 2. Модуль моста и сторы

- [x] 2.1 Вынести из `routeRows.ts` предикаты, на которых стоит блокер (единый состав,
      опознанность машин, известный груз), не меняя поведения карточки. Проверка:
      существующие тесты `routeRows` зелёные без правок ожиданий.
- [x] 2.2 `setEntries(entries)` в `consistStore` — мост кладёт готовые `ConsistEntry`, а не
      id (существующие `add`/`merge` оживляют через `trainById`, где только Iron Horse).
      Заодно проверить конструктор состава в ванильном режиме (`ironHorse: false`):
      добавляется ли машина и переживает ли состав перезагрузку; если нет — научить `merge`
      оживлять id обоих наборов (`trainByAnyId`). Раз состав теперь переживает и смену
      набора, вкладки расчёта берут из него только машины активного набора
      (`activeEntries`) — иначе к ванильной машине применятся шифты Iron Horse. Проверка:
      тесты стора — `setEntries` кладёт ванильные записи, они переживают partialize/merge; и
      тест вкладки — состав чужого набора в расчёт не попадает.
- [x] 2.3 Поле `prefillOrigin: { source, label, values } | null` в `routeStore` и `optimizerStore`
      (в persist) плюс функция сравнения значений. Проверка: юнит-тесты стора — поле
      пишется, переживает partialize/merge, сравнение ловит правку и возврат значения.
- [x] 2.4 `features/savegame/bridge.ts`: `routeToIncome(row, snapshot)` — полный набор при
      прогнозе, «только состав и груз» при ≥3 остановках, причина иначе (порядок мостовой:
      состав и груз важнее числа остановок); станция погрузки по рейтингу груза (fallback —
      первая остановка), поток = сумма produced поставщиков. Проверка: юнит-тесты на
      сценарии спеки (полный перенос, ванильная партия, кольцевой, поток неизвестен → 0,
      чужая машина, разнородный парк, кольцевой с разнородным парком называет парк).
- [x] 2.5 `routeToOptimizer(row, snapshot)` (груз + дистанция + поток; при ≥3 остановках и
      при неизвестной дистанции — только груз, прочие поля не трогаются; причина только без
      груза) и `industryToOptimizer(produced)`. Проверка: юнит-тесты на сценарии
      спеки, включая «состав мешает доходу, но не оптимизатору».
- [x] 2.6 Применение моста: запись в `consistStore`/`routeStore`/`optimizerStore` вне React
      (`getState()`), состав — готовыми записями из `activeTrains(settings.game)`, полный
      мост перезаписывает все свои поля (`amount` = вместимость прогноза, `manualDays` =
      null, неизвестный поток = 0), частичный не трогает прочие; `prefillOrigin` с
      перенесёнными значениями. Проверка: юнит-тесты записи в сторы по сценариям спеки.

## 3. UI

- [x] 3.1 Переходы маршрута в его строке: стрелка `ActionIcon.btn-add` в последней колонке
      («Доход рейса») и груз-ссылка («Лучший поезд»); клик по обоим не раскрывает строку.
      Недоступная стрелка помечается `aria-disabled`, а не `disabled` (иначе выпадет из
      обхода клавиатурой), причина `game.blocker.*` — в подсказке и доступном имени; там,
      где груза нет, подсказку несёт прочерк. Проверка: тесты компонента — стрелка активна
      при прогнозе; при чужой машине помечена отказом, ничего не пишет и никуда не ведёт; у
      кольцевого маршрута с разнородным парком называет парк, а не число остановок; груз-
      ссылка ведёт в оптимизатор даже там, где стрелка закрыта.
- [x] 3.2 Груз-ссылка «в Лучший поезд» у каждого произведённого груза **в колонке
      производства** `IndustriesTab` (рендерером, переданным в `GoodsCell`, — не внутри
      самого `GoodsCell`, иначе ссылка появится и в колонке вывоза). Ссылку рисует общий
      `CargoBridgeLink` — тот же, что в списке маршрутов, поэтому подсказка, доступное имя
      и обработка неизвестного груза совпадают. Проверка: тесты компонента — ссылка есть
      только в колонке производства, у груза без данных месяца и у груза вне активного
      набора её нет, клик переносит именно этот груз.
- [x] 3.3 Пометка происхождения на `RoutePage` и `OptimizerPage`: видна, пока значения равны
      перенесённым. Проверка: тесты компонентов — после моста пометка есть, после правки
      дистанции нет, после возврата прежнего числа снова есть.
- [x] 3.4 Новые UI-строки в `en.json`/`ru.json` (подписи переходов, пометка происхождения). Проверка:
      `cd web && npx vitest run src/i18n/__tests__/locales.test.ts` зелёный — файл проверяет
      и набор ключей, и то, что русские строки переведены.

## 4. Финал

- [x] 4.1 Запись в `CHANGELOG.md` → `[Unreleased]` → `### Added` (по-английски) тем же
      изменением. Проверка: секция описывает мосты и расширение снапшота.
- [x] 4.2 Прогон `make test` и `make check-visual` (новые кнопки в таблицах — вид скина).
      Проверка: оба зелёные.
