## 1. Зависимости и провайдер

- [x] 1.1 Замерить исходный размер бандла: `make build`, записать размеры чанков во временный
  файл `/tmp/ottd-tools-bundle-before.txt` — с ними сверяемся в конце.
- [x] 1.2 Поставить `@mantine/core@^9.5.1`, `@mantine/hooks@^9.5.1`, `@mantine/notifications@^9.5.1`,
  `@mantine/charts@^9.5.1`, `recharts@^3.10.1`, `mantine-datatable@^9.4.0`, `clsx@^2` в `web/`.
  Проверка: `npm ls` без предупреждений о peer-зависимостях, `make build` проходит.
- [x] 1.3 Обернуть дерево в `MantineProvider` (`main.tsx`) с `forceColorScheme="dark"`, импортом
  `styles.layer.css` пакетов и объявлением `@layer mantine, app;`. Проверка: приложение
  открывается, вид не изменился, в localStorage не появилось ключа цветовой схемы Mantine.

## 2. Тема и слой скина

- [x] 2.1 Написать `web/src/theme.ts`: расширение градиента 8 → 10 оттенков, группы `grey`,
  `yellow`, `green`, `red`, `darkBlue` в `theme.colors`, `primaryColor`, `defaultRadius: 0`,
  шрифты игры. Проверка: юнит-тест на расширение градиента (длина 10, крайние продублированы).
- [x] 2.2 Написать `cssVariablesResolver`, отдающий переменные Mantine значениями `var(--skin-*)`
  (корпус, текст, `--mantine-color-default*`, ссылки, ошибка). Проверка: в DevTools вычисленные
  значения переменных Mantine совпадают с токенами скина.
- [x] 2.3 Собрать временную страницу-образец со всеми типами контролов (кнопка, поле, число,
  селект, свитч, чекбокс, таб, таблица, `Alert`, уведомление, тултип). Проверка глазами: бевели
  2px, нулевое скругление, цвета из палитры игры.
- [x] 2.4 Перенести правила скина на статические классы Mantine внутри `@layer app`. Проверка:
  на странице-образце ни одного `!important`, вид совпадает с текущими контролами.

## 3. Оболочка и общие компоненты

- [x] 3.1 Перевести `App.tsx` на `AppShell`/`Tabs` (шапка, вкладки, подвал), сохранив маршруты и
  фильтрацию вкладки FIRS по настройке. Проверка: все маршруты открываются, активная вкладка
  подсвечена, редиректы `/` и `/combined` работают.
- [x] 3.2 Перевести `components/Warning.tsx` на `Alert`, а `Money`, `CargoIcon` и `TrainImage` —
  на компоненты Mantine с сохранением классов `.money-neg`, `.cargo-icon` и `.train-sprite`, если
  на них ещё ссылается CSS.
  Проверка: предупреждение об инфляции видно на всех вкладках и ведёт в настройки.
- [x] 3.3 Подключить `Notifications` и завести уведомления на разовые действия («состав очищен»,
  «настройки сброшены»). Проверка: уведомление появляется и само исчезает, постоянные
  предупреждения остались статичными.

## 4. Вкладка настроек

- [x] 4.1 Перевести `SettingsPage.tsx` на `Switch`, `Select`, `NumberInput`, `Fieldset`
  (17 полей ввода, 13 селектов, 8 групп). Проверка: `settingsStore` не изменён, значения
  сохраняются между перезагрузками, числа на других вкладках прежние.
- [x] 4.2 Удалить `input-switch-polyfill` из зависимостей и его импорты в `main.tsx`. Проверка:
  свитчи работают, `npm ls input-switch-polyfill` пуст.

## 5. Конструктор составов и каталог

- [x] 5.1 Перевести фильтры каталога на компоненты Mantine (`Select` с поиском для груза,
  `NumberInput` для года, `TextInput` для поиска), по решению Р6 — с `@mantine/form` или без.
  Если `@mantine/form` не даёт выигрыша, пакет не ставится, и это отмечается в отчёте. Проверка:
  фильтрация даёт тот же список машин, что и до правки.
- [x] 5.2 Заменить `@tanstack/react-table` на `DataTable` из `mantine-datatable`: колонки с
  `title: () => t(...)`, контролируемый `sortStatus`, собственный компаратор в `useMemo`,
  `pinFirstColumn` / `pinLastColumn`. Проверка: сортировка по каждой колонке даёт тот же порядок,
  что и раньше.
- [x] 5.3 Заменить обрезку «первые 400» встроенной пагинацией. Проверка: доступны все машины,
  подходящие под фильтр, переключение страниц не сбрасывает фильтры и сортировку.
- [x] 5.4 Перевести панель состава (список, счётчики, сводка) на `Stack`/`Group`/`Table`.
  Проверка: добавление, изменение количества, удаление и очистка дают те же числа сводки.
- [x] 5.5 Сделать маршрут вкладки составов лениво загружаемым, чтобы `mantine-datatable` не
  попадал в основной чанк. Проверка: в `make build` datatable выделен в отдельный чанк.

## 6. Доход рейса и график

- [x] 6.1 Перевести элементы управления и сводки `RoutePage.tsx` на компоненты Mantine.
  Проверка: доход, время круга и окупаемость совпадают со значениями до правки.
- [x] 6.2 Заменить самодельный SVG на `LineChart` из `@mantine/charts` с `referenceLines` для
  текущего времени рейса; точки — тот же `incomeCurve()`. Проверка: кривая и подпись
  «время → доход» совпадают с прежним графиком при тех же параметрах.
- [x] 6.3 Сделать маршрут вкладки лениво загружаемым, чтобы `recharts` не попадал в основной
  чанк. Проверка: в `make build` recharts выделен в отдельный чанк.

## 7. Оптимизатор и цепочки FIRS

- [x] 7.1 Перевести `OptimizerPage.tsx` (8 полей, чекбоксы «под вопросом», таблица результатов)
  на компоненты Mantine. Проверка: подбор возвращает тот же список машин в том же порядке.
- [x] 7.2 Перевести `FirsPage.tsx` (вкладки экономик, боковая панель), не трогая graphviz-SVG и
  обработку кликов по узлам. Проверка: граф рисуется, клик по узлу открывает ту же панель.

## 8. Чистка и локализация

- [x] 8.1 Удалить из `index.css` и `skin.css` правила, оставшиеся без разметки. Проверка: поиск по
  каждому оставшемуся классу находит его в коде; вид вкладок не изменился.
- [x] 8.2 Снять `@tanstack/react-table` с зависимостей и убрать временную страницу-образец.
  Проверка: `make build` проходит, `npm ls @tanstack/react-table` пуст.
- [x] 8.3 Добавить новые UI-строки в `i18n/en.json` и `ru.json` (подписи пагинации, пустого
  состояния таблицы, текстов уведомлений). Проверка: `npx vitest run i18n` зелёный.

## 9. Проверка и завершение

- [x] 9.1 Пройти все пять вкладок в браузере (chrome-devtools) на русском и английском: бевели,
  палитра, шрифт, отсутствие непереведённых строк, сохранение состояния после перезагрузки.
  Проверка: скриншоты до/после по каждой вкладке.
- [x] 9.2 Замерить бандл после миграции и сравнить с `bundle-before.txt`. Проверка: числа записаны
  в отчёт, ленивые чанки на месте.
- [x] 9.3 Обновить `CLAUDE.md`: пример правила локализации таблицы (`header: () => t('table.name')`
  → как это выглядит в `DataTable`) и описание того, откуда берётся вид интерфейса (скин плюс тема
  Mantine, слои CSS). Проверка: в файле не осталось примеров кода, которого больше нет.
- [x] 9.4 Записать изменение в `CHANGELOG.md`, секция `[Unreleased]`, на английском.
- [x] 9.5 Прогнать `make verify` и `make test`. Проверка: обе команды зелёные.
