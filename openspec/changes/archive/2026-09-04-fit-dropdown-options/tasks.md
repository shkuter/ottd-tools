## 1. Ширина выпадающего списка

- [x] 1.1 В `web/src/theme.ts` завести секцию компонентов (её в файле пока нет) и задать
      `Select.defaultProps.comboboxProps`: `width: 'max-content'` и свой `middlewares.size.apply`,
      пишущий дропдауну `minWidth = rects.reference.width` и `maxWidth = availableWidth`. Рядом
      комментарий на английском: чью формулу повторяем (`dropdown.cpp`, `UpdateSizeAndPosition`),
      почему ширину нельзя задать в CSS (Mantine пишет её инлайном в ветке `size` функции
      `getMiddlewares`, `@mantine/core/.../use-popover.mjs`) и что список другого вида
      (`MultiSelect`, `Autocomplete`) придётся внести сюда же. Номера строк в комментарий не
      писать: они поедут на первом обновлении пакета. Проверка: `make dev`, на вкладке
      «Снабжение» пункт «Садоводческое и свиноводческое хозяйство» стоит одной строкой, список
      шире поля; список типа путей (пункты короче поля) шириной с поле
- [x] 1.2 Прижать список к левому краю поля (`position: 'bottom-start'` в тех же умолчаниях):
      Mantine центрирует всплывающий блок на цели, и список, ставший шире поля, уезжает влево —
      игра же ставит список от левого края кнопки. Проверка: утверждение из 5.2 зелёное
- [x] 1.3 Убедиться на узком окне (~400px), что список остаётся в его пределах, а длинный пункт
      переносится и виден целиком. Правил `white-space` не добавлять: перенос обязан получаться
      из `max-content` и `maxWidth`. Проверка: утверждение из 5.3 зелёное

## 2. Картинка в пункте списка

- [x] 2.1 Завести `web/src/components/PictureSelect.tsx`: списки `CargoSelect` и `TrainSelect`,
      каждый рисует картинку и в пункте (`renderOption`), и в закрытом поле (`leftSection`) —
      одно место вместо тройки пропсов, повторяемой на каждой вкладке. Проверка: `cd web && npx tsc --noEmit`
      чистый
- [x] 2.2 Правила пункта с картинкой в `web/src/skin.css` (разметку рисует наш компонент, а не
      библиотека): ячейка шириной `--skin-sprite-cell` — новый токен вместо числа 65, которое
      уже стояло в колонке `.cell-vehicle` и в правиле спрайта, — выравнивание по центру
      строки, картинка не растягивает строку. Проверка: `npx vitest run src/__tests__/skin-palette.test.ts` зелёный (в правило не
      вписан hex); вид проверяется в 5.3
- [x] 2.3 Подключить хелпер со спрайтом в список «Локомотив замены»
      (`features/network/CorridorUpgrade.tsx`). Проверка: при выбранном пути и составе список
      замены показывает спрайты, названия начинаются с одной вертикали
- [x] 2.4 Подключить хелпер с иконкой груза в списки выбора груза:
      `features/optimizer/OptimizerPage.tsx`, `features/route/RoutePage.tsx`,
      `features/consist/ConsistPage.tsx` (отбор по грузу и груз для вместимости). Проверка:
      иконка в открытом списке та же, что в закрытом поле, на всех четырёх

## 3. Поле «Локомотив замены»

- [x] 3.1 Перевести поле на `TrainSelect` (спрайт выбранной машины и ячейка под него, которую
      задаёт правило `.train-select .mantine-Input-wrapper` в `skin-mantine.css` — рядом с тем,
      что даёт прочим полям секцию 20px, иначе объявление не доходит); в
      `web/src/skin.css` дать полю ширину `calc(var(--skin-field-wide) + var(--skin-sprite-cell))`
      вместо 140px от `.network-inputs .field`: одной ширины поля с грузом не хватает — спрайт
      забирает 65px, шеврон 24, а самому длинному имени набора нужно 230px. Проверка: имя выбранного локомотива помещается целиком, ряд
      полей коридора не сломан (`.network-inputs` переносит), высота поля равна высоте соседних

## 4. Специмены для проверок

- [x] 4.1 Добавить в `features/kit/KitPage.tsx` три специмена: список с пунктом заведомо длиннее
      поля, список из одних коротких пунктов (мера строки без картинки) и список с картинкой в
      пунктах — все с устойчивым `data-testid`. Специмены временные:
      следующий change (`expand-kit-page`) перестраивает витрину и переносит их в её ярусы —
      написать это комментарием рядом, чтобы перенос не выглядел случайной потерей. Проверка:
      `/kit` открывается, все три списка раскрываются
- [x] 4.2 Подписи специменов в `web/src/i18n/en.json` и `ru.json`. Проверка:
      `npx vitest run src/i18n/__tests__/locales.test.ts` зелёный

## 5. Проверки вида

- [x] 5.1 В `web/src/__tests__/visual/dropdown.visual.test.ts` — утверждения о ширине: список с
      длинным пунктом шире своего поля, пункт занимает одну строку (высота пункта равна высоте
      строки); список с короткими пунктами шириной не уже поля. Проверка: `make check-visual`
      зелёный
- [x] 5.2 Там же — утверждение о выравнивании: в окне, где справа от поля есть место, левый
      край списка совпадает с левым краем поля. Проверка: `make check-visual` зелёный
- [x] 5.3 Там же — утверждение о переносе на упоре: при окне, суженном до ширины меньше самого
      длинного пункта, правый край списка не выходит за окно, а пункт занимает больше одной
      строки. Проверка: `make check-visual` зелёный
- [x] 5.4 Там же — утверждение о наборе в поле с поиском: после ввода, оставляющего только
      короткие пункты, ширина списка не меньше ширины поля. Проверка: `make check-visual`
      зелёный
- [x] 5.5 Там же — утверждения о картинке: у пункта есть картинка, названия пунктов начинаются с
      одной вертикали, высота пункта с картинкой равна высоте пункта без неё. Проверка:
      `make check-visual` зелёный
- [x] 5.6 Утверждение о списке грузов на вкладке «Лучший поезд»: у каждого пункта отрисована
      иконка, и та же иконка стоит в поле. Проверка: `make check-visual` зелёный
- [x] 5.7 Утверждение о поле «Локомотив замены»: после выбора машины в поле стоит её спрайт, а
      самое длинное имя из списка кандидатов помещается в место, которое поле оставляет тексту
      (меряется на вкладке, с выбранным локомотивом, — иначе левой секции нет и замер лжёт). Проверка: `make check-visual` зелёный
- [x] 5.8 Утверждение на настоящей вкладке (не на витрине): список предприятий на «Снабжении»
      держит пункт в одну строку и не уже своего поля — правило живёт в теме, а не на странице.
      Проверка: `make check-visual` зелёный
- [x] 5.9 Прогнать `clipping.visual.test.ts` на обоих языках — правка ширины полей коридора не
      должна ничего обрезать. Проверка: `make check-visual` зелёный целиком

## 6. Завершение

- [x] 6.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском: список раскрывается по
      содержимому, как в игре, и показывает картинку машины или груза в пункте. Проверка: секция
      `[Unreleased]` содержит запись, текст английский
- [x] 6.2 Прогнать `make test` и `make verify`. Проверка: оба зелёные
