## 1. Координаты площадок в снапшоте

- [x] 1.1 Добавить `plot: IndustryPlot | null` (`{ x, y }`) в `SnapshotIndustry`
      (`web/src/savegame/snapshot.ts`), раскладывая тайловый индекс площадки на оси при
      разборе; проверка — тест на сейве из набора: у индустрии снапшота есть координаты, а на
      сейве без размера карты `plot` равен `null` и разбор не падает
- [x] 1.2 Добавить в `web/src/savegame/` функцию расстояния между двумя площадками
      манхэттенской метрикой; проверка — юнит-тест на паре, отстоящей на 30 и 10 клеток по
      осям, даёт 40 (сценарий спеки `savegame-snapshot`)
- [x] 1.3 Бампнуть `SNAPSHOT_SCHEMA_VERSION` в `web/src/savegame/snapshotStore.ts`; проверка —
      существующий тест на отбрасывание записи устаревшей версии проходит с новым числом

## 2. Обращение выпуска в движке снабжения

- [x] 2.1 Добавить `requiredDelivery()` в `web/src/engine/supply.ts` — обращение
      `secondaryOutput()`, `null` при нулевой конверсии или нулевом выходном отношении;
      проверка — юнит-тест, что `secondaryOutput(requiredDelivery(o, c, r), c, r) === o` на
      данных коксовой печи, и `null` на вырожденных аргументах

## 3. Обратный обход цепочки

- [x] 3.1 Создать `web/src/features/firs/dependencies.ts` с обходом вверх по входам активной
      экономики; разворачивается только производитель с правилом `conversion`, всё остальное —
      лист (`pool`, `no-supplies`, `unknown`); проверка — юнит-тест на доменной печи Steeltown:
      в обходе есть коксовая печь и уголь, а выше угольной шахты (принимает `ENSP`) и выше
      порта (принимает семь грузов) обход не идёт
- [x] 3.2 Закрыть циклы и повторные пути множеством посещённых; проверка — юнит-тест на
      экономике с побочным грузом, возвращающимся ниже по цепочке: обход завершается, каждое
      предприятие названо один раз
- [x] 3.3 Убедиться, что `chainNodes()` и подсветка графа не изменились; проверка —
      существующие тесты `web/src/features/firs/__tests__` проходят без правок

## 4. Список задач снабжения и объёмы

- [x] 4.1 Собрать плоский список задач «груз — источник — получатель», по одной на каждый вход
      каждого предприятия цепочки; проверка — юнит-тест: у предприятия с тремя входами три
      задачи, каждая называет оба конца
- [x] 4.2 Посчитать требуемую подачу сверху вниз от желаемого выпуска цели через
      `requiredDelivery()`, деля сумму между входами пропорционально входным отношениям;
      проверка — юнит-тест: удвоение желаемого выпуска удваивает подачу каждой задачи цепочки
- [x] 4.3 Для получателей с накопительным пулом отдавать оба порога как требуемый объём
      вместе с уровнем и бонусом каждого; проверка — юнит-тест на задаче к угольной шахте:
      названы оба порога и их проценты из данных набора
- [x] 4.4 Для получателя с неизвестным правилом снабжения отдавать задачу без объёма, отличая
      это от нуля; проверка — юнит-тест на таком получателе: задача есть, объёма нет

## 5. Сведения из импортированной партии

- [x] 5.1 Вывести состояние узла (обеспечен / стоит без поставок / нет на карте) из снапшота:
      маршрут компании с этим грузом и остановкой на станции, накрывающей площадку получателя;
      груз опознаётся по ненулевой вместимости в составе, а не по загруженному количеству;
      проверка — юнит-тесты на три состояния из собранного снапшота-фикстуры
- [x] 5.2 Подставить в задачи источники на карте и длины плеч, выбирая ближайший источник и
      считая число кандидатов; проверка — юнит-тест: из двух шахт в 40 и 120 клетках выбрана
      ближняя, кандидатов названо двое
- [x] 5.3 Оставить задачи без плеч и источников там, где партии нет или сейв не назвал размера
      карты; проверка — юнит-тест: список строится, длин плеч нет, ошибок нет

## 6. Порядок задач

- [x] 6.1 Упорядочить список двумя ключами: класс плеча (один город → разные города → город
      неизвестен), внутри класса — длина по возрастанию; задачи без известного плеча идут после
      задач с известным, обеспеченные — в конце; без партии порядок по месту в цепочке (ближе к
      цели — выше); проверка — юнит-тесты на каждое правило сортировки из спеки, включая
      «плечо 40 внутри города выше межгородского 25»

## 7. Режим на вкладке FIRS chains

- [x] 7.1 Добавить в `firsStore` целевое предприятие режима и желаемый выпуск (не persist);
      дефолт выпуска — из снапшота, когда цель на карте, иначе 100; проверка — юнит-тест
      стора на оба дефолта
- [x] 7.2 Собрать раздел режима в `web/src/features/firs/FirsPage.tsx`: выбор цели (клик по
      узлу графа и список-выбор), поле масштаба, список задач с объёмами, состояниями,
      источниками и плечами; проверка — компонентный тест: выбор цели даёт непустой список
      задач, смена масштаба меняет объёмы
- [x] 7.3 Показать в интерфейсе оговорки, которых требует спека: деление суммы по входам —
      допущение калькулятора; пометка «обеспечен» не утверждает удержания окна поставок;
      расстояние — приближение; без партии состояния и плечи появятся после импорта; проверка —
      компонентный тест на присутствие каждой оговорки в соответствующем состоянии

## 8. Мост на вкладку «Снабжение»

- [x] 8.1 Добавить мост задачи в `web/src/features/savegame/bridge.ts` в форме `Bridge<V>`:
      получатель и груз всегда, длина плеча и выпуск источника — когда партия импортирована;
      блокировка с названной причиной только там, где груза или получателя нет в активном
      наборе; проверка — юнит-тесты на перенос с партией, на перенос без партии и на причину
      недоступности
- [x] 8.2 Завести пометку происхождения на вкладке «Снабжение»: четвёртый источник в
      `PrefillSource` (`web/src/state/prefill.ts`) и `prefillOrigin` в `industrySupplyStore`,
      переиспользуя `prefillMatches()`; проверка — юнит-тест стора: пометка видна, пока поля
      равны перенесённым, гаснет при правке и возвращается, когда значение вернули
- [x] 8.3 Повесить мост на строку задачи значком-стрелкой в конце строки; проверка —
      компонентный тест: переход выбирает получателя и заполняет поля входа, вкладка называет
      источник значений

## 9. Строки, вид и завершение

- [x] 9.1 Завести строки режима в `web/src/i18n/en.json` и `ru.json`; проверка —
      `i18n/__tests__/locales.test.ts` проходит (названия грузов и предприятий не трогать —
      они генерируются)
- [x] 9.2 Проверить вид режима в скине; проверка — `make check-visual` зелёный, новых
      исключений в `visual/exemptions.ts` не добавлено
- [x] 9.3 Дописать запись в `## [Unreleased]` файла `CHANGELOG.md` (на английском), назвав
      режим и то, что импортированную партию нужно загрузить заново из-за версии снапшота
- [x] 9.4 Прогнать `make test`; проверка — набор зелёный
