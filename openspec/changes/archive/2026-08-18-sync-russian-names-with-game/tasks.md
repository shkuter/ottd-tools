## 1. Источник перевода

- [x] 1.1 Добавить в `Makefile` пин `FIRS_RU_REF` (полный sha `61a0f097…` ветки
      `russian-translation` форка ChronosXYZ/firs-ru) рядом с `FIRS_REF` и цель, скачивающую
      `src/grf/lang/russian.toml` по этому sha в `vendor/firs-ru/russian.toml` (по образцу
      `fetch-opengfx2`). Проверка: после `make fetch` файл на месте, в нём есть
      `[STR_CARGO_NAME_STEEL_BILLETS_AND_BLOOMS]` со значением «Стальные заготовки».
- [x] 1.2 Записать версию перевода в `data/meta.json` рядом с версиями Iron Horse и FIRS, чтобы
      футер приложения показывал, на каком переводе собраны названия.

## 2. Экстрактор словарей

- [x] 2.1 Создать `pipeline/extract_firs_ru.py`: импорт `firs` тем же приёмом, что в
      `extract_firs.py` (подмена `sys.argv` / `os.chdir` до импорта), и разрешение id строки
      каждого груза и предприятия через `cargo.type_name` /
      `industry.get_property("name", economy)` + `utils.unwrap_nml_string_declaration()`.
      Проверка: скрипт печатает по одной строке на элемент и находит id строки для всех
      96 грузов и 86 предприятий, включая `pipe_shop` → `STR_IND_PIPEWORK_FABRICATOR`.
- [x] 2.2 Научить экстрактор читать `vendor/firs-ru/russian.toml` (ключи `STR_*`) и
      `vendor/openttd/src/lang/russian.txt` (ключи `TTD_STR_*` без префикса, маркер `{G=…}`
      отбрасывается, падежные варианты игнорируются). Проверка: `Coal` → «Уголь» из локали игры,
      `Acid` → «Кислота» из перевода FIRS.
- [x] 2.3 Добавить перевод ванильного набора из `vanilla_cargos.json` по `STR_CARGO_PLURAL_*`.
      Проверка: `wood` → «Древесина», при этом FIRS-груз `logs` остаётся «Брёвнами».
- [x] 2.4 Создать `pipeline/ru_overrides.json` (9 записей: 5 на регистр, 2 на орфографию,
      3 на букву ё — состав в `design.md`) и применение правок с проверкой исходного значения:
      несовпадение — ошибка с указанием устаревшей записи. Проверка: подменённое ожидаемое
      значение роняет экстрактор.
- [x] 2.5 Сгенерировать `web/src/i18n/cargos.ru.json` (`names` по id, `labels`, `units`) и
      `industries.ru.json` (по id); непокрытые строки печатать отчётом. Проверка: в отчёте пусто,
      `git diff --stat web/src/i18n/` показывает ожидаемые ~82 изменённые строки.
- [x] 2.6 Подключить экстрактор к `make data` и добавить проверку на дрейф (перегенерация в
      память + сравнение с закоммиченным) в `pipeline/validate.py` или тест `pipeline/tests/`.
      Проверка: повторный `make data` не меняет файлы; ручная правка словаря роняет проверку.

## 3. Подстановка названий в интерфейсе

- [x] 3.1 Перевести `cargoName()` в `web/src/i18n/names.ts` на ключ `cargo.id` с сохранением
      фолбэка на английское имя из данных; `localiseDot()` перевести на карту `labels`.
      Проверка: `npx tsc --noEmit` и `npx oxlint` чисты.
- [x] 3.2 Обновить `web/src/i18n/__tests__/locales.test.ts`: полнота по `id` для обоих наборов
      грузов и для предприятий, кейс на `WOOD` («Древесина» против «Брёвен»), кейс на
      `localiseDot` с новым названием узла. Проверка: `cd web && npx vitest run -t "cargo names"`
      и `-t "industry names"` зелёные.
- [x] 3.3 Пройти вкладки в `make dev` и убедиться, что названия совпадают с игрой: FIRS chains на
      Steeltown (граф и карточки), Route income (выбор груза), Consist builder (фильтр по грузу),
      Best train. Отдельно — с выключенным FIRS.

## 4. Документация и завершение

- [x] 4.1 Переписать раздел «Локализация» в `CLAUDE.md` и блок `Локализация:` в
      `openspec/config.yaml`: русские названия грузов и предприятий генерируются из перевода
      игры (источник, пин, `ru_overrides.json`), руками словари не правятся.
- [x] 4.2 Добавить запись в `CHANGELOG.md` в секцию `## [Unreleased]` (на английском): русские
      названия грузов и предприятий приведены к тем, что показывает игра, и генерируются
      пайплайном.
- [x] 4.3 Прогнать `make data` и `make test` целиком; убедиться, что дерево после `make data`
      чистое, кроме ожидаемых словарей.
