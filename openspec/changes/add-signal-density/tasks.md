## 1. Настройки партии

- [ ] 1.1 Добавить в `GameSettings` (`web/src/engine/settings.ts`) поля `brakingModel`
      (`'original' | 'realistic'`, дефолт `'original'`) и `trainAccBrakingPercent` (5…200,
      дефолт 100) с JSDoc на исходники JGRPP, плюс хелпер `realisticBrakingApplies(game)` рядом
      с `linearMaintenanceApplies`; проверка — `npx vitest run` в `web/` проходит, а хелпер вне
      JGRPP возвращает `false` при сохранённом `'realistic'`
- [ ] 1.2 Показать обе настройки в JGRPP-секции `web/src/features/settings/SettingsPage.tsx`
      (`Select` для модели по образцу модели ускорения, `NumberInput` для коэффициента
      масштабирования); проверка — вне JGRPP строк нет, внутри переключение меняет сохранённое
      состояние
- [ ] 1.3 Перенести `vehicle.train_braking_model` из `INFO_SETTINGS` в читаемые настройки
      `web/src/savegame/mapping.ts` и убрать ключи `savegame.info.trainBrakingModel/
      brakingOriginal/brakingRealistic` из обоих словарей; проверка —
      `web/src/savegame/__tests__/extract.test.ts` видит настройку среди переносимых
- [ ] 1.4 Завести чтение `vehicle.train_acc_braking_percent` в `savegame/mapping.ts` **с нуля**:
      настройки нет ни в переносимых, ни в справочном списке; отсутствие в сохранении читается
      как 100 %; проверка — тест извлечения на сейве с иным значением и на сейве без него

## 2. Тормозной путь в физике

- [ ] 2.1 Экспортировать `airDrag()` из `web/src/engine/physics.ts` и добавить
      `brakingDeceleration(c, game)` с обеими ветками модели ускорения (формулы — раздел 2
      design.md); проверка — юнит-тест на ветку `original` (`clamp(acceleration×7/2, 1, 200)`)
      и на предел `cached_deceleration` 120 при `trainAccBrakingPercent = 100`
- [ ] 2.2 Добавить `brakingDistanceTiles(c, game, speedInternal, descentLevels)` — `v²/(32×decel)`;
      проверка — сверка единиц с комментарием игры (замедление 130 и 160 км/ч дают ≈6,2 клетки)
      и путь на подъёме **равен** пути по ровной линии
- [ ] 2.3 Добавить ветку спуска через uncapped-замедление, гейтя её реалистичной моделью
      ускорения (`train_cmd.cpp:912`); проверка — на спуске путь длиннее при `accelerationModel
      = 'realistic'` и равен ровному при `'original'`
- [ ] 2.4 Добавить ветку машин на магнитной подвеске (`power_w / 25` без аэродинамики, качения
      и осевого сопротивления, `train_cmd.cpp:1334-1339`); проверка — тест на маглев-пути даёт
      другое замедление, чем та же машина, посчитанная обычной веткой
- [ ] 2.5 Учесть множитель массы грузов (`freightTrains > 1`) в тормозной длине состава —
      только в ветке `AM_REALISTIC`, как в игре (`train_cmd.cpp:1288-1298`); проверка — тест:
      при реалистичной модели ускорения множитель 2 меняет путь, при оригинальной — нет

## 3. Движок плотности

- [ ] 3.1 Создать `web/src/engine/signals.ts` с `usefulSpacingTiles(...)`: реалистичная модель —
      тормозной путь + длина состава + 1,5 клетки обзора, оригинальная — длина состава;
      проверка — тест на обе ветки и на то, что интервал больше тормозного пути
- [ ] 3.2 Добавить `signalPlan(...)`: текущий интервал, рекомендованное число сигналов,
      годовое содержание сейчас и при рекомендации через `networkMaintenance`, экономия,
      флаг разреженности; проверка — тест на экономию партии 1 612 → 723 = 11 345 400
- [ ] 3.3 Обработать края: сеть без длины не считается, сеть без сигналов считается, статья
      обслуживания выключена — нули и причина; проверка — по тесту на каждый край

## 4. Состояние и интерфейс

- [ ] 4.1 Завести в `web/src/state/routeStore.ts` поле «худший спуск, уровней высоты» (дефолт 0)
      с доливкой через существующий `merge`; проверка — `state/__tests__/routeStore.test.ts`:
      состояние, сохранённое без поля, читается с нулём
- [ ] 4.2 Создать `web/src/features/route/SignalDensity.tsx` по образцу `CorridorUpgrade.tsx`
      (поле спуска, таблица: скорость, с которой считается торможение, тормозной путь, полезный
      интервал, текущий интервал, рекомендованное число, содержание сейчас → при рекомендации,
      экономия) и смонтировать третьим блоком в `RoutePage.tsx`; проверка — блок виден на
      вкладке, скорость показана в единице настроек и совпадает с панелью выше
- [ ] 4.3 Добавить предупреждение о разреженности, объяснение для оригинальной модели
      торможения и правило «интервал вдоль пути = куски между соседними головами»; проверка —
      UI-тест: при интервале вдвое реже полезного показано предупреждение и не обещана экономия
- [ ] 4.4 Добавить класс `.route-signals` в `web/src/skin.css` рядом с `.route-network`;
      проверка — `make check-visual` проходит, блок стоит в той же колонке

## 5. Строки

- [ ] 5.1 Добавить группу `signals.*` в `web/src/i18n/en.json` и `ru.json` (структура — как
      `corridor.*`: заголовок, метки полей, правило счёта, `need*`, строки таблицы, подсказка);
      проверка — `i18n/__tests__/locales.test.ts` проходит
- [ ] 5.2 Назвать настройки дословно как игра: «Модель торможения поездов» / «Коэффициент
      масштабирования ускорения/торможения поезда», варианты «оригинальное» и «реалистичное»
      (`vendor/openttd-patches/src/lang/extra/russian.txt:94,130,139`,
      `vendor/openttd/src/lang/russian.txt:1446`); в подсказке коэффициента сказать, что расчёт
      берёт только тормозную половину, а разгон в модели рейса не участвует; проверка — строки
      совпадают с локалью игры дословно

## 6. Тесты и приёмка

- [ ] 6.1 Написать `web/src/engine/__tests__/signals.test.ts`: сверка единиц тормозного пути,
      тяжёлый состав тормозит дольше, спуск удлиняет только при реалистичной модели ускорения,
      маглев считается своей веткой, экономия партии 11 345 400, обе модели торможения дают
      разные интервалы; проверка — набор зелёный
- [ ] 6.2 Добавить кейсы `brakingModel` и `trainAccBrakingPercent` в
      `web/src/engine/__tests__/settings-effect.test.ts` со своей базой и строкой `signals:` в
      снимке; проверка — тест падает, если настройку не прокинуть в расчёт
- [ ] 6.3 Написать `web/src/features/route/__tests__/SignalDensity.test.tsx` по образцу
      `NetworkMaintenance.test.tsx`; проверка — набор зелёный
- [ ] 6.4 Сверить полезный интервал на данных партии и записать полученное число в тест; если
      оно вне 12–16 клеток, назвать расхождение с исходной карточкой в `proposal.md` (раздел
      «Why») и обновить карточку FR-22 в `.scratch/feature-backlog/plan.md`; проверка — число
      зафиксировано тестом, а оба текста говорят одно и то же

## 7. Документация и закрытие

- [ ] 7.1 Добавить в `CONTEXT.md` рядом с «Track piece» и «Load threshold» термины
      **полезный интервал** (расстояние между сигнальными головами, ниже которого уплотнение
      перестаёт окупаться) и **тормозной путь**, оговорив соотношение клеток вдоль пути и
      кусков; проверка — термины употребляются в спеке и UI-строках в том же значении
- [ ] 7.2 Дописать запись в `CHANGELOG.md`, секция `## [Unreleased]` → `### Added`, на
      английском; проверка — запись описывает блок и две настройки
- [ ] 7.3 Прогнать `make verify` (data + check-i18n + тесты + check-visual); проверка — всё
      зелёное
