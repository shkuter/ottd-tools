## 0. Эталон

- [x] 0.1 Эталон получен 02.09.2026 и записан в [research-counts.md](research-counts.md):
      окна инфраструктуры обеих фикстур, количества и годовые суммы. Модель обслуживания на
      них уже сверена — все строки и оба итога совпадают до рубля, так что расхождение при
      разборе карты будет означать ошибку счётчиков, а не формулы

## 1. Чтение карты

- [ ] 1.1 Добавить чанки карты в `WANTED_CHUNKS` (`web/src/savegame/parse.ts`): `WMAP` и
      ванильные `MAPT`, `MAPO`, `M3LO`, `M3HI`, `MAP5`, `MAPE`, `MAP7`, `MAP8`; проверить на
      обеих фикстурах, что нужные чанки приходят непустыми
- [ ] 1.2 Написать `web/src/savegame/extract/tiles.ts`: читалки полей тайла по индексу
      (`type`, `m1`, `m3`, `m4`, `m5`, `m6`, `m7`, `m8`) поверх байтов чанка, без копий.
      Ветка `WMAP` — два массива подряд (`size × 8` байт `Tile`, затем `size × 4` байта
      `TileExtended`), ветка ванили — по массиву на поле; тест: обе ветки дают одни и те же
      значения на одних и тех же тайлах
- [ ] 1.3 Ветка «карта не разобрана»: `WMAP` длиной `size × 10` (расширение по 2 байта, нет
      `m8`), отсутствие чанков, длина, не сходящаяся с размером из `MAPS`; тест: читалка
      возвращает «не смогли», а не бросает и не отдаёт мусор

## 2. Подсчёт

- [ ] 2.1 `web/src/savegame/extract/infrastructure.ts`: обход тайлов, разбор типа тайла и
      владельца, аккумуляторы на компанию; тест на синтетической карте, что компании не
      смешиваются
- [ ] 2.2 Обычный путь: `CountBits(TrackBits)`, квадрат при `TracksOverlap`, второй тип путей
      у HORZ/VERT; тесты на одиночный путь, две параллели разных типов и пересечение
- [ ] 2.3 Ж/д депо: один кусок пути своему типу; тест
- [ ] 2.4 Сигналы: головы из `m3` биты 4–7; тест на клетку с двумя и с четырьмя головами
- [ ] 2.5 Станции: станционная клетка засчитывается всем типам, кроме аэропорта и буя; ж/д
      станция и вейпоинт дают ещё кусок пути, «заблокированная» клетка — не дают; автобусная
      и грузовая остановка дают по два дорожных куска каждому роду дорог; док и буй на канале
      дают клетку воды. Тесты на каждый род станции
- [ ] 2.6 Мост и тоннель: считать только с того торца, с которого считает игра; середина по
      `TUNNELBRIDGE_TRACKBIT_FACTOR`, плюс куски торцов, плюс второй тип путей на торцах;
      водный мост — середина и оба торца; тест на короткий мост известной длины
- [ ] 2.7 Переезд: два куска пути своему типу и дорожные куски по родам дорог; тест
- [ ] 2.8 Дороги и трамвайные пути: тип из `m4` / `m8`, владелец — из тайла у депо и
      дорожный владелец у остального (`m1` на обычном полотне, `m7` на прочих), депо — два
      куска; тесты на полотно, депо и полотно чужого владельца
- [ ] 2.9 Вода: клетка канала, судовое депо и шлюз по `LOCK_DEPOT_TILE_FACTOR` (средняя
      клетка шлюза — трижды по нему и каналом не считается), клетка объекта с водным классом
      канала; тесты
- [ ] 2.10 Сигналы моста с симуляцией (JGRPP): добавляются к счётчику сигналов. Проверить
      синтетической картой с таким мостом; если на `londworth-1975.sav` мостов с симуляцией
      нет, записать это прямо в тесте — иначе расхождение с окном спишут не на то правило

## 3. Снапшот

- [ ] 3.1 Положить счётчики в снапшот необязательным полем на компанию (`snapshot.ts`),
      собирать их в воркере (`worker.ts`); ассерт, что тайлы в снапшот не попадают — по
      размеру сериализованного снапшота фикстуры
- [ ] 3.2 Бампнуть версию схемы снапшота; тест, что снапшот прошлой версии предлагается
      импортировать заново (правило уже есть в спеке)
- [ ] 3.3 Сверка с партией: на `londworth-1975.sav` счётчики совпадают с окном инфраструктуры
      той партии (эталон из задачи 0.1), на `vanilla-1951.sav` разбор идёт веткой раздельных
      чанков и даёт числа этой партии. Расхождение — это ошибка правила подсчёта, а не повод
      подогнать ожидание под результат
- [ ] 3.4 Измерить время разбора на самой большой доступной карте (фикстуры маленькие,
      поэтому пересчитать замер на 16,7 млн тайлов линейно по числу тайлов) и записать
      результат в задаче; если оценка выходит за секунду-две, вернуться к раскладке чтения
      до того, как считать задачу закрытой

## 4. Мост в блок обслуживания

- [ ] 4.1 Вернуть `network` в `RoutePrefill` и `routePrefillState`
      (`state/routeStore.ts`, `features/savegame/applyBridge.ts`); тест на снятие пометки
      при правке
- [ ] 4.2 Мост с карточки компании: перенос сети выбранной компании в блок обслуживания;
      тест вкладки, что количества равны счётчикам компании и помечены
- [ ] 4.3 Мост недоступен, когда счётчиков нет, и причина названа как у остальных мостов; тест
- [ ] 4.4 Мост не трогает остальные входы вкладки; тест
- [ ] 4.5 Строки интерфейса в `en.json` / `ru.json`; проверить `i18n/__tests__/locales.test.ts`

## 5. Сверка модели

- [ ] 5.1 Тест: годовой итог `networkMaintenance` на счётчиках и настройках импортированной
      партии совпадает с итогом окна инфраструктуры этой партии (эталон из задачи 0.1),
      аэропорты исключены явно

## 6. Завершение

- [ ] 6.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском, с пометкой
      `**BREAKING**`: смена версии схемы снапшота сбрасывает сохранённую партию
- [ ] 6.2 Прогнать `make test` и `make check-visual`; оба зелёные
