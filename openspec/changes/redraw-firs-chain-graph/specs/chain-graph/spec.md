## Purpose

Граф цепочек грузов экономики FIRS: как он собирается из данных набора и как читается —
узлы, цвета, дубли, подписи, выбор узла и навигация по полотну.

## ADDED Requirements

### Requirement: Узел предприятия показывает предприятие как в игре

Узел предприятия SHALL показывать картинку этого предприятия из набора и его имя на языке
интерфейса. Под именем SHALL стоять режим приёма входов, а для грузов снабжения — строки
«Требует: <груз>» по каждому принимаемому грузу снабжения и «Производит: <груз>» по каждому
производимому; имена грузов SHALL браться из словаря названий, как и везде. Размер узла
SHALL NOT зависеть от языка: длинное имя переносится или обрезается внутри той же рамки.

#### Scenario: Прокатный стан Steeltown

- **WHEN** пользователь смотрит узел листопрокатного стана в экономике Steeltown
- **THEN** в узле картинка стана, его имя и строка «Требует: Расходные сварочные материалы»,
  а самого груза сварочных материалов узлом на графе нет

#### Scenario: Смена языка не двигает граф

- **WHEN** пользователь переключает язык интерфейса
- **THEN** подписи узлов меняются, а положение узлов и рёбер остаётся прежним

### Requirement: Груз и его рёбра окрашены цветом груза из игры

Узел груза SHALL быть бейджем с именем груза на заливке цвета этого груза, а каждое ребро
груза SHALL быть того же цвета. Цветом груза SHALL быть цвет, которым игра рисует этот груз
в выбранной экономике, — индекс палитры игры, а не подобранный оттенок; цвет одного груза
в разных экономиках MAY отличаться, как отличается в игре.

#### Scenario: Цвет угля совпадает с игрой

- **WHEN** пользователь смотрит бейдж угля и его рёбра в экономике Steeltown
- **THEN** они окрашены тем же цветом палитры, что индекс, который FIRS назначает углю в
  этой экономике

### Requirement: Раскладка следует тюнингу набора, а не собственному правилу

Граф SHALL повторять приёмы читаемости, которые экономика набора задаёт для своей
документации: грузы из списка дублей SHALL получать отдельный узел у каждого производителя
(дубль на выпуск) или у каждого потребителя (дубль на приём); предприятия из списка
«wormhole» SHALL NOT получать рёбер от принимаемых грузов — вместо ребра в бейдже груза
SHALL стоять строка «На <предприятие>», а в узле самого предприятия — строка «Требует:
<груз>»; ранги, кластеры и группы рёбер SHALL передаваться раскладчику как заданы.
Городские предприятия (магазины, гостиница) SHALL быть wormhole в любой экономике и SHALL
NOT рисоваться узлами — так их держит сам набор. Экономика без тюнинга SHALL рисоваться без
дублей, и строки «На …» в ней SHALL называть только городские предприятия — как она
нарисована у самого набора. Собственного правила дублирования «по числу связей» SHALL NOT
быть.

#### Scenario: Шлак у трёх печей

- **WHEN** шлак стоит в списке дублей на выпуск экономики Steeltown
- **THEN** у каждой печи, производящей шлак, свой бейдж «Шлак», связанный с общим узлом шлака

#### Scenario: Причал как wormhole

- **WHEN** причал стоит в списке wormhole-предприятий, а цемент им принимается
- **THEN** ребра от цемента к причалу нет, в бейдже цемента стоит строка «На причал», а в
  узле причала — «Требует: Цемент»

#### Scenario: Экономика без тюнинга

- **WHEN** выбрана экономика, у которой набор не задал дублей и wormhole-предприятий
- **THEN** каждый груз — один узел, строки «На …» есть только у грузов, которые принимают
  городские предприятия, и граф читается как у набора

### Requirement: Грузы снабжения не рисуются узлами

Грузы, которые набор сам исключает из своего графа как снабжение, SHALL NOT рисоваться
узлами и рёбрами: их место — строки «Требует» и «Производит» в узле предприятия. Пассажиры и
почта SHALL NOT рисоваться вовсе.

#### Scenario: Сельхозтовары

- **WHEN** экономика Steeltown содержит сельхозтовары, которые попутно выпускает завод
  помола шлака
- **THEN** узла сельхозтоваров на графе нет, а в узле завода стоит «Производит: Сельхозтовары»

### Requirement: Дубль — тот же груз

Клик по любому дублю груза SHALL выбирать сам груз: подсвечиваются все его дубли, общий
узел и все узлы, связанные с ним, а в карточке показывается груз. Обход связанных узлов
SHALL идти по грузу и предприятию, а не по дублю: дубль SHALL NOT считаться самостоятельным
узлом графа.

#### Scenario: Клик по дублю шлака

- **WHEN** пользователь щёлкает по бейджу «Шлак» у кислородного конвертера
- **THEN** подсвечены все бейджи шлака, общий узел шлака, его производители и потребители,
  а в карточке — шлак

### Requirement: Полотно с зумом, панорамированием и поиском

Граф SHALL показываться на полотне фиксированной высоты, которое масштабируется колесом и
кнопками и двигается перетаскиванием; кнопка «вписать» SHALL показывать граф целиком. Обычный
клик по узлу SHALL доходить до узла: указатель SHALL захватываться полотном только с началом
перетаскивания — захват с самого нажатия уводит клик на полотно, и выбор узла мышью
перестаёт работать, оставаясь исправным при программном клике.
Поиск по узлу SHALL предлагать предприятия и грузы экономики по их именам на языке
интерфейса; выбор SHALL центрировать узел на полотне, выбрать его и подсветить связанные
узлы. При увеличении выше натуральной величины узлы предприятий в поле зрения SHALL
показывать картинку полного размера, остальные — уменьшенную.

#### Scenario: Вписать граф

- **WHEN** пользователь нажимает «вписать»
- **THEN** весь граф виден на полотне целиком

#### Scenario: Поиск шахты

- **WHEN** пользователь выбирает в поиске «Угольная шахта»
- **THEN** полотно центрируется на узле шахты, узел выбран, его связи подсвечены

#### Scenario: Картинка при увеличении

- **WHEN** пользователь увеличил полотно выше натуральной величины
- **THEN** у узлов в поле зрения картинка полного размера, а у узлов за его пределами —
  уменьшенная

#### Scenario: Клик мышью по узлу

- **WHEN** пользователь кликает мышью по узлу предприятия на полотне
- **THEN** выбран этот узел, а не сброшен выбор, и это проверено настоящим указателем в
  браузере, а не только программным событием

### Requirement: Выбор узла и режим цепочки живут вместе

Выбор узла на полотне, подсветка, боковая карточка, панель цели и список задач снабжения
SHALL оставаться на вкладке в прежних отношениях: клик по предприятию по-прежнему делает
его целью обратного обхода, смена экономики сбрасывает и выбор, и цель.

#### Scenario: Клик по печи делает её целью

- **WHEN** пользователь щёлкает по узлу доменной печи
- **THEN** печь выбрана, её связи подсвечены, а список задач снабжения строится от неё
