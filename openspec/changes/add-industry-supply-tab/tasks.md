## 1. Сводный расчёт

- [x] 1.1 Расширить `web/src/engine/supply.ts` расчётом по набору входов: конверсия из
      удерживаемых входов (с потолком суммы ratio, пограничные входы считаются поданными),
      признак неполноты при незаданных, отказ считать получателя с неизвестным правилом
- [x] 1.2 Определение узкого места: вход, ограничивающий конверсию, с различением
      «дотягивается парком» и «упирается в ограничение»
- [x] 1.3 Накопительный пул на уровне предприятия: суммарный объём за окно, уровень
      производства
- [x] 1.4 Тесты: все входы поданы, часть не задана, один выпал, выпавший вход при упёршейся в
      потолок сумме ratio (конверсия не меняется), пограничный вход, конверсия максимальна
      (узкого места нет), неизвестное правило снабжения, пул по обоим порогам

## 2. Состояние вкладки

- [x] 2.1 Завести состояние формы вкладки (выбранное предприятие, расстояние и выпуск источника
      по каждому входу) с persist по существующему образцу сторов
- [x] 2.2 Тест: состояние переживает перезагрузку и не ломается при смене экономики или
      предприятия

## 3. Вкладка

- [x] 3.1 Создать `web/src/features/industry-supply/` с ленивой загрузкой и маршрутом в оболочке
- [x] 3.2 Выбор предприятия (входы — из активной экономики) и таблица входов: расстояние и
      выпуск источника на вход, подобранный флот, отношение к окну, состояние входа
      (удерживается / пограничный / выпал / не задан)
- [x] 3.3 Действие «то же расстояние всем входам», заполняющее поля видимыми значениями
- [x] 3.4 Итог: конверсия предприятия, признак неполноты, узкое место словами игрока
- [x] 3.5 Ветка первичных предприятий и портов: накопительный пул вместо конверсии
- [x] 3.6 Предприятие без входов и получатель с неизвестным правилом снабжения: сообщение
      вместо формы и итога
- [x] 3.7 Смена экономики: выбор предприятия сбрасывается, итог не считается по чужой экономике

## 4. Локализация

- [x] 4.1 Строки вкладки в `web/src/i18n/en.json` и `ru.json`, прогнать
      `i18n/__tests__/locales.test.ts`

## 5. Сверка с игрой

- [x] 5.1 Стенд на чистой карте (Steeltown, читы на деньги): угольная шахта → коксовая печь,
      один поезд, плечо задаётся длиной петли пути. Вход COAL идёт ratio 8, поэтому выпадение
      из окна роняет конверсию в ноль — печь встаёт целиком. Убедиться, что вкладка на тех же
      вводных показывает интервал/окно > 1, состояние «выпадает из окна» и конверсию 0 %
- [x] 5.2 Добавить на маршрут столько поездов, сколько назвала вкладка, и убедиться, что печь
      выходит на постоянный выпуск; записать наблюдение (что показывала вкладка, что стало
      в игре)

## 7. Правки по ревью

- [x] 7.1 Узкое место: брать наименьший парк среди рассмотренных составов, а не у победителя
      строки; при нескольких необслуживаемых входах называть тот, чьё ratio больше
- [x] 7.2 Итог не показывает посчитанное там, где расчёта не было: при отсутствии заданных
      входов не выводить ни конверсию 0 %, ни пул «0 → 100 %»
- [x] 7.3 Сообщение «Все входы поданы» выводить только когда они действительно все поданы —
      случай выпавшего входа при упёршейся в потолок сумме ratio нуждается в своей фразе
- [x] 7.4 Убрать мёртвую строку `supply.needsFirs` из обоих словарей: вкладка при выключенном
      FIRS не показывается
- [x] 7.5 Стили состояний входа: `.supply-marginal` и `.supply-unset` (сейчас окрашены только
      «подан» и «выпадает»)
- [x] 7.6 Перевести `state/__tests__/industrySupplyStore.test.ts` на английский — новый файл
      пишется на языке остального кода
- [x] 7.7 Вернуть завершающий перевод строки в `i18n/en.json` и `ru.json`
- [x] 7.8 Тесты на 7.1–7.3 и прогон `make verify`

## 8. Правки по второму ревью

- [x] 8.1 Длину состава брать из `OptimizeResult.lengthTiles`, а не пересчитывать в компоненте:
      инвариант «тайл = 16 единиц» живёт в движке
- [x] 8.2 Предприятие с пулом, у которого ни один заданный маршрут не даёт объёма: не показывать
      «привозят 0 → 100 % базового выпуска», а сказать, что маршруты не собрать
- [x] 8.3 `reachable` не должен склеивать «нет состава» и «парк не посчитан»: сообщение про
      пустой список покупки выдавать только по факту отсутствия состава
- [x] 8.4 Тесты на `features/industry-supply/inputs.ts` (`leanestOf`, `isRouted`, ветка
      «нечем возить»), сейчас модуль не покрыт
- [x] 8.5 Мелочи: устаревший комментарий в тесте «нечем возить несколько входов»; убрать
      недостижимый дефолт `?? 1` в `bottleneckOf`
- [x] 8.6 Дописать в `CONTEXT.md` понятия узкого места, состояния входа и неполного итога
- [x] 8.7 Прогнать `make verify`

## 9. Рефакторинг по ревью

Числа не меняются — только форма кода. Проверка: та же выдача до и после.

- [x] 9.1 Общие параметры перебора: `year`, `stationTiles`, `maxTrains`, `allowElectric` с одними
      дефолтами для `optimizerStore` и `industrySupplyStore` (сейчас продублированы поле в поле,
      и правка дефолта разъедет вкладки)
- [x] 9.2 Один хелпер подписи состава на обе вкладки: сейчас «N× имя» собирается тремя слегка
      разными способами (`consistLabel`, инлайн в ячейке снабжения, строка оптимизатора)
- [x] 9.3 Узкое место должно отдавать вход, а не лейбл: `cargoLabelName` и `leanestLabel` дважды
      ищут один и тот же `run` по строке
- [x] 9.4 Ветвление по правилу снабжения вынести из JSX во вью-модель: каскад
      `conversion / pool / неизвестное` сейчас живёт и в движке, и четырьмя ветками в разметке
- [x] 9.5 Прогнать `make verify` и убедиться, что числа на обеих вкладках не изменились

## 6. Завершение

- [x] 6.1 Запись в `CHANGELOG.md`, секция `[Unreleased]`, на английском
- [x] 6.2 Прогнать `make test` и `make verify`
